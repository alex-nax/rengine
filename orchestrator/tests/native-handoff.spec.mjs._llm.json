{
  "version": 1,
  "entries": [
    {
      "id": "instrumented-resume-consumer",
      "kind": "rationale",
      "anchor": {
        "start": 13,
        "end": 13,
        "snippet": "test('handoff waits for a presented native pane and reload retains the once-resumed CLI', { timeout: 60000 }, async () => {"
      },
      "note": "Use an instrumented managed Codex executable and synthetic local session metadata in a temporary project with the real Bash launcher, PTY and native launcher. Verify no invocation before presentation, explicit resume/MCP argv once, concurrent session creation, interactive output, dirty draft recovery, repeated launch and explicit Stop followed by a fresh waiting session. First exercise the native keyboard shortcut, then call list_desktops/reload_desktop through actual MCP stdio and observe a replacement desktop ID, second build, same CLI PID, preserved dirty text and one invocation. Reject the stale target. This does not reopen the active real conversation or claim provider continuation, exact edit insertion position, or Windows qualification.",
      "refs": [
        "docs/specs/058-orchestrator-handoff.md",
        "docs/specs/062-agent-desktop-actions.md"
      ]
    }
  ],
  "source": {
    "path": "orchestrator/tests/native-handoff.spec.mjs",
    "sha256": "135a03123e021eca53abc9717380fb596178830c52defed73d244e5f6ee7a060",
    "format": "text",
    "format_fingerprint": "05db8feb0843c51a026668bb69cc122617afc04000263bbb759093d8bf2f314e"
  }
}
