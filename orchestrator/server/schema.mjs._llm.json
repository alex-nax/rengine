{
  "version": 1,
  "entries": [
    {
      "id": "subset-scope",
      "kind": "constraint",
      "anchor": {
        "start": 5,
        "end": 5,
        "snippet": "export function validateSchema(schema, value, root = schema, at = '$') {"
      },
      "note": "Deliberately a bounded Draft 2020-12 subset (type, const, enum, string/number/array bounds, uniqueItems, properties/required/additionalProperties, items/prefixItems, allOf, local $ref) so the committed contracts stay the machine-readable truth without adding an npm dependency. A keyword outside the subset is silently ignored, so contracts must only use listed keywords; extend the validator and its tests together when a contract needs more. Type mismatches stop deeper checks to avoid cascades. Property and required checks use Object.hasOwn so prototype names such as toString are unknown keys, not matches (review finding 2). additionalProperties may also be a schema (used for env maps); keys stay unchecked there and cross rules constrain them.",
      "refs": [
        "docs/specs/074-project-format-registry.md",
        "contracts/project-v1.schema.json"
      ]
    }
  ],
  "source": {
    "path": "orchestrator/server/schema.mjs",
    "sha256": "0b5dc31925cc3e32011c023f66b8ddd5b53e6e096e8e17fb7ac98cfea76b0400",
    "format": "text",
    "format_fingerprint": "05db8feb0843c51a026668bb69cc122617afc04000263bbb759093d8bf2f314e"
  }
}
