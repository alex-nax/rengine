{
  "version": 1,
  "entries": [
    {
      "id": "root-bound-replaceable-tools",
      "kind": "rationale",
      "anchor": {
        "start": 16,
        "end": 16,
        "snippet": "const scopedState = async () => {"
      },
      "note": "Tool implementation lives in a replaceable process behind the stable facade. Resolve only a validated runtime descriptor for the original host, retain the original root and reject foreign sessions/desktops before each operation. Probe/update/status never launches a coding conversation. The existing eight-tool connector can remain running while an agent uses runtime/client.mjs with its original context.",
      "refs": [
        "docs/specs/065-layered-workspace-updates.md"
      ]
    },
    {
      "id": "linked-project-operations",
      "kind": "rationale",
      "anchor": {
        "start": 73,
        "end": 73,
        "snippet": "const windowCapability = state => { if (state.capabilities.projectWindows !== 1) throw new Error('Project-window control needs the current runtime supervisor. Use the documented context-bound bootstrap.'); };"
      },
      "note": "New window and report tools retain the connector\u2019s captured root. Opening a window attaches an existing root-bound agent; only the recorded origin/project pair can inspect its window or exchange reports. The origin can explicitly view its linked project inbox or attribute its own project-side observation without claiming another agent wrote it.",
      "refs": [
        "docs/specs/069-project-window-dogfooding.md"
      ]
    },
    {
      "id": "interactive-script-tools",
      "kind": "rationale",
      "anchor": {
        "start": 88,
        "end": 88,
        "snippet": "const scriptCapability = state => { if (state.capabilities.scriptActions !== 1) throw new Error('Update the workspace worker before opening script tabs.'); };"
      },
      "note": "Script execution is effectful and not idempotent; root-bound tools require an explicit script path, literal argv and a target desktop. View attachment failure returns a retained process identity. show_session attaches that process without another execution, and ordinary stop_session remains the explicit termination action.",
      "refs": [
        "docs/specs/071-interactive-script-tabs.md"
      ]
    },
    {
      "id": "preview-file-tool",
      "kind": "rationale",
      "anchor": {
        "start": 107,
        "end": 107,
        "snippet": "const PREVIEW_BUDGET = 32000;"
      },
      "note": "preview_file runs the project's own declared executable, so it is open-world and not read-only even though it never writes. The route returns the whole sanitized tree; the tool slices dir/depth and pages files with offset/limit, then shrinks depth and limit until the serialized reply fits the 32,000-character budget, reporting truncated/nextOffset (review finding 5). Command metadata is made root-relative and the generic tool wrapper replaces the absolute root path in error text with <root>. Entry text is bounded like read_file; the hex window is dropped because agents get size and SHA-256 instead.",
      "refs": [
        "docs/specs/074-project-format-registry.md"
      ]
    },
    {
      "id": "dashboard-tools",
      "kind": "rationale",
      "anchor": {
        "start": 92,
        "end": 92,
        "snippet": "const dashboardCapability = state => { if (state.capabilities.dashboard !== 1) throw new Error('This retained service predates the project dashboard. Update the workspace layer first.'); };"
      },
      "note": "dashboard_actions is read-only (availability only); dashboard_capture is open-world because it runs a project executable and writes files. Script and log actions deliberately get no new tool: scripts go through open_script with the listed script/args/env, logs start from the desktop tab and are followed with show_session/session_output, so process creation stays on the existing audited paths.",
      "refs": [
        "docs/specs/075-project-dashboard.md"
      ]
    },
    {
      "id": "declared-game-tools",
      "kind": "rationale",
      "anchor": {
        "start": 136,
        "end": 136,
        "snippet": "const gameCapability = state => { if (state.capabilities.projectGame !== 1) throw new Error('This retained service predates per-project game declarations. Update the workspace layer first.'); };"
      },
      "note": "game_preflight and launch_game address only the games the bound project declares in .rengine/project.json; their descriptions name no game, and the connector never carries a built-in executable or data path. Both take an optional gameId and default to the first declared record, so a single-game project needs no argument while a multi-game one is addressed explicitly and an unknown id is answered with the declared ids. launch_game is open-world because it executes the project's own program; it returns the running session of that same game id when one exists, so agents inspect the returned id rather than assuming a fresh process, and two declared games of one project can be running at once. The removed nolf_preflight/launch_nolf have no alias: nothing had shipped them to a user.",
      "refs": [
        "docs/specs/078-project-game-declaration.md"
      ]
    }
  ],
  "source": {
    "path": "orchestrator/agents/mcp-worker.mjs",
    "sha256": "fc0e2e11b3a7c863b13b5e37ae31e54e527a62741157c5bde9ceaa46f0c38ac5",
    "format": "text",
    "format_fingerprint": "05db8feb0843c51a026668bb69cc122617afc04000263bbb759093d8bf2f314e"
  }
}
