{
  "version": 1,
  "entries": [
    {
      "id": "pinned-contract-skip-rule",
      "kind": "rationale",
      "anchor": {
        "start": 216,
        "end": 216,
        "snippet": "supported = contract.get(\"enum\", [contract[\"const\"]] if \"const\" in contract else [])"
      },
      "note": "The pinned tier reads which contracts the pinned schema accepts and skips with that reason when the project declares a newer one, so a consumer can land a contract-3 declaration before the pin carries contract 3 without a red test. The skip disappears by itself at the pin bump, which is the point: the test then starts validating against rEngine's own schema and reader with no edit.",
      "refs": [
        "docs/runbooks/project-integration.md"
      ]
    }
  ],
  "source": {
    "path": "orchestrator/templates/project/test_rengine_project_decl.py",
    "sha256": "9c71792aafb6035c57d147a77416df469263803a01d1358d4c182e7785c2c7f3",
    "format": "python",
    "format_fingerprint": "a67a51640399e60d6402a43d48acddd852a615617d5502a24e0e77d080b238af"
  }
}
