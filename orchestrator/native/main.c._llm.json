{
  "version": 1,
  "entries": [
    {
      "id": "native-view-lifetime",
      "kind": "rationale",
      "anchor": {
        "start": 30,
        "end": 30,
        "snippet": "int main(int argc, char **argv) {"
      },
      "note": "The C desktop owns only native views, renderer resources and service connections. Normal exit waits for durable draft/layout requests before detaching. The event-driven loop schedules one settling frame when microui next_hover_root differs from hover_root; without it the first click after entering another container can miss because hover is computed from the previous frame. Static workspaces do not repaint continuously. --automation allocates optional inspection metadata and is a local testing seam, not a production remote-control endpoint. Automation windows have a distinct title so a visible regression fixture is distinguishable from the owner's workspace.",
      "refs": [
        "docs/specs/056-native-desktop.md"
      ]
    },
    {
      "id": "native-reload-and-presentation",
      "kind": "rationale",
      "anchor": {
        "start": 119,
        "end": 119,
        "snippet": "    re_draw_end(draw);"
      },
      "note": "Only after presentation, for visible non-minimized terminal content, report readiness to the attached session. Cmd/Ctrl+Shift+R consumes the shortcut before terminal/editor/game dispatch. An authenticated desktop action sets the same closing/reload flags after the app tick. Both use normal quit persistence before exit code 75; failed persistence cancels reload and quit. The launcher rebuilds and reconnects; native exit never stops the service or agent. Direct binary launches report the required launcher. microui wheel input uses the shared signed precise accumulator, converting vertical deltas to content offsets without undoing system inversion.",
      "refs": [
        "docs/specs/058-orchestrator-handoff.md",
        "docs/specs/062-agent-desktop-actions.md",
        "docs/specs/061-pane-scroll-controls.md"
      ]
    },
    {
      "id": "legacy-launcher-bootstrap",
      "kind": "rationale",
      "anchor": {
        "start": 43,
        "end": 43,
        "snippet": "if (!automation && !smoke && getenv(\"RENGINE_CAN_RELOAD\") && !getenv(\"RENGINE_LAYERED_CHILD\")) {"
      },
      "note": "Before SDL or any native worker thread starts, the first new binary reached by the old launcher delegates to the update supervisor. Successful handoff exits this bootstrap process without opening a duplicate window. Managed children suppress delegation; explicit smoke/automation clients keep the direct inspection path. Failure visibly falls back to the retained workspace. No CLI is created or resumed by this bridge.",
      "refs": [
        "docs/specs/065-layered-workspace-updates.md"
      ]
    },
    {
      "id": "restricted-window-control",
      "kind": "rationale",
      "anchor": {
        "start": 89,
        "end": 89,
        "snippet": "const char *op = re_string(command, \"op\"); int id = re_number(command, \"id\");"
      },
      "note": "Managed production views accept only control-state, control-snapshot, control-focus and control-close over their supervisor-owned stdin pipe. Test keyboard/mouse/text commands remain gated by --automation. Snapshots are taken after drawing; close uses the normal draft/input-drain lifecycle. The supervisor selects private snapshot paths and correlates negative request IDs; these commands never launch/stop agents.",
      "refs": [
        "docs/specs/069-project-window-dogfooding.md"
      ]
    }
  ],
  "source": {
    "path": "orchestrator/native/main.c",
    "sha256": "5b550c7a17fe13f133a5d01054bdbea6302793a9d472e16a87978d1bd3a0cac6",
    "format": "c",
    "format_fingerprint": "6893201a8120b54c4ae86198441a791fd50e0731d85917c6c1fe0e5fb8ad9627"
  }
}
