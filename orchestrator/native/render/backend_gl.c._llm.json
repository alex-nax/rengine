{
  "version": 1,
  "entries": [
    {
      "id": "sdf-shapes",
      "kind": "rationale",
      "anchor": {
        "start": 34,
        "end": 51,
        "snippet": "static const char *fragment_source =",
        "symbol": "fragment_source"
      },
      "note": "One program serves every command: mode 0 is a plain quad (rects, frame highlight) with no feather so current-UI scenes stay pixel-exact against the SDL reference; modes 1-3 evaluate a rounded-box signed distance in drawable pixels with a one-pixel feather (fill), an outer-minus-inner band (ring, also the 1px frame border) and a smoothstep falloff over the shadow width; mode 4 samples the coverage atlas, mode 5 an RGBA texture. Per-corner radii come from the corner mask so tabs can round only their top. This is the anti-aliased behaviour the owner chose in spec 068; Metal and Vulkan reuse the same shader logic.",
      "refs": [
        "docs/specs/068-opengl-adapter.md"
      ]
    },
    {
      "id": "gradient-strips",
      "kind": "rationale",
      "anchor": {
        "start": 89,
        "end": 109,
        "symbol": "gradient",
        "snippet": "static void gradient(GlBackend *b, const ReCommand *c) {"
      },
      "note": "The OpenGL adapter draws a ramp as one quad per logical pixel, each carrying the whole rectangle's shape parameters. The vertex format already interpolates colour, so a single quad with two colours would have been cheaper, but then this adapter's ramp would be the graphics card's interpolation while the SDL reference stepped it in software, and the comparison in tools/render_compare.py would be measuring the difference between two definitions of a gradient. Stepping every adapter through re_gradient_sample makes the ramp part of the contract instead. Passing the full shape rather than the strip keeps the signed-distance field rounding the ends, so a rounded track needs no special case, and a slider's track costs a couple of hundred quads, which is nothing beside a frame of text.",
      "refs": [
        "orchestrator/native/render/draw_list.h",
        "docs/specs/067-draw-list-contract.md"
      ]
    },
    {
      "id": "reference-parity",
      "kind": "constraint",
      "anchor": {
        "start": 116,
        "end": 135,
        "snippet": "static Glyph *glyph(GlBackend *b, uint8_t face, int16_t size, uint32_t cp) {",
        "symbol": "glyph"
      },
      "note": "Glyph quads land at x*density+dx and (y+ascent+2)*density+dy, which is exactly where the SDL reference's float rectangle scales to at integer density, and the atlas is sampled with GL_NEAREST at texel-aligned coordinates, so text is compared under the tight current-UI tolerance. The atlas is a single 2048\u00b2 R8 texture packed on shelves; when it fills, the batch is flushed and the atlas repacked from scratch instead of growing, keeping memory bounded. Blending uses separate alpha factors to reproduce SDL_BLENDMODE_BLEND. Changing any of this changes the comparison numbers in the evidence.",
      "refs": [
        "docs/specs/068-opengl-adapter.md",
        "orchestrator/native/render/backend_sdl.c._llm.json"
      ]
    }
  ]
}
