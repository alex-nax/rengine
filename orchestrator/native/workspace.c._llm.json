{
  "version": 1,
  "entries": [
    {
      "id": "native-focus-and-detach",
      "kind": "rationale",
      "anchor": {
        "start": 238,
        "end": 238,
        "snippet": "bool re_app_event(ReApp *a, const SDL_Event *e, ReDraw *draw) {"
      },
      "note": "Pointer focus selects a view without changing its root/session binding. Toolbar focus clears custom editor/terminal/game keyboard routing. Detach never calls Stop. Wheel targets the visible hovered terminal/editor; negotiated terminal reporting gets first refusal before native history. Mouse motion/buttons route to an existing held terminal first, otherwise the hovered visible terminal; layout drags do not become application motion. Hover never changes keyboard focus. Focus loss releases all terminal holds, focus transfer releases the previous terminal, and hidden views release during drawing. Native UI still receives mouse motion to settle microui hover; consumed application buttons are not forwarded to chrome.",
      "refs": [
        "docs/specs/056-native-desktop.md",
        "docs/specs/061-pane-scroll-controls.md",
        "docs/specs/063-terminal-mouse-reporting.md"
      ]
    },
    {
      "id": "inspection-control-geometry",
      "kind": "rationale",
      "anchor": {
        "start": 10,
        "end": 10,
        "snippet": "void re_app_control(ReApp *a, mu_Context *ui, const char *role, const char *key, int tab) {"
      },
      "note": "Only explicit automation allocates the controls array. Record at most 512 visible control rectangles per rendered frame, preserving actual microui actions and tab/session identity. Ordinary widgets are clipped to their container. Upstream overflow scrollbar geometry is outside the content clip and is read directly from the container after mu_end_window; do not consult the popped clip stack there. Production rendering allocates none of this metadata. Tests use ordinary SDL pointer/wheel events to reach controls; they cannot bypass scrolling or call button handlers by name. Exported so owned view modules (formatview.c) register their controls without duplicating the clip arithmetic.",
      "refs": [
        "docs/specs/055-launch-command-qualification.md",
        "docs/specs/061-pane-scroll-controls.md"
      ]
    },
    {
      "id": "bounded-tab-strip",
      "kind": "rationale",
      "anchor": {
        "start": 96,
        "end": 96,
        "snippet": "static void pane_header(ReApp *a, mu_Context *ui, int n) {"
      },
      "note": "Visible tab slots and previous/next controls fit the measured pane header; only displayed tabs receive hit rectangles. Strip offsets are ephemeral and can browse away from the selected content without retargeting it. Explicit layout changes invalidate the cached width to reveal the selected tab even when pane indices are reused. Drops choose insertion boundaries from visible header midpoints, then adjust for removal when source and destination are the same pane. Root/session identity stays on ReTab.",
      "refs": [
        "docs/specs/057-native-pane-navigation.md"
      ]
    },
    {
      "id": "format-tab-layout",
      "kind": "rationale",
      "anchor": {
        "start": 75,
        "end": 75,
        "snippet": "static void editor_ui(ReApp *a, mu_Context *ui, int index, mu_Rect content, mu_Rect below) {"
      },
      "note": "A format-aware editor tab draws the mode toolbar first; text mode keeps the Save/Discard row below it and shifts the buffer down by one row, raw and text previews take the whole content area as a draw-list rect, and a tree preview lives inside the scrollable microui window with the selected entry drawn in the reserved lower split. Actions returned by the view are performed after drawing so request/mode changes never interleave with layout. The third root container per pane is avoided on purpose: the entry area is a draw-list rect, not another microui window (upstream's 32-root limit).",
      "refs": [
        "docs/specs/074-project-format-registry.md"
      ]
    }
  ],
  "source": {
    "path": "orchestrator/native/workspace.c",
    "sha256": "98538f2991adaa8eaa8496edc49ece5d8b726f8a957b3c4effc4c77765ecb150",
    "format": "c",
    "format_fingerprint": "6893201a8120b54c4ae86198441a791fd50e0731d85917c6c1fe0e5fb8ad9627"
  }
}
