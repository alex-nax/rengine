{
  "version": 1,
  "entries": [
    {
      "id": "view-state-ownership",
      "kind": "rationale",
      "anchor": {
        "start": 5,
        "end": 5,
        "snippet": "struct ReFormatView {"
      },
      "note": "One view owns everything a format-aware tab needs beyond the text editor: mode and whether the user chose it, the raw window, the sanitized preview, a read-only text buffer, and the selected entry with its own hex/text/error. The UI function only mutates view state and returns an action; app.c owns every request so mode changes, refreshes and entry loads share the generation/staleness rules of ordinary tab loads.",
      "refs": [
        "docs/specs/074-project-format-registry.md"
      ]
    },
    {
      "id": "glob-parity",
      "kind": "constraint",
      "anchor": {
        "start": 66,
        "end": 66,
        "snippet": "bool re_format_glob(const char *g, const char *s) {"
      },
      "note": "Mirrors the service matcher (case-insensitive *, ? and [...] with ! negation on the base name) so the client's default-mode decision agrees with the server's format choice; keep both in step when extending the glob subset.",
      "refs": [
        "docs/specs/074-project-format-registry.md",
        "orchestrator/server/formats.mjs"
      ]
    },
    {
      "id": "tree-in-microui",
      "kind": "rationale",
      "anchor": {
        "start": 121,
        "end": 121,
        "snippet": "static int tree_rows(ReFormatView *v, ReApp *a, mu_Context *ui, const cJSON *node, const char *prefix, int depth, int tab, bool entries) {"
      },
      "note": "Directories use upstream treenodes keyed by their joined path (siblings may repeat names at different levels); expanded state lives in microui's 48-slot pool, which is enough for archive browsing. Depth is capped by RE_METRIC_FORMAT_TREE_DEPTH because each level pushes two ids onto microui's 32-entry id stack. Files become left-aligned buttons only when the format declares an entry command; otherwise they are labels so a click cannot promise bytes the project cannot produce.",
      "refs": [
        "docs/specs/074-project-format-registry.md"
      ]
    }
  ],
  "source": {
    "path": "orchestrator/native/formatview.c",
    "sha256": "00dd417bcd1b262897f8eb367949fa8fd3a5e45526b88bf0d3de12be2ef86d5d",
    "format": "c",
    "format_fingerprint": "6893201a8120b54c4ae86198441a791fd50e0731d85917c6c1fe0e5fb8ad9627"
  }
}
