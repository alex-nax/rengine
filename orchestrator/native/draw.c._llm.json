{
  "version": 1,
  "entries": [
    {
      "id": "generated-theme",
      "kind": "constraint",
      "anchor": {
        "start": 84,
        "end": 86,
        "snippet": "void re_draw_bind(ReDraw *d, mu_Context *ui) {",
        "symbol": "re_draw_bind"
      },
      "note": "Colours, microui metrics, the font pixel size and the line height come from orchestrator/native/theme.h, which `python3 tools/design.py generate` derives from design/tokens.json; the same tokens feed the Claude Design preview cards. Edit the tokens and regenerate, never the header: `tools/design.py check` rejects a stale header and any mu_color/vterm_color_rgb literal outside the palette. Applying upstream microui's default slots explicitly here is behaviour-neutral (identical values) and makes every slot designable. Every owned native source uses RE_COLOR_* through common.h; `check` fails on any numeric colour literal, so a new colour starts as a token.",
      "refs": [
        "docs/specs/064-design-system-handoff.md",
        "design/tokens.json",
        "tools/design.py"
      ]
    },
    {
      "id": "deferred-flush",
      "kind": "constraint",
      "anchor": {
        "start": 102,
        "end": 106,
        "snippet": "static void flush(ReDraw *d) {",
        "symbol": "flush"
      },
      "note": "The list is flushed exactly once per frame by whichever of re_draw_snapshot or re_draw_end runs first, because main.c snapshots before presenting. Commands execute in append order with the same adapter calls the former immediate-mode path issued; that is what keeps native snapshots byte-identical across the contract change (spec 067). Views must append to the list, never draw directly, and anything above the list must not name graphics-API rendering symbols (tools/design.py check enforces it).",
      "refs": [
        "docs/specs/067-draw-list-contract.md",
        "orchestrator/native/render/draw_list.h"
      ]
    },
    {
      "id": "renderer-names-are-seam-copies",
      "kind": "rationale",
      "anchor": {
        "start": 44,
        "end": 48,
        "symbol": "re_draw_select",
        "snippet": "if (!strcmp(choice, \"opengl\")) return \"opengl\";"
      },
      "note": "Each of these three names is one compile-time copy of `packs/gpu`'s seam, linked with its public symbols renamed and reached through the generated `seam_backends.h`. There is no `backend_gl.c` any more, and no fourth `sdl` value.\n\n**Why a runtime switch at all**, when the pack is emphatically compile-time selected: a game ships one backend and pays no indirection, which is the whole of charter D14b; rEngine's desktop is not a game, and being able to ask the same binary for all three is what makes the render suite a comparison rather than three builds. The owner chose this shape over one-binary-per-backend (spec 126 decision 3). The cost is paid once per window, in `re_draw_open`, not once per draw.\n\nSDL's own 2D renderer used to be a fourth value and the suite's live oracle. It is retired (charter D49) and the oracle is now `orchestrator/tests/references/*.png`, captured from it while it still shipped \u2014 data rather than a code path, which is the point: every backend left shares the seam, so a seam defect moves pixels in all of them at once and only a recorded frame can see it.",
      "refs": [
        "docs/specs/124-seam-three-backends.md",
        "docs/specs/126-scene-view-and-test-artifacts.md",
        "orchestrator/native/render/seam_backends.h"
      ]
    }
  ],
  "source": {
    "path": "orchestrator/native/draw.c",
    "sha256": "588526814eee8526dde0530bc362d7ca36e75f8348f7cb90d02cc3994c8cb1e9",
    "format": "c",
    "format_fingerprint": "6893201a8120b54c4ae86198441a791fd50e0731d85917c6c1fe0e5fb8ad9627"
  }
}
