{
  "version": 1,
  "entries": [
    {
      "id": "real-vt-screen",
      "kind": "rationale",
      "anchor": {
        "start": 127,
        "end": 127,
        "snippet": "void re_terminal_message(ReTerminal *t, const cJSON *j) {"
      },
      "note": "Only attached snapshot/output bytes from the selected retained PTY feed libvterm. Sequence gaps request a fresh snapshot rather than a launch command. Each snapshot rebuilds the parser/emulator to clear stale mouse encoding and screen state; pinned upstream hard reset alone leaves mouse protocol details behind. Snapshot replay is output-muted: historical cursor/device queries must not be answered a second time, filling the outgoing queue and delaying fresh input. Live queries retain their normal replies. Disconnection clears attached/presented state and drops local held buttons without queuing offline releases; a fresh attached snapshot is required before input. Selection, clipboard, full keyboard/font/resize compatibility remain separately qualified.",
      "refs": [
        "docs/specs/056-native-desktop.md",
        "docs/specs/059-native-terminal-recovery.md",
        "docs/specs/063-terminal-mouse-reporting.md"
      ]
    },
    {
      "id": "ready-after-attached-snapshot",
      "kind": "rationale",
      "anchor": {
        "start": 122,
        "end": 122,
        "snippet": "void re_terminal_presented(ReTerminal *t) {"
      },
      "note": "Only an attached snapshot explicitly waitingForView needs the presented message. Ordinary terminals and older services receive no new readiness traffic. Attachment sequence recovery resets local presentation state but never spawns or resumes a CLI; the retained service releases each handoff gate once.",
      "refs": [
        "docs/specs/058-orchestrator-handoff.md"
      ]
    },
    {
      "id": "bounded-primary-screen-history",
      "kind": "rationale",
      "anchor": {
        "start": 29,
        "end": 29,
        "snippet": "static int history_push(int cols, const VTermScreenCell *cells, void *user) {"
      },
      "note": "Store the actual evicted libvterm cells with their original column count in a ring capped at 2,000 rows and 8 MiB including each line header. Increment a nonzero viewing offset as output pushes rows so the same text stays visible; oldest-row eviction limits that guarantee. Pop newest rows on upstream height growth, padding missing cells with width 1 so libvterm resize iteration advances. History retains old widths and is clipped/padded rather than reflowed. libvterm excludes alternate-screen scrolling from push callbacks; changing alternate-screen mode returns the view to live. Fresh attached snapshots reset both screen and history before replay to avoid duplicates. This is view-owned memory; the sidecar raw-output retention bounds reconstruction after detach/reload.",
      "refs": [
        "docs/specs/060-native-terminal-scrollback.md"
      ]
    },
    {
      "id": "scrolling-and-visible-text",
      "kind": "rationale",
      "anchor": {
        "start": 188,
        "end": 188,
        "snippet": "void re_terminal_event(ReTerminal *t, const SDL_Event *e) {"
      },
      "note": "Wheel input accumulates signed fractional deltas and scrolls three rows per unit. FLIPPED describes the system inversion already applied by Cocoa; negating again would undo the owner preference. Shift+PageUp/PageDown/Home/End are local primary history navigation; ordinary keys retain terminal encoding and return to live only when sending input. Browsing works while disconnected without queuing input. Drawing and inspection use the same history/live mapping; hide the live cursor while browsing and respect cursor visibility. The vertical bar maps oldest to zero and live to count, reserves a stable gutter to avoid column oscillation, and consumes drag/page events before PTY input. Alternate-screen applications hide primary history controls.",
      "refs": [
        "docs/specs/060-native-terminal-scrollback.md",
        "docs/specs/061-pane-scroll-controls.md"
      ]
    },
    {
      "id": "negotiated-mouse-events",
      "kind": "rationale",
      "anchor": {
        "start": 161,
        "end": 161,
        "snippet": "bool re_terminal_mouse(ReTerminal *t, const SDL_Event *e, int x, int y) {"
      },
      "note": "Use VTERM_PROP_MOUSE to select reporting and let pristine libvterm encode click/drag/move modes and SGR/X10 packets. Store the rendered logical cell size/content rectangle; exclude chrome, partial gutters and old history from application clicks. Match SDL left/middle/right to VT buttons 1/2/3 and signed vertical/horizontal wheel steps to 4/5/6/7, preserving fractional motion without a second direction inversion or extra application scroll multiplier. Shift+wheel uses local primary history. Capture a pressed button until release, clamp held motion outside content, and release on focus loss/hide/detach. Native scrollbar drags take precedence. Query inspection exposes negotiated mode and actual cell/grid sizes rather than stale service snapshot dimensions.",
      "refs": [
        "docs/specs/063-terminal-mouse-reporting.md"
      ]
    },
    {
      "id": "wrapped-reference-cells",
      "kind": "rationale",
      "anchor": {
        "start": 300,
        "end": 300,
        "snippet": "bool re_terminal_file_at(ReTerminal *t, int x, int y, char *target, size_t capacity) {"
      },
      "note": "Extract nearby displayed rows through the same history/live cell mapper as drawing. Trim terminal row padding before inserting a hard line break, preserve the clicked UTF-8 offset, and join rows filled to the right edge as before. Explicit application word wraps remain newlines for the reference parser to normalize only inside delimited targets. Blank cells are not link targets. The bounded buffer does not reconstruct arbitrary off-screen chat or hidden OSC-8 metadata.",
      "refs": [
        "docs/specs/113-chat-file-image-tabs.md"
      ]
    }
  ],
  "source": {
    "path": "orchestrator/native/terminal.c",
    "sha256": "aac62ade0aed06d4f4d98c7219a39065b2383825d151e5f4b8182fe87471e685",
    "format": "c",
    "format_fingerprint": "6893201a8120b54c4ae86198441a791fd50e0731d85917c6c1fe0e5fb8ad9627"
  }
}
