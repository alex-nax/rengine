{
  "version": 1,
  "entries": [
    {
      "id": "menu-and-chooser-are-file-scope",
      "kind": "rationale",
      "anchor": {
        "start": 28,
        "end": 36,
        "snippet": "static struct {"
      },
      "note": "The agent menu and the open chooser are file-scope here rather than fields on ReApp because one window draws one Tasks surface at a time and one row has a chooser open at a time, nothing outside this file reads either, and fixed arrays mean nothing is owned that a close path would have to free. The menu carries the root it answered for: a second Tasks tab on another project must not inherit this one's agent list, so every use is behind menu_answers(tab root) and a chooser opened before the answer says \"Reading the agent menu\" rather than listing another project's CLIs. The kinds are mutually exclusive by construction: chooser_toggle writes one `kind` and one `task`, so opening Tests on a row closes a Spawn chooser open elsewhere. That is deliberate \u2014 two open blocks would each claim to describe \"the\" task, which is the confusion the inline layout exists to prevent.",
      "refs": [
        "docs/specs/103-task-driven-agents.md",
        "orchestrator/tests/native-tasks-controls.spec.mjs"
      ]
    },
    {
      "id": "chooser-is-inline-not-a-popover",
      "kind": "rationale",
      "anchor": {
        "start": 217,
        "end": 217,
        "snippet": "static void agent_rows(ReApp *a, mu_Context *ui, int tab, const char *key, const cJSON *criteria, bool answered) {"
      },
      "note": "The chooser draws as rows under its task instead of as an overlay for three reasons, in order of weight: an overlay costs one of microui's 32 root containers, which fifteen leaf panes with a surface open already fill (the same budget that keeps the token segment out of microui); a chooser that scrolls with its row cannot end up describing a different task than the one under it; and the pane is already a scrolled list, so nothing about the layer is gained. The row's control cluster reserves its columns by negative width the way the trailing pill does -- a leading -1 would fill to the right edge and push everything after it out of the pane (5a0bc38). F115 (spec 116) adds a third kind, RE_CHOOSER_TESTS, drawing the task's claims and the evidence that backs them; it follows this rule for the same three reasons rather than becoming the popover the other two refused to be. Its Tests caret is drawn on EVERY row, including a provider that answers with an empty evidence list, because an absent control reads as \"this task has no tests\" rather than \"this provider cannot say\" \u2014 so its column is reserved unconditionally, unlike Decompose and Hold token which are local-only. That rendering choice is not covered by the native suite (KI-073): every row the harness can reach comes from a local provider, so gating the caret on `local` leaves the suite green.",
      "refs": [
        "docs/specs/103-task-driven-agents.md"
      ]
    },
    {
      "id": "capability-gates-and-the-note-row",
      "kind": "rationale",
      "anchor": {
        "start": 148,
        "end": 154,
        "snippet": "static const char *refused(const ReApp *a, const char *brief) {"
      },
      "note": "The worker advertises agentsMenu always but agentSpawn and taskWrites only where it owns the ledger (spec 095's asymmetry: a caller is refused by name rather than reaching a worker with no gate). So the pane refuses locally and names the missing prerequisite instead of posting into a 404. Decompose has the stricter gate of the two because its only legitimate output is task_add calls, and a worker that cannot write the inventory has nowhere to put the subtasks it would produce. Refusals land in menu.note, drawn as a row of the pane and reported as `tracker-note`: the worker's own refusals name a whole prerequisite (a session host predating task-driven panes) that a 512-byte status line truncates, and its `detail` -- started, retained, could not be shown -- must not read as an error to retry.",
      "refs": [
        "docs/specs/103-task-driven-agents.md",
        "docs/evidence/tasks-pane-controls-2026-09-07.md"
      ]
    }
  ],
  "source": {
    "path": "orchestrator/native/tracker.c",
    "sha256": "4cb5ccf731db4f543ed5dce9057610626b8f224fd4b17345bb175bd60dac8b4b",
    "format": "c",
    "format_fingerprint": "6893201a8120b54c4ae86198441a791fd50e0731d85917c6c1fe0e5fb8ad9627"
  }
}
