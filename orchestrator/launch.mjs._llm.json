{
  "version": 1,
  "entries": [
    {
      "id": "desktop-does-not-own-sidecar",
      "kind": "rationale",
      "anchor": {
        "start": 23,
        "end": 23,
        "snippet": "await import('./build.mjs');"
      },
      "note": "The development launcher uses system Node for native PTY ABI consistency. It reuses live sessions by explicit root/type/agent identity and opens their existing IDs in the desktop. Desktop exit affects this launcher only. Game prerequisite checks run before creating shell/agent sessions so a missing build does not leave fresh processes without the requested GUI. Installer/package prerequisites remain explicit; this is not yet an installable Windows/macOS release."
    },
    {
      "id": "explicit-ui-inspection",
      "kind": "constraint",
      "anchor": {
        "start": 51,
        "end": 51,
        "snippet": "if (options.inspectUI) desktopArgs.unshift('--remote-debugging-address=127.0.0.1', '--remote-debugging-port=0');"
      },
      "note": "Only the explicit --inspect-ui flag enables Electron's ephemeral loopback debugging endpoint. The combined acceptance check uses it to observe the ordinary production launcher through CDP instead of constructing a different desktop/session setup. Do not enable inspection by default or bind it to a remote address. Test state and runtime logs are local and disposable."
    }
  ],
  "source": {
    "path": "orchestrator/launch.mjs",
    "sha256": "7071a04b48efcbf8c212a10a287378999dde9a3ec04fcffe6babf8c4b3ec908c",
    "format": "text",
    "format_fingerprint": "05db8feb0843c51a026668bb69cc122617afc04000263bbb759093d8bf2f314e"
  }
}
