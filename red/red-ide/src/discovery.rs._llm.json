{
  "version": 1,
  "entries": [
    {
      "id": "counted-the-way-the-cli-counted",
      "kind": "history",
      "anchor": {
        "start": 130,
        "end": 130,
        "symbol": "offered_editors",
        "snippet": "pub fn offered_editors(directory: &str, locks: &Path, cwd: &str, living: &dyn Fn(i64) -> bool) -> Vec<Editor> {"
      },
      "note": "The CLI's discovery code contains two filters: `workspaceFolders` against the working directory, and a check that the lock's pid is one of the CLI's first ten ancestors. Reading it, the ancestry check looks like the one that disambiguates two workspaces publishing the same folder \u2014 and that reasoning is wrong.\n\nMeasured on 2026-09-07 by publishing four locks over `/Users/alex/rengine` and driving a real CLI with `/ide`: all four were listed, including one belonging to another workspace's host, which is nobody's ancestor here. The only filter that fired was folders-against-cwd, and the locks it excluded were reported separately as not matching the cwd. So the ancestry gate is conditional on something not true in a real pane.\n\nThis counts the way the CLI was observed to count. If a future version starts applying ancestry, this becomes conservative \u2014 it will decline to auto-connect where the CLI would have found exactly one \u2014 which is the safe direction: a pane that does not auto-connect is a `/ide` away, and a pane that auto-connects into a menu is a question nobody asked.\n\nThe order is `fs.readdir`'s, which is libuv's scandir: sorted by name, whatever the filesystem says. The port drifted through this once \u2014 Rust's `read_dir` answers in the filesystem's order \u2014 and the record caught it.\n\nPorted from `orchestrator/agents/ide-connect.mjs` (spec 129); the JavaScript this describes is gone.",
      "refs": [
        "docs/evidence/editor-as-claude-ide-2026-09-07.md",
        "docs/specs/102-editor-as-claude-ide.md",
        "docs/specs/133-ide-bridge-in-rust.md"
      ]
    },
    {
      "id": "naming-the-port-beats-counting",
      "kind": "rationale",
      "anchor": {
        "start": 161,
        "end": 161,
        "symbol": "auto_connect",
        "snippet": "pub fn auto_connect("
      },
      "note": "`--ide` alone auto-connects only when exactly one editor is valid, and on a machine where two workspaces bind the same project that is never true \u2014 the first version of this declined forever on the owner's own checkout, correctly and uselessly.\n\nThe CLI has a second path: a lock whose port equals `CLAUDE_CODE_SSE_PORT` is valid regardless of the other checks, and the selection then returns that editor alone. So the question becomes which editor is *this pane's*, and ancestry answers it without asking anything of anyone: the session host forks every pane, so its pid is in this process's ancestor chain, and the lock naming it is ours.\n\nAn earlier attempt asked the host for its pid on `/api/state` and got `undefined` \u2014 that field is newer than the running host, and a host is exactly the thing that does not get replaced. Ancestry needs no cooperation from it. Counting survives as the fallback for when the chain matches none of the offered editors.\n\nThe flags and the variable's name are inputs rather than a registry read: the recipe is `red-agents`' data, already resolved in Rust, and this crate must not depend on that one to answer a pane's question. Every sentence here is a pane's startup line and is held word for word by `ide-connect-corpus.json`, `undefined` for a nameless editor included.\n\nPorted from `orchestrator/agents/ide-connect.mjs` (spec 129); the JavaScript this describes is gone.",
      "refs": [
        "docs/evidence/editor-as-claude-ide-2026-09-07.md",
        "docs/specs/133-ide-bridge-in-rust.md"
      ]
    }
  ],
  "source": {
    "path": "red/red-ide/src/discovery.rs",
    "sha256": "39d160b66ecaa36584189d2380e62c63c6990f2bf5fb79d4d3cb34cc4540cc78",
    "format": "rust",
    "format_fingerprint": "3802714b4569aa9e93696227dc96f5433c9f027b6349406e4e8901968d9e7fea"
  }
}
