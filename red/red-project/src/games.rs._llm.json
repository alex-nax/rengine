{
  "version": 1,
  "entries": [
    {
      "id": "remote-launch",
      "kind": "rationale",
      "anchor": {
        "start": 79,
        "end": 85,
        "symbol": "remote_refusal",
        "snippet": "/// The remote launch belongs to the project's own script, which holds knowledge that does not"
      },
      "note": "Remote launching is deliberately outside this contract. The consumers' own scripts do it and hold knowledge that does not belong in an orchestrator: an SSH logon session has no window station, so a GL context cannot be created and the launch has to go through Task Scheduler with /IT; APK freshness is compared against the installed base.apk mtime. So the refusal is computed here, where the declaration is in hand, and names the project's own script actions bound to that device — derived from data, so rEngine still names no specific script. The string is carried on the config so the worker can refuse before anything reaches the retained host (the KI-043 lesson) and Games.start can refuse again for a direct caller, without two copies of the wording.",
      "refs": [
        "docs/specs/082-project-devices.md"
      ]
    },
    {
      "id": "preflight-relocation",
      "kind": "rationale",
      "anchor": {
        "start": 137,
        "end": 143,
        "symbol": "inspect_game",
        "snippet": "pub fn inspect_game(context: &Context<'_>, declared: &Value, game_id: Option<&str>) -> Result<Value, Fail> {"
      },
      "note": "Preflight is exported as a free function taking the root record, not a Games method, because it reads only the declaration, the filesystem and root.id/root.path — no session, surface or store state. That is what lets the replaceable workspace worker answer GET /api/game-config from its own checkout above a retained session host that predates contract 3, exactly as dashboard.mjs is already consumed from both processes. Games.inspect is now a one-line delegation, so the host and the worker run the same body and there is no second copy of the checks to drift. Launch stays a method: it needs Sessions and Surfaces, which are the host's process state. One consequence worth knowing: a bare executable candidate resolves through the calling process's PATH, so it is the worker's environment that answers when the worker serves the route. Contract 4 adds an options argument threaded to deviceStatus so an explicit refresh reaches the probe cache; it stays a free function for the same reason, since a probe needs only the root record and the shell environment.",
      "refs": [
        "docs/specs/078-project-game-declaration.md"
      ]
    },
    {
      "id": "device-relative-executable",
      "kind": "constraint",
      "anchor": {
        "start": 180,
        "end": 190,
        "symbol": "inspect_game",
        "snippet": "    let mut executable: Option<String> = None;"
      },
      "note": "THE rule of spec 082: a target bound to a non-local device is never resolved or stat-ed against the local filesystem. That check is exactly what produced \"Game executable not found; expected build/vtmb-vr ...\" for a target that is condition = \"windows\" and can never be built on the machine the workspace runs on — every word true, and the conclusion (build it) impossible. The cwd is skipped for the same reason: it is a directory on that machine. The record's own requires are still checked locally, because requires is local by definition even on a remote device. The regression test proves the rule rather than the symptom: it places the executable ON the local disk and asserts it is still unresolved, so the test cannot pass by the file merely being absent.",
      "refs": [
        "docs/specs/082-project-devices.md"
      ]
    }
  ]
}
