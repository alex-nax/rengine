# Known issues and open decisions

| ID | Issue | Consequence / next action |
| --- | --- | --- |
| KI-001 | Owner selected iklib as the first two-game library proof; exact profiles, parity measures and resource budgets remain open. | Complete `docs/specs/001-library-pilot.md`; shared command tooling remains supporting work. |
| KI-002 | The active NOLF workspace goal authorizes 15 scoped features; the larger proposal remains separate. | Follow `features.json` and F55. The historical interactive review is not a new implementation gate. |
| KI-003 | Powered-by minimum is confirmed as verified pinned capability adoption; its record format was proposed for a year. **Answered 2026-09-08 by charter D44 and spec 110**: the adopting project holds the evidence, and the proof is the owner's sign-off in a spec naming the pack, both halves of the pin, the project and what was seen. Closed as a question; the first actual sign-off is still owed, and F61 is the obvious candidate. | Record the first adoption when F61 lands. A declared check command was considered and rejected — it proves a command went green, not that the adoption is good — and remains the successor if this stops scaling. |
| KI-004 | Both engines use an optional local `infra-vr` source path. | Plan reproducible packaging without duplicating its reporting/backend work. |
| KI-005 | iklib host migrations are pending in its tracker. | Reference F131/F130/F133/F132; do not claim integration from presets alone. |
| KI-006 | Reference worktrees are active and contain edits. | Re-pin and recheck changed files before any actual integration. |
| KI-007 | No game, native library, device or training workload was executed during reconnaissance. | Treat inspected commands and past logs as reported evidence, not current green gates. |
| KI-008 | Future library license, distribution, platform and packaging policies are undecided. | Resolve at curation admission; no package publication or license declaration in setup. |
| KI-083 | **Closed 2026-09-11.** The Vulkan seam's `re_seam_buffer_update` memcpyed into one buffer while the frame's draws were only recorded, so every draw read the last fill. Opened rather than fixed when Metal's copy of the same defect was found, because nothing here exercised the Vulkan case and a fix could not have been watched going red. `seam_host_vk.c` is that consumer: with the draw list rendering through the Vulkan seam the frame came back with most of its geometry replaced by the last batch's, which is the failure this describes. Fixed with the Metal answer — a buffer generation per in-frame update, counter reset at `frame_begin`, reuse safe because `frame_end` waits — and `--renderer seam-vulkan` is now pixel-identical to every other backend (spec 124). | Nothing further. |
| KI-084 | `native-project-windows.spec.mjs` failed once on 2026-09-11 waiting for a reopened window's recovery draft: the editor tab held `"Retain this drafgame disk text\n"` — the draft truncated one character short of `"Retain this draft"` and run together with the file's disk contents — so the `until` never matched. An immediate re-run passed, and two earlier full-suite runs the same day passed. It names a race between applying a recovery draft and loading the file on reopen. Unrelated to the F133 rendering work in the same session: that change adds `--renderer` branches and seam copies, and this path is the editor's draft restore under the default backend. | Reproduce by running `native-project-windows.spec.mjs` in a loop; the assertion to watch is the `gui.until` at line 80. Look at whether the draft is applied before or after the disk read in the reopen path, and make the order explicit rather than timing-dependent. |
| KI-085 | **Closed 2026-09-11.** The bookkeeping pass ran and found three things the rows were right to be waiting on, none of which were bookkeeping: `pack.json` described only the device layer and listed one public header; the outside-project test linked only that layer, so "the pack carries both" was a claim about a file; and **allocations per frame were recorded nowhere**, which F130 c6 and F131 c4 both require. All three are fixed, each sabotage-verified, and F123, F129–F133 now pass — 31 features passing, up from 23. | Nothing further. The Windows half moved to KI-087. |
| KI-086 | The Vulkan seam allocates one descriptor set per draw — 32 per steady frame where OpenGL and Metal allocate nothing — because `re_seam_frame_begin` resets the descriptor pool and a set cannot outlive that. Found 2026-09-11 by `re_seam_counters`, added for exactly this reason: the three backends rendered identical pixels while one of them did per-draw work the others did not. It is pool suballocation rather than a heap allocation and it is an idiomatic Vulkan pattern, so this is a cost, not a defect. | Cache sets by the textures they bind and stop resetting the pool each frame, which means the cache keys must cover everything that affects a set's contents — a change to the Vulkan backend's frame model, worth making when a consumer needs the frames. `pack-gpu-backends.test.mjs` gates the shape meanwhile: no more than one allocation per draw, observed failing at two. |
| KI-087 | Nothing evidences the seam migration on Windows. The three prefixed seam copies, the `/FI` force-include MSVC needs instead of `-include`, and rEngine's draw list rendering through the pack have only ever run on macOS — while `RE_DEFAULT_BACKEND` there moved from `sdl` to `opengl` on 2026-09-11 on the strength of adapter evidence from 2026-09-06 that measured `backend_gl.c`, which is now deleted. The code path is the one all three macOS copies share. | Run `npm run build`, `npm run test:native` and the render spec on the authorized Windows host (KI-014, charter D31), and record what the three renderers produce against the committed reference frames. KI-038's unrelated Windows suite failures are the known noise to read past. |
| KI-088 | `project-token.test.mjs`'s "the extended routes refuse an agent that does not hold the token, and never refuse the desktop" failed once on 2026-09-11 in a full `npm test` run, and passed both alone and on an immediate re-run of the whole suite. The token tests turn on contest deadlines and a 60 s window, so they are the suite's most timing-sensitive, and the run that failed was the first to also carry the F140 contract harness (which starts a session host and a runtime worker) and the F144 companion test. Unrelated to either change by subject — it is the token ledger — but plausibly provoked by their load. | Run `project-token.test.mjs` in a loop under parallel load to reproduce. If it is deadline arithmetic racing a slow tick rather than a real ordering bug, the fix is to drive the window from an injected clock instead of wall time, which is what makes the assertion about ordering rather than about speed. Second observation of suite-load flakiness after KI-084 — then a third and fourth on 2026-09-12, both under the full desktop suite and both green alone and on an immediate re-run: `native-handoff` (its wait for a presented pane) and `native-label-clip`. Four specs in four subjects now, which points at the runner rather than at any one fixture: `--test-concurrency=1` still leaves a desktop, a session host and a worker per spec competing for one machine. Worth timing the suite with a deliberate gap between specs before suspecting any of them again. |
| KI-089 | **Closed 2026-09-12.** The companion now draws through `ReDraw` and the D33 owned control layer, and the text was never a renderer defect. `re_draw_begin(draw, w, h)` asks the backend for the density by dividing the drawable it owns by the `w` passed in, so passing the phone's **physical** 1080x2400 reported a density of 1.0 and drew the whole interface at 1/2.75 scale — 12px text on a 1080-wide panel, which read as "rects render, glyphs do not". The recorded bisect had the measurement that said so and it was misread: `text_width("HELLO")=40` is a cell width of 8, which is the mono advance at density 1.0 and not at 2.75. Passing logical pixels renders Inter, Phosphor icons, the segmented group and the accent button on the device. | Nothing further. The lesson is recorded in spec 128: a density derived from a caller-supplied width is a units contract, and the header now says so. |
| KI-090 | **Closed 2026-09-12.** Touch never reached the companion because `android_main` assigned `onAppCmd` and **not** `onInputEvent` — the handler and the assignment were both lost when `companion.c` was reverted to the direct-backend version, and the glue treats a null `onInputEvent` as an event nobody wanted rather than an error. The recorded diagnosis ("assigned and never called, verified by logging while tapping with `adb shell input tap`") was wrong twice over: the file had no handler at all, and the taps never happened — this phone (MIUI) refuses `adb shell input` with `SecurityException: INJECT_EVENTS`, and SELinux refuses `sendevent` on `/dev/input` to the shell domain, so that command had been proving nothing. `companion-build.test.mjs` now asserts the entry registers a handler and feeds `mu_input_*`, observed failing for its own reason with the assignment deleted. | Nothing further. Note for any future device work here: neither `adb shell input` nor `sendevent` can drive this phone, so touch is verified by the owner tapping while `logcat -s rengine.companion` shows the `touch down at x,y logical` line. |
| KI-091 | **F147 (red-store) does not fit one loop tick; split-proposed, not started.** The first J0 deletion row bundles three things the hourly contract (spec 129) cannot land honestly in one run: (1) the Rust store crate with byte-parity against the JS on-disk format plus a captured fixture corpus; (2) the first JS-host→Rust service channel the epic needs — every deletion row until F152 has the still-JS host consuming a Rust module, and the channel's shape, lifecycle ownership and error-taxonomy hop (`store.mjs`'s `fail` statuses must survive byte-exact or "external API unchanged" is false) have no spec answer yet; the LSP/MCP-worker stdio JSON-RPC pattern is the house answer and is the recommendation; (3) the live swap of ten server consumers plus the deletion. The row also carries a dependency the graph missed: `store.mjs` imports `agents/registry.mjs` for conversation-id shaping (`recordConversation`), so the Rust store needs the recipe data — which is F148's content. F147 and F148 are graphed as independent; they are not. | Proposed split, for owner approval into features.json rows (F147's own row stays untouched): **F147a** — red-store crate + parity corpus, nothing deleted; criterion 1's proof, with `schema.mjs`'s validator riding along as a second module (identical error strings; its deletion waits for the swap). **F147b** — the stdio store service + thin JS client, the ten consumers rewired, `store.mjs`/`schema.mjs` deleted; criteria 2–3; depends on **F148** landing first so conversation shaping reads the same TOML on both sides. Filed by the J0 loop on 2026-09-12. **Approved by the owner the same day**: F169 (F147a) filed depending on F139; F170 (F147b) filed depending on F169 and F168 (the registry data lands before the swap). F147 stays open until F170's evidence lands. |
| KI-092 | **F148 (registry-as-data) is two ticks wearing one row; split-proposed, not started.** Sized on the second J0 loop tick. The TOML half is one honest tick: the shipped five-recipe table becomes one TOML document (absent capabilities as omitted tables — TOML has no null; id-shape regexes as literal strings; a `recipes.*` document shape the EXTRA file shares), a bounded TOML-subset parser on each side (JS in registry.mjs, Rust in a new red-agents lib crate), cook() and the exported API byte-stable, the EXTRA file moving from JSON to TOML (a spec 127 contract change, recorded here), agent-registry.test.mjs updated not dropped, and resolved-recipe parity for every CLI proven against a JSON dump from the Rust side. The spawn-env half is another: the RENGINE_AGENT_* composition in sessions.mjs:129-201 has to be pinned at the pty.spawn boundary (stub pty, real store, workspace context present and absent), its boundary decided (does the context include RENGINE_ORCHESTRATOR_SESSION and the HANDOFF_* family, or only the pane-identity three the row names?), ported to red-agents, and compared byte-exact for kimi, claude and codex — mint-when-start-capable, refuse-named-without-start-unless-resume, listing only for a bare pane, cleared-in-both-compositions. Each half is a tick; together they are not a little one. | Proposed split, for owner approval (F148's row untouched): **F148a** — the TOML document, both parsers, EXTRA through TOML, recipe parity; criteria 1 and 3. **F148b** — the spawn-env composition port with its stub-pty harness; criterion 2; depends on 148a (the Rust side composes from the recipes it parses). **Standing-rule ask**: the J0 rows were written coarser than the hourly cadence the owner asked for — two of two ticks have hit this wall. Proposal: the loop may implement a filed split's first sub-row on the NEXT tick without waiting, passing the parent row only when every criterion has evidence; the split's row text stays untouched as the record. Owner may revoke by saying so. Filed 2026-09-12. **Approved by the owner the same day** ("approved, do now"), standing rule included: F167 (F148a) implemented and passing with evidence `docs/evidence/agent-registry-toml-f167-2026-09-12.md`; F168 (F148b) filed depending on it. F148 stays open until F168's evidence lands. |
| KI-093 | **F149 (red-agents II) is three ticks wearing one row; split-proposed under the KI-092 standing rule.** Sized on the fifth J0 loop run: 959 lines across registry.mjs/config.mjs/report-session.mjs/bind.mjs, three acceptance criteria with different natural homes, and a dependency the parent's wording hides. Criterion 3 (agent.sh dispatches to the Rust binary, CLI surface unchanged) and criterion 2 (codexHookKey/codexHookTrustHash byte-exact) are shell-shaped and pure: agent.sh already shells out, and the hash math is a sha256 over canonical JSON — no JS-host channel needed. Criterion 1 (report-session byte-identical payloads for all five providers) is a subprocess boundary the launcher composes into hook command lines — also channel-free, and it deletes report-session.mjs. What remains is the hard part: registry.mjs/config.mjs/bind.mjs have in-process consumers on the still-JS host (tasks.mjs's menu/model flags, store.mjs's conversation-id shaping, sessions.mjs's capability reads, worker/mcp.mjs's agentLaunch), and deleting those modules needs the same JS-host→Rust channel F170 needs — the decision KI-091 already flags as undesigned. | Split (filed under the standing rule, parent's row untouched): **F171 (F149a)** — the red-agents CLI (list/show/show-field byte-exact with registry.mjs's CLI, error paths included), the three conversation flag parsers ported without a regex dependency, the codex hook key + trust-hash math in Rust, agent.sh dispatching its registry reads to the binary while its own CLI stays byte-identical; criteria 2 and 3. **F172 (F149b)** — red-agents `report-session` with byte-identical payloads for all five providers on recorded hook fixtures; the launcher composes the Rust binary into hook command lines; report-session.mjs deleted; criterion 1. **F173 (F149c)** — the server-side consumers move off the remaining modules and registry.mjs/config.mjs/bind.mjs are deleted; depends on F170 (the channel) and F172. Filed 2026-09-12; F171 implemented in the same run under the standing rule. |
| KI-094 | **The live workspace cannot hear hook reports: its session host predates the route, and the failure is silent.** Found 2026-09-12 by the codex quota probe (evidence addendum in `docs/evidence/codex-sessionstart-hook-2026-09-11.md`): the host (PID 33465) has run since 2026-09-06 00:03, and `/api/agent-conversation` landed 2026-09-07 (F93) — every hook report since then has died on `Unknown workspace endpoint.` The workspace's conversations map is empty for every root as a result. KI-062's disease, one route later — and the silent half is its own defect: report-session exits 0 by design (a hook must never fail its CLI) and notes only to a pane's stderr nobody watches, so a report path can be dead for days with every signal green. | Remedy: `node orchestrator/launch.mjs --state /Users/alex/rengine/.cache/orchestrator-development --replace-host` **run from a terminal outside rEngine** (Terminal.app) — it refuses by design from any pane (insideHost), it ENDS the running sessions (conversations resume from the pane; restart-supervisor is the panes-preserving tool but does not refresh the host), and no dashboard action covers it (checked 2026-09-12). Afterwards re-verify a manual report records a conversation. The silent-failure half wants a surfacing: the reporter could write a marker the workspace reads (a failed-report note in the state directory shown on the Sessions tab), filed here rather than fixed in passing. |
| KI-009 | Desktop v0 is macOS/Windows with a live NOLF tab, multi-root bindings, retained sessions/browser and basic editing/Vim with recovery drafts and explicit saves. C/microui is owner-selected; the current native subset and remaining qualification are in spec 056. | Complete the qualification inputs in `docs/specs/032-desktop-v0.md`; test terminal/editor/game surfaces, draft recovery and root isolation on both desktops. |
| KI-010 | Meta XR Operator has a documented native distribution, but neither game has been tested with it. | Verify actual runtime/graphics/profile compatibility and endpoint isolation; pane streaming is a separate requirement. |
| KI-011 | Quest distribution is supported in principle by upstream Android/PWA routes; no rEngine client or desktop link has been tested. | Prove paired sessions, keyboard/pointer input, game stream and package route on a real headset. |
| KI-012 | “Any app in a pane” has no universal proven mechanism. | Test native reparenting separately from capture/control; record per-app/platform support instead of a blanket claim. |
| KI-013 | The normal launcher now passes the combined native macOS NOLF/tree/editor/shell/Codex workflow, connected MCP tools, GUI restart and session browser detach/reattach/Stop. | Evidence is in `docs/evidence/native-workspace-macos-2026-09-05.md`; remaining release criteria and the overall goal stay open. |
| KI-014 | Current sidecar/agent tests ran on macOS only. The NOLF-configured Windows host is reachable, but automatic approval review rejected source transfer without direct owner authorization for that destination. The owner authorized `pr0fe@192.168.31.217` on 2026-09-06 (charter D31, spec 073): commits only, by git bundle, into the isolated checkout `C:\Users\pr0fe\rengine`. | The F59/F62 renderer runs happened on 2026-09-06 (spec 073): MSVC build, CTest 4/4, SDL/OpenGL/Vulkan smoke snapshots byte-identical, render comparisons within tolerance; the desktop suite there passes 7 of 15 tests (KI-038). Actual NOLF evidence on Windows still follows. Windows environment normalization has portable regression coverage only. |
| KI-015 | Editor storage uses staged rename/version checks and 250 ms draft checkpoints with an awaited GUI-close flush. Normal GUI restart recovery is tested; forced crash and power-loss durability remain unqualified. | Test failure recovery; do not claim OS-level atomic compare-and-swap against external writers. |
| KI-016 | Native game streaming uses macOS SDL interposition and CPU RGBA readback capped at 30 fps and 1920×1080. Native button-release/capture-loss checks pass and real NOLF lobby movement is observed; Windows wiring, budgets, relative aiming and DPI/resize remain unqualified. | Complete F42/F43; see `docs/evidence/gameplay-input-macos-2026-09-05.md` for the precise scope. |
| KI-017 | The existing user-configured Capture MCP failed its startup handshake during Codex qualification. rEngine MCP connected with eight tools. | Diagnose Capture separately before claiming every pre-existing user integration works; no global settings were rewritten by rEngine. |
| KI-018 | Agent overlays preserve settings in tests for Codex, Claude, OpenCode and Gemini; only installed Codex has runtime proof. | Qualify other supported recipes and Windows. Actual isolated Codex download/upgrade was verified from 0.153.3 to 0.153.4. |
| KI-019 | Historical Electron/Playwright frame tracing grew its main process to 6.2 GiB. That desktop and its browser-specific probes were removed under D26–D27. | Retain the historical finding; qualify the C desktop with native events and measured resource bounds. Do not reuse browser results as native evidence. |
| KI-020 | The Mac became unlocked; the new SDL desktop passes native relative capture, held-key release on Escape and delivery of the next Escape. | The old lock condition is cleared. Real in-level aiming and logical/drawable game DPI mapping still need native qualification. |
| KI-021 | The native C/microui migration is a working checkpoint, not desktop v0 completion. Native PNG/JPEG/GIF previews and visible chat file references now have macOS evidence in spec 113. Terminal selection/copy, richer editor/Vim behavior, service-restart/failure recovery, resource budgets, packaging and Windows runtime proof remain open. Session-stream reconnection and bounded primary-screen scrollback have macOS evidence in specs 059–062, including visible bars and agent-requested reload. | Follow spec 056 and the unchanged active feature criteria. Glyph coverage follows the selected system font. |
| KI-022 | The GUI and game runtime have no Electron/browser dependency. The existing Node launcher/session service remains separate; an optional owner clarification on moving that service to C is pending. | Preserve stable service contracts for a later independent web client; do not claim a complete native-service migration. |
| KI-023 | Tab overflow/reordering and pane merging are implemented under spec 057. Remaining native issues include a global session view labeled “Missing root,” a stale conflict message after successful Discard, and toolbar textboxes limited to typing/backspace rather than full clipboard/navigation. | Refine native field/status behavior and extreme pane-size constraints while preserving root bindings and upstream microui sources. |
| KI-024 | After moving NOLF between narrow/wide panes, the latest `game-input.png` still shows the main menu after Enter. The combined automated check passed because it did not assert a menu transition. | Resume investigation inside the orchestrator: reproduce with a meaningful menu-state oracle and check letterbox pointer coordinates before changing game input. Pane movement/streaming is proven; menu input after that movement is not. |
| KI-025 | The exact real Codex conversation resumed inside the native pane and successfully called its root-bound rEngine MCP on 2026-09-06. After the freeze repair in KI-026, the next real handoff again verified its matching running session and root-bound MCP. | Current development can proceed in that verified pane. Native Cmd/Ctrl+Shift+R retains the CLI; after a CLI exits, use the exact handoff rather than creating a duplicate conversation or goal. Scheduler controls remain application-owned. |
| KI-026 | A burst that filled the 128-message native receive queue killed its worker; the disconnect notice could be lost in the same full queue, leaving both panes apparently connected with undelivered input. Animated ANSI output alone remained interactive. | Fixed with bounded queue backpressure, bounded per-tick processing, visible reconnect status and fresh snapshot reattachment. Mac native tests cover real PTY input in both panes, an 8,000-event burst, outage recovery with the same PIDs and discarded offline input; actual ended Codex output replay also passes. See spec 059 and its evidence. Changed service identity, Windows and full terminal compatibility remain unqualified. |
| KI-027 | The owner could not scroll the running native agent pane because primary-screen history and wheel navigation were absent. | Implemented in spec 060: bounded 2,000-row/8 MiB history, wheel/trackpad and Shift navigation, output anchoring, hover routing without keyboard-focus changes, alternate-screen isolation and same-PTY reconstruction. Reload the desktop to load the new implementation. Terminal selection/copy, old-width reflow, glyph fallback and Windows runtime remain unqualified. |
| KI-028 | The terminal negated `SDL_MOUSEWHEEL_FLIPPED`, undoing the system preference already applied to Cocoa deltas. Native terminal/editor scrollbars were missing. | Spec 061 uses signed precise motion consistently and adds draggable terminal/editor bars; upstream tree/session bars remain intact. Native pointer checks cover both axes, history endpoints, track paging, overflowing trees and unchanged editor bytes. Load the built desktop with Cmd/Ctrl+Shift+R; Windows runtime remains unqualified. |
| KI-029 | Agent desktop discovery/reload now passes actual MCP stdio plus native rebuild/reattach with an unchanged CLI PID and dirty draft. The current retained service/connector predate `desktopActions: 1`. | Spec 062 rejects unsupported old services explicitly. Upgrade service/connector through explicit session management later; do not stop the active CLI to bootstrap it. One targeted shortcut attempt was blocked by macOS Accessibility (`osascript` cannot send keystrokes); no settings were changed or sessions restarted. The native keyboard shortcut remains available. |
| KI-030 | The `design/` bundle is synced to the owner's Claude Design design-system project “rEngine native workspace” (`9e977c1b-7cbd-4a89-90a4-5524771712d7`, 18 files, 2026-09-06). Edits made there do not reach the desktop until a card is pulled back and imported. | Pull edited cards into `design/previews/` and run `python3 tools/design.py import`; re-sync with a per-run plan limited to `design/**`. Native colours and layout metrics go through the generated `RE_COLOR_*`/`RE_METRIC_*` constants, and `check` guards both. See spec 064. |
| KI-031 | Running the existing agent-launcher test from a live handoff pane inherits `RENGINE_HANDOFF_FILE` for the real project and fails its temporary-project check. Sandboxed service tests also lack loopback access. | Session 19 runs tests with loopback/PTY access and removes `RENGINE_HANDOFF_FILE`/`RENGINE_HANDOFF_GATE` only from test-child environments. The real CLI context remains intact. Make the launcher fixture isolate this inherited environment in a separate harness repair. |
| KI-032 | Claude fullscreen requested alternate screen and SGR mouse reporting, but the native terminal ignored clicks and alternate-screen wheel events. Replay also filled the input queue with obsolete query replies, and immediate GUI close could lose a held-button release. | Spec 063 adds negotiated tracking/encoding, cell-based pointer routing, application wheel handling and balanced releases. Historical snapshots rebuild muted; live queries still reply. Normal close/reload drains outgoing input, canceling after two seconds if busy. Mac native and private actual-stream replay evidence qualify this path; live Claude UI judgment, terminal clipboard, OSC-8 hidden-target hyperlinks and Windows remain separate; visible file references are covered by spec 113. |
| KI-034 | The Claude Design theming update (2026-09-06) needs primitives the SDL_Renderer path cannot draw: rounded alpha rectangles, frames with inner highlight, shadows, two text faces at several sizes, tinted icon glyphs, focus rings, timed hover transitions and one popover layer. The desktop keeps the interim `orchestrator/native/theme.json` look. The pulled `tokens.css` pointed the terminal cursor at an undefined `--ui-accent-dim`; fixed to `--re-accent-dim` and synced back with the regenerated mirror, manifest and README. | F56 (spec 067) and F57 (spec 068) landed on macOS on 2026-09-06: draw-list contract, SDL reference adapter, and the OpenGL 3.3 core adapter that is now the macOS default with pixel-identical current-UI scenes and half the SDL frame cost. F58 (spec 072) followed the same day: the Metal adapter is pixel-identical to OpenGL, passes the same gates and is now the macOS default (`--renderer opengl|sdl` stay selectable); F56–F58 pass on macOS evidence and F62 tracks Windows OpenGL evidence (KI-014). Next: F59 Vulkan, then the theming update and library candidacy per spec 066. The design project and `design/` are in step again as of 2026-09-06. |
| KI-033 | The original live host/connector predate layered updates. | Spec 065 passes native/MCP/CLI/bootstrap fixtures. Spec 069's explicit shell bootstrap adopted the live host and opened the same-agent NOLF window on 2026-09-06 without a keyboard reload. Routine managed-window updates preserve the original host and CLIs. Windows runtime proof remains pending. Host/supervisor protocol replacement requires quiescence. |
| KI-035 | Project-window inspection and durable integration reports now work through MCP/CLI and a terminal shell routine on macOS. | Spec 069 proves independent layout/draft recovery, retained agent identity and two-root reports; the real NOLF window is attached. Reports require explicit polling, simultaneous editing of the same file is not a collaboration contract, and Windows runtime is not yet qualified. |
| KI-036 | The NOLF project submitted a request for independent pane zoom on a second monitor. | Received as durable report sequence 2 and acknowledged to the project. Requested keyboard +/−/0 and visible controls, persisted pane scale, and retained-PTY dimension changes. Intake is recorded; implementation has not started. |
| KI-037 | Spec 074 lands the project format registry (contract 1), raw hex and preview modes on macOS. A Codex review (2026-09-06) found six defects that are fixed on `fix/format-registry-review`: microui's 48-slot treenode pool aborted the desktop on the 49th expanded directory (the earlier claim that the oldest collapsed was wrong; expansion state is now owned by the view), a structurally malformed declaration threw and left every text editor of that root waiting, `${file}` was confined before a separate open/spawn, timeouts killed only the child, `preview_file` had no breadth budget and echoed absolute paths, and the native HTTP deadline was fixed at 5 s below declared budgets. Remaining: Windows `.exe` resolution of relative `argv[0]` is untested (KI-014); paging an entry re-runs its declared command rather than caching output; the confinement window between the final realpath and the producer's own open of `${file}` cannot be closed for a path-taking executable (single-user loopback sidecar, documented in spec 074). | Owner verifies `nolf/NOLF.REZ` in the real project window after `update_workspace` (workspace, desktop, connector). Cache or stream entries only if a real archive shows the re-run cost; record Windows behaviour with the F62/KI-014 pass. |
| KI-038 | The native desktop suite on the Windows host (2026-09-06, `eb88c92`) passes 7 of 15 tests on OpenGL and on Vulkan alike. Failing: the empty-workspace bootstrap cannot unlink a running `rengine.exe` in the layered-update versions directory (EPERM: Windows locks executing files); the wide-tree layout test never scrolls `dir063` into view; the project-windows fixture creates a symlink, which needs a Windows privilege; the terminal mouse, recovery and scroll specs and the native events spec report PTY output or input `not reached` (their shell input is POSIX and the default Windows shell is PowerShell, while the render spec's PowerShell terminal scene does work); the render spec fails only on the Vulkan memory delta (KI-039). A spec process also stays alive after its tests on Windows, so `tools/windows-verify.cmd` runs the suite with `--test-force-exit`. | Fixed on the way: `_spawnv` argument quoting in `bootstrap.c`, `SDL2.dll` next to the CTest executables and the surface fixture, `python` for the comparison, the game spec's fixture path. The rest is the Windows PTY/editor/update work KI-014 anticipated; F62 stays open until the suite passes there. |
| KI-039 | Vulkan on the Windows host uses 54–60 MiB more working set than the SDL reference (OpenGL on the same driver DLL: 24–28 MiB) against the 32 MiB delta ceiling of spec 068 decision 6; on macOS the same adapter sits below SDL. The adapter's own share was trimmed (`b0a6d4b`, 71 → 54 MiB); the remainder is the NVIDIA Vulkan driver's process baseline. | Every other Vulkan gate passes on Windows (pixel-identical to OpenGL, below the SDL median, 0 validation messages). The owner set a per-platform ceiling of 64 MiB for Vulkan on Windows on 2026-09-06 (charter revision record, spec 068 amendment); the Windows render run passes under it (delta 60380 KiB) and F59 passes on its macOS and Windows evidence. Revisit if a driver update moves the baseline. Closed. |
| KI-040 | Spec 075 lands the project dashboard (contract 2, step 1) on macOS: declaration, availability, script/log sessions, PNG captures with a manifest, MCP tools and the native tab. Deferred by owner decision: the log filter overlay on terminal tabs (step 2) and showing captured images in the tab (step 3, needs native image rendering); `stream`/`operator` kinds are reserved. A retained host older than the session `title` option labels dashboard sessions generically in the session list (the tab title is correct); Windows `taskkill`/PATHEXT paths are untested (KI-014). | Consumer merges its staged `.rengine/dashboard.json` into `project.json` with `contract: 2` after the pin bump; owner verifies the real dashboard after a layered update; implement steps 2 and 3 as their own specs. |
| KI-041 | Spec 078 replaces the hard-coded NOLF button and `nolf_*` tools with per-project game declarations (contract 3, `games` array) on macOS: a dashboard action `kind: "game"` referencing a declared record by id (with optional literal `args`, availability from that record's preflight, and its first issue named when it is disabled), generic `game_preflight`/`launch_game` taking an optional `gameId`, and an `external` surface that opens the game's own window with only its PTY output retained (this is how an SDL3-static consumer such as vtmb-vr launches today). The owner reconciled this lane with nolf-improved's F1615 array proposal on 2026-09-06: array superset, contract bump to 3, singular `game` removed with no alias, `embedded` replacing `sdl2-interpose`, and none of that proposal's deprecated NOLF-named tool aliases. Reconciled with the contract-2 dashboard (spec 075) already on main: one schema with both optional blocks, one reader that reports `gamesError` and `dashboardError` independently, and both tool families behind their own capability. On 2026-09-06 the owner also **removed the toolbar game control outright** (button, `Games` menu and disabled entries, built in `0cf70b6`) after a consumer launching from its dashboard got a terminal tab: games are launched from the dashboard, the toolbar row is fixed again, and configurable toolbar items are a separate feature the owner will scope later — do not re-add a stub button. Open: a cooperative in-pane surface for SDL3/static applications does not exist (only the SDL2 interposer streams frames); the live connector, worker and desktop predate the game routes until a layered update; a retained host older than the session `title`/`surface` fields labels game sessions generically and shows them as live views; Windows `.exe`/PATH resolution and launch stay unqualified (KI-014). | Owner merges `feat/project-game`, runs `update_workspace` (workspace, desktop, connector) and verifies the real consumers' declarations; an SDL3 cooperative surface is its own spec; record Windows behaviour with the KI-014 pass. |
| KI-042 | The integration recipe is stored as a runbook, the `integrate-project.sh` wizard, templates and a test (F70, spec 077), but the orchestrator UI does not run it: the toolbar's **Add project** button opens a window on an already-integrated project only. The wizard is meant to be opened in a script tab (spec 071) until native controls exist. A second gap the scaffold exposes: every contract requires at least one format record, so a project whose only surfaces are a dashboard and its game targets must declare an inert placeholder format. | Make Add project run this recipe natively (spec 002/071): pick a directory, collect the name and an optional first game target, run the stages in a script tab and open the window on the result; later a declaration editor and a pin-bump action. Consider relaxing `formats` to `minItems: 0` for contract 2 and above when the dashboard/game branches settle. |
| KI-043 | The merged per-project game capability was unreachable on a live workspace: the replaceable workspace worker served the dashboard routes itself but **forwarded** `game-config`/`game` to the retained session host, so `projectGame` could only ever come from a host that predates contract 3. Every MCP game tool answered "This retained service predates per-project game declarations. Update the workspace layer first." and no number of `update_workspace` calls could change it; the host answered `game-config` with **HTTP 200** and the removed built-in NOLF config (`--flat --game nolf --width 1280 --height 720`, `Build NOLF first; expected build/relith-nolf`) for the vtmb-vr root and every `gameId`, which also rendered both of that project's flat dashboard entries unavailable. **General lesson, recorded in spec 078: a capability served only by the retained host cannot be delivered by a layered update.** | Fixed on `fix/worker-game-routes` (F74): `games.mjs` exports `inspectGame(root, gameId)`, the worker serves `GET /api/game-config` from its own checkout and advertises `projectGame: 1`. Launching stays with the host — it owns the PTY, the `embedded` surface reservation and the session-id map behind `/surface`, and its `/api/terminal` refuses `type: "game"` — so it is advertised separately as `projectGameLaunch`, mirrored from the host's own `projectGame`, and refused by name above an older host rather than forwarded into the removed built-in game. Launching a declared game on the live workspace therefore still needs the session host replaced, which requires quiescence and stops the retained PTYs; preflight and dashboard availability do not. |
| KI-044 | Game recording (spec 081, F75) lands video and logs; **audio is a declared slot, not a stream**. A game's sound goes to the system output device through its own audio API and never passes through the workspace, which sees only a frame socket and a PTY, so rEngine cannot recover it from anything it holds. Per-app audio needs an OS capture path (macOS ScreenCaptureKit; Windows pending) plus an ASR backend, and capture-mcp at the owner's `/Users/alex/capture` already does exactly that — per-app audio, chunked, timestamped and transcribed — beside window video and process output. Driving it from the desktop was rejected for this slice: it would make an unpinned tool at a `~/...` path a runtime requirement (a boundary this repository sets) and would need screen/microphone consent granted to rEngine rather than to the tool the owner already trusts with it. Every manifest therefore carries `audio: { present: false, provider: "capture-mcp", reason, issue }`, and `recording_read` reports it verbatim, so a reader is told what is missing and where it comes from. Recording an `external` game (its own operating-system window, no frame stream) is the same problem and is deferred with it. | Spec the integration: an optional per-project audio provider a commit invokes for the segment's window, whose timestamped transcript chunks land beside the keyframes on the same clock (`atMs`/`wall`), with the manifest's `audio` slot filled in place. Until then do not build a second audio stack in rEngine and do not drop the slot. |
| KI-062 | Measured 2026-09-07 on the owner's rEngine workspace (host PID 33465, supervisor 44390, worker 79969, desktop 88067): the layered mechanism was installed and had run eleven times, and the tracker routes still could not reach the editor because they had been added to the host only — the worker forwarded `/api/tracker` to a host from before the route existed (KI-043's shape, third occurrence after game-config and, on hirebase-v2, the same routes). Two of the five MCP connectors bound to that host (PIDs 93041 under the owner's `claude` pane and 20159 under a `codex` pane) predate the facade of `fffc0be` and list the eight-tool set with `launch_nolf`/`nolf_preflight`; they run code loaded before any refresh path existed and cannot be changed from outside. Codex 0.153.4 ignores `notifications/tools/list_changed` (openai/codex #10105, #19155, #33266), so a Codex session never sees a new tool *name* until it is restarted; Claude Code 2.1.263 honours it. | Spec 101 / F98: the tracker routes move to the worker with the state directory found by instance, the facade refreshes on the descriptor without waiting for a request and answers a stale name with the way back. For the two pre-facade connectors: reconnect the server from Claude Code's `/mcp` menu (Reconnect keeps the conversation and pane) or restart the Codex CLI in its pane; neither touches the host or a session. Every future route that needs no PTY, surface or store state goes in `runtime/worker.mjs`, not `server/main.mjs` — a route on the host is unreachable until `--replace-host`. |
| KI-063 | Observed 2026-09-07 on the owner's rEngine connector (`rengine_046c207bf579`) right after `/mcp` → Reconnect gave it the current 32 tools: every token-gated tool — `update_workspace`, `launch_game`, `stop_session`, `open_script`, `dashboard_capture`, `reload_desktop`, `restart_agent` — is refused with *"Only an identified agent can act on the token; this request carried no X-Rengine-Agent header"*, and `token_contest` is refused the same way even though the token is free. The connector's context was written before F90 minted agent identities, and Reconnect re-runs the same context, so the reconnect that fixes the tool list cannot fix the identity. | The read-only tools all work, and the same updates run through `orchestrator/runtime/client.mjs`, which carries the runtime's own token rather than an agent's. A connector bootstrapped before F90 needs re-bootstrapping with an identity to hold the token; belongs with F90's remaining work rather than to the layered-update path. |
| KI-045 | The desktop suite is intermittently slow on this Mac: a full `npm run test:desktop` run drops one or two different tests to an 8 s automation timeout, and every one of them passes when rerun alone. Observed at the committed baseline `a5e6036` (3 tests) as well as on the F73 tree (0 to 2 tests, never the same ones), so it is environmental rather than a defect in either change. | Do not read a single red run as a regression: rerun the named test alone before investigating. The likely cause is contention as 26 desktop windows are created and torn down in sequence; raising the automation timeout or serialising window creation would settle it. Two of my own tests had real races and were fixed by waiting on desktop state instead of a delay. |
| KI-046 | Every project scaffolded by the integration recipe pointed its sidecar at the shared default state directory, so two projects bound both their roots into one workspace: the project selector appeared not to switch, and a host restart from one checkout killed the sibling project's retained agent session, because live PTYs belong to the host and are never persisted. Reported by the nolf-improved session after it cost a live session. | Fixed in `orchestrator/templates/project/editor.sh`: the launcher now keys the state directory on the checkout's absolute path, with `--state` still overriding for deliberate sharing, and `orchestrator/tests/integrate-project.test.mjs` asserts it. Projects scaffolded before this carry the old default and should be relaunched once with `--state` or have their launcher updated. |
| KI-047 | Regression tests can pass for the wrong reason, and a fixture can leave the suite without anything saying so. Five blind assertions were found across two sessions on 2026-09-06 from five distinct causes, plus one spec that stopped running because a merge resolution took one side of a single-line script. A green report is silent about both. | Evidence and the derived rule are in `docs/evidence/blind-regressions-2026-09-06.md`: the sabotage must be the specific failure the test claims to prevent, not any change that turns the test red. `orchestrator/tests/suite-coverage.test.mjs` now fails when a spec is in the tree and no npm script runs it, with deliberate exclusions listed by name and reason. The owner took it into the `AGENTS.md` work protocol on 2026-09-06. |
| KI-050 | Contract 4 devices (F76, spec 082) landed with tests and a native section, but **no consumer declares devices yet** and no probe has run against a real Quest or a real SSH host from this branch. Every probe in the suite is a local fixture script by design. The two live consumers stay contract 3 and are unaffected; the owner owns their declarations. Until one is promoted, the end-to-end claim rests on fixtures. | Owner: promote `vtmb-vr` (and later `nolf-improved`) to contract 4, binding `vtmb-vr` to the PCVR host and the Quest actions to the Quest, then verify in the real window that the Devices section names both as unreachable with their reasons and that `vtmb-vr` reports the device rather than a missing local file (the consumer's own F1146 acceptance gate). Delivery needs `update_workspace` for the worker layer and a desktop reload for the native section; this lane deliberately ran neither. |
| KI-051 | A dashboard listing now pays for one probe per declared device, and an `ssh -o ConnectTimeout=4` probe against a box that is off costs about four seconds of that listing. The 15 s cache and in-flight coalescing bound it to once per device per 15 s rather than once per action, and probes across devices run concurrently, so the worst case is roughly the slowest declared probe. It is still a visible pause the contract-3 dashboard did not have. | Measure it on a real declaration before optimising. If it bites, the options are a longer TTL for a device that has been unreachable for a while, or serving the dashboard from the last known status and refreshing in the background — but only the Devices section may ever probe on its own, never a timer. |
| KI-052 | Sidecar anchors were already drifted on `origin/main` for files this lane did not touch: `store.mjs`, `native-client.mjs`, `automation.c`, `bootstrap.c`, `draw.c`, `editor.c`, `game.c`, `layout.c`, `main.c`, `scroll.c`, `terminal.c`, `ui/ui.c` and the four render backends. `sidecar_tool.py check` is therefore not repo-clean on this branch, and was not before it. | Not this lane's to fix: several of those files are being edited right now by the F68 draw-list session, so repairing anchors here would collide. Repair them from whichever lane last edits each file. The files `feat/devices` touched are all clean and stamped. |
| KI-053 | The cooperative surface (F77, spec 078) is proven against a fixture producer, not against a real consumer: **no declaration names `cooperative` yet**. vtmb-vr has implemented the client side in its own engine (its F1147) and the owner owns the flip of its `.rengine/project.json`, which this repository deliberately did not edit; the shape it will become is pinned and validated here against a copy. So the end-to-end claim -- a real SDL3-static game streaming into a real pane -- rests on a fixture that speaks the same protocol. | Owner: flip `vtmb-flat` to `surface: "cooperative"` with its `VTMB_HIDDEN_WINDOW` env entry, then launch it from the dashboard and confirm the pane shows the game rather than an empty surface. Delivery needs `update_workspace` for the worker layer and a desktop reload for the native tab; this lane ran neither. If the pane stays on "Waiting for game", read `Surfaces.status` first: it distinguishes a missing FRAME channel from a missing INPUT one. |
| KI-054 | A committed segment reports `"truncated": true` for practically every explicit start/stop, because `begin_commit` derives it from `dropped_lead > 0` and the first keyframe sampled at the declared rate is almost never on the same millisecond as the mark. The first real segment (`20260906T201912Z-45e037`, nolf-improved) says `"truncated": true, "droppedLeadMs": 29` with a 120 s / 64 MiB ring that had dropped nothing at all (`droppedFrames: 0`). Spec 081 reserves the flag for an explicit recording that **outruns the ring**, and pairs it with a `requestedStartMs` this implementation does not write either (it writes `requestedSeconds`, which is 0 for a segment). Found while fixing the empty log slice (spec 081, *Defect: the windowed log slice*); left alone so that fix stayed one change. | Distinguish the two facts: keep a sampling lead as `droppedLeadMs` alone, and set `truncated` only when the mark predates the oldest frame the ring still holds — that is, when eviction actually removed the requested start. Write `requestedStartMs` as spec 081 states, or amend the spec to the field that ships. Cover both in `rengine_recording_test` beside the existing outrun case. |
| KI-060 | The headless start (F85, spec 090) is verified on macOS with the child's `PATH` pointing at an empty directory, so no `cmake`, `nmake` or compiler exists for it. That is the Windows box's SSH-logon condition by construction, not the same machine: the failure it fixes was observed there, in `C:\Users\pr0fe\rengine\<pin>\.state\headless.log`, as a CMake NMake-generator error. The consumer's `scripts/wizards/remote-rengine.sh` still invokes the full launcher. | Change that wizard's launcher line to `--headless` and re-run it against the box; the proof is `rengine headless ready` in that log, followed by the capability list answering through the tunnel. Also unproved on Windows: the recording-stub check for the full start path, which skips there because Node refuses to spawn a `.cmd` without a shell. |
| KI-061 | **Two workers own one token ledger after a workspace-only layer replacement.** Spec 065 lets existing streams finish through the replaced worker, and spec 095 put the ledger's desktop channel on one of them, so after `update_workspace` with `layers: ['workspace']` and a desktop attached, the desktop reads and writes the retired worker's ledger while agents read and write the current one; both stay subscribed to the host's `/events` and both mint frames into the same `tokens/<rootId>/feed.json`, so sequence numbers collide (measured: `game.started` at 4 in the served feed and at 3 in the file). The person's Reject/Grant/Revoke/Free land on a ledger no agent reads, and the segment shows a contest the live ledger has already resolved. Evidence and both reds in `docs/evidence/project-token-e2e-2026-09-07.md`. | **Resolved** by retention *by kind* (spec 095, *Retirement*): views keep draining through the replaced worker, so `runtime.test.mjs:91` is untouched, and the token/feed service hands off. The supervisor sends `{ type: 'retired' }` where a retirement is committed; that worker drops its host `/events` subscription and its ledger — it never writes `token.json`/`feed.json` again — closes its own `/feed` clients with a reason naming retirement, forwards the ledger's routes to the current worker through the supervisor, forwards its retained desktops' `token-action`/`recording` frames with the desktop actor on `X-Rengine-Desktop`, and relays the pinned `token` push back unchanged. The alternative that was tried and reverted is row 15 of the sabotage table, still red. Evidence: `docs/evidence/token-retirement-2026-09-07.md`, with `orchestrator/tests/token-retirement.test.mjs` and the criterion the e2e lane had to delete restored in `native-token-e2e.spec.mjs`. Closed. |
| KI-064 | **A desktop restoring a saved layout after `--replace-host` could not register at all.** The layout in `<state>/workspace.json` outlives the host whose sessions its tabs name, so the first `desktop-register` after a deliberate replacement advertised ids the new host had never had; `Desktops.register` validated each with `sessions.snapshot(id)`, which fails `Unknown session.` (404) on the first stale one, and refused the **whole** frame. Measured twice on 2026-09-07 on two projects (rEngine's own workspace; hirebase-v2, replaced at 12:22): `GET /api/desktops` answered `[]` while the desktop ran, the supervisor's `waitView` timed out with "Replacement desktop did not register before timeout.", the runtime supervisor exited with an empty `runtime.log` (the failure travels over IPC), and the workspace came back with no runtime layer at all — on one machine the desktop process was gone with it. The same binary registered normally when its layout named no stale session. | **Resolved** on `fix/stale-sessions-registration`, in the three layers spec 098 now names: the host drops unknown ids and reports them as `unknownSessions` (reaches a workspace only at the next `--replace-host`); the worker filters `sessionIds` against `/api/state` first and publishes the last refusal on `/api/runtime-desktops` (reaches it at the next `update_workspace`); the desktop advertises only sessions the state holds and marks the rest ended; `waitView` now names the refusal and the desktop's stderr. Evidence, with the reds and the sabotage table, in `docs/evidence/stale-sessions-registration-2026-09-07.md`. Closed. |
| KI-065 | **`writeAtomically` named its temporary after the writing process, so two writes to one file in the same process shared it.** The first `rename` moved the bytes both had written into place and the second failed `ENOENT: no such file or directory, rename tokens/preferences.json.<pid>.tmp`, throwing away that write and, through the callers that await it, their answer. Seen as a flake in `token-retirement.test.mjs` about one run in three, reproduced on a clean checkout, so it is in the shipped code and not in a branch. `Tokens`' two preference writers (`setWindow`, `bumpGeneration`) were also unserialised, so even with distinct temporaries the last rename decided the file and it could disagree with memory. | **Resolved** on `fix/stale-sessions-registration`: the temporary is `<file>.<pid>.<write>.tmp` — pid first, so the retirement check that reads which processes wrote a ledger directory still works, plus a per-write component; and `Tokens.persist()` serialises the preference writes the way `Ledger.persist` already serialised the ledger's. `orchestrator/tests/atomic-write.test.mjs` fires eight writes at one file and three writers at the preference file; both were red on the old code in every run, with that exact `ENOENT`. Closed. |
| KI-066 | **An IDE session did not survive a layered update.** Found live on 2026-09-07, by this session's own connection dying: the bridge bound an ephemeral port per worker, so `workspace,desktop` moved rEdit from 49953 to 61709, and Claude Code — which reads a lock once and afterwards reconnects to the port it read, never re-reading the directory — reported `WebSocket connection to 'ws://127.0.0.1:49953/' failed`. Every connected pane would have lost its editor on every update, and the person would have had to notice and re-run `/ide`. The feature whose premise is that capabilities arrive by layered update was broken by layered updates. | Fixed the same day: the supervisor reserves one port for the runtime's life, carries it in `runtime.json` as `idePort`, and hands it to every worker it starts; a worker whose predecessor still holds the port retries rather than settling for a different one, because a different one is a session the CLI cannot get back. Regression in `ide.test.mjs` (`the port survives a worker replacement`), verified by sabotage with the old fall-back-to-ephemeral behaviour. Closed. |
| KI-067 | **The desktop called a route its workspace does not serve, and the status line paid for it.** F100's selection report was sent unconditionally, but only the workspace worker serves `/api/ide-selection`; a desktop bound straight to a session host got `404` every 150 ms and the status line the person was reading became `Unknown workspace endpoint.` Found by `native-format-hardening`, whose own assertion about a malformed declaration was the thing being overwritten — a peer reported the spec red on origin/main and it turned out to be this. | Fixed the same day: the worker advertises `ide: 1` and the desktop posts only when it sees it. The selection is still computed and inspectable either way, because it is the desktop's own fact; what is conditional is telling anyone. Regression: `native-ide-selection.spec.mjs` asserts no `Unknown workspace endpoint` reaches the status line while bound to a bare host. Closed. |
| KI-068 | **A spawned agent pane was offered the conversation picker, and answered it with a stray keystroke.** Found live on 2026-09-07 by the first `spawn_agent` of spec 103: the worker called the host's terminal route with the model flag and the rendered prompt in `args`, the host minted a conversation *and* wrote the project's remembered ones to `<id>.conversations.tsv`, and `scripts/agent.sh` put the spec 097 picker in the new pane and blocked on stdin. The first pane received a `1` — clicking into a pane is enough — and **resumed the spawning agent's own conversation** (`5b8d47c2`) in a second process, its record rewritten to that id by the report-session hook; the second sat on the prompt for an hour until Enter arrived through `/api/input`. A second defect of the same shape was found on the way: `spawnTerminal` clears the pane-identity family with `undefined` overrides and then composes the child environment with a *second* `shellEnvironment()` call, which re-inherits every one of them from `process.env` — so a session host running inside an agent pane handed each pane it spawned that host's own `RENGINE_AGENT_RESUME`/`_CONVERSATION`/`_CONVERSATIONS`. On `origin/main`, run from such a pane, that turns spec 103's own spawn test red (`--session-id` became `--resume`). | **Resolved** on `fix/spawn-no-picker` in the three layers the fix touches: `scripts/agent.sh` offers the picker for an interactive bare launch only (no trailing arguments after `--`, no `RENGINE_AGENT_RESUME=1`), and reaches a running workspace immediately because the launcher is read from the checkout at every pane launch; `server/sessions.mjs` writes the listing only when the caller named no conversation and passed no args, and clears the pane-identity family in both environment compositions — at the next `--replace-host`; `runtime/worker.mjs` names the conversation it mints on the host call — at the next `update_workspace`. Redundant on purpose: a listing can also arrive from a stale environment, and the host can be older than the worker. Evidence, with the reds and a six-row sabotage table: `docs/evidence/spawn-no-picker-2026-09-07.md`. Closed. |
| KI-069 | **Icon numbering is frozen by the plugin ABI.** `render/icons.h` is included by `orchestrator/native/plugin_abi.h`, so every plugin compiled against ABI 1 bakes in today's `RE_ICON_*` values. Reordering or removing an entry in `design/icons.json` would silently change what an already-built plugin draws — no build error, no test failure, a different glyph. Found while implementing F108 (spec 106 decision 15), not by anything failing. | `design/icons.json` is **append-only** from 2026-09-07: add at the end, never reorder, never remove. A removal that is genuinely needed is an ABI version bump, because that is what it costs. |

| KI-070 | Native image views (spec 113) decode PNG/JPEG/GIF first frames, and Cmd/Ctrl-click opens visible terminal file references. WebP decoding, animated playback, OSC-8 links with hidden targets, and Windows runtime qualification remain open. | The service retains WebP support; native opening reports its decoder limit explicitly. Preserve the original terminal root and do not infer hidden targets from labels. F37 stays unpassed. |
| KI-071 | The macOS native renderer acceptance exceeds its OpenGL-minus-SDL RSS allowance of 32,768 KiB: current full suite 35,776 KiB, isolated retry 33,712 KiB, unchanged `85d49a9` baseline 34,000 KiB (2026-09-09). Pixel comparisons, frame-time ceilings and Vulkan validation pass. | Inherited failure reproduced in a separate original-revision Release build; cause remains uninvestigated. No renderer or budget changes. Three reports and reproduction details: `docs/evidence/chat-file-images/README.md`. |

## KI-036 — Per-pane zoom intake

The NOLF-bound agent reported the owner request through project window
`4c55dec7-f1e5-4626-9286-01e8d37ef8f2`, key `nolf-improved:per-pane-zoom:2026-09-06`.
The owner confirmed receipt was expected in this rEngine conversation. Candidate acceptance from
the report covers separate pane font/cell metrics, Cmd/Ctrl +/− and 0, a pointer control, layout
persistence, and terminal resize without process restart. Verify the current renderer before
implementation; the reporter's line references describe the checkout at report time.

## KI-070 — a paste past ~128 characters was dropped in silence (fixed, spec 113)

vterm calls the terminal's output callback once per character, and the callback queued one socket
message each. `RE_NET_QUEUE` is 128, `push` refuses past it, and `re_socket_send`'s return value was
never checked — so the tail of any large paste was freed and lost without a word, while the
JSON-per-character work made what did arrive slow. Fixed by gathering one event's bytes and sending
them together, chunked at 128 KiB (under the host's 2 MB `maxPayload`) and split only on UTF-8
boundaries. **Still open**: a refused send is now counted through `re_terminal_inspect_output` but
still not surfaced to the person who pasted.

## KI-071 — the IDE lock ignores CLAUDE_CONFIG_DIR

`re_ide`'s `ideDirectory()` (`orchestrator/runtime/ide.mjs:27`) always writes the lock under
`~/.claude/ide/`. Anthropic documents `CLAUDE_CONFIG_DIR` as moving that directory, and nothing in
`orchestrator/` reads the variable, so a workspace whose Claude config lives elsewhere publishes a
lock the CLI never reads and auto-connect silently finds no editor. Found by research for spec 114;
carried as an acceptance criterion of F113 rather than fixed in isolation.

## KI-072 — headset-free VR automation is closed to both games

Meta XR Simulator requires Vulkan, Direct3D 11/12 or Metal and states plainly that "OpenGL and
OpenGL ES are not supported" (developers.meta.com, page updated 2026-09-04, read 2026-09-09). Both
games request `XR_KHR_OPENGL_ES_ENABLE` on Quest and `XR_KHR_OPENGL_ENABLE` on Windows
(`nolf-improved/src/vr/vr_session.cpp:363-366`, `vtmb-vr/src/vr/vr_session.cpp:228-234`), so neither
can run in the Simulator, and its Session Capture — the VRS record/replay that would give
deterministic VR repro — is unavailable with it. Meta's AutoDriver does not support OpenXR at all.
VR automation therefore needs real hardware, or a Vulkan binding in the games (charter D48).

## KI-073 — the native tracker harness cannot reach a remote provider's rows

`native-tracker.spec.mjs` drives a real desktop binary, which makes its own tracker requests, so a
fixture cannot inject a Linear or GitHub response the way `tracker.test.mjs` can with its `fetch`
argument. Every row the native suite sees comes from a `local` provider. Found by sabotage S4 of
spec 116: gating the Tests caret on `local` left the whole native suite green, so the decision to
draw it on every provider's rows is implemented and unverified in the desktop. The data half is
covered — a GitHub row is asserted to answer with an empty evidence list. Closing this needs a
native fixture that can serve remote rows.

## KI-074 — no test builds an owned UI control (narrowed: labels now have a pixel regression)

`re_ui_label_ex` drew at its cell's origin without clipping to it, so a long label ran across
whatever the row put beside it; found by the owner in the Tasks view once F115's Tests caret
narrowed the title column, fixed by routing the label through `text_clipped` (spec 117). The fix has
no automated regression: `orchestrator/native/tests/draw_list_test.c` works below the UI layer, and
the automation snapshot reports a control's cell rect, which was correct both before and after — the
defect was the text drawn *beyond* it. Closing this needs a UI-level test harness that renders an
owned control and inspects the emitted draw list for the clip command; it would cover every control,
not just labels.

**Narrowed 2026-09-09 (spec 118).** The label case now has a real regression that fails for its own
reason: `orchestrator/tests/native-label-clip.spec.mjs` snapshots two frames through the desktop's
own `op: 'snapshot'` and asserts that lengthening a task title changes no pixel to the right of its
column. It caught that the first fix was insufficient — `text_clipped` had never clipped, because
`apply_scissor` restored the wider scissor before anything was drawn. What remains open is the
*draw-list* form: a harness that renders an owned control and names the missing clip command
directly. The pixel test proves what reached the screen; it cannot say why.

## KI-075 — native-game.spec.mjs intermittently receives scancode 0 instead of the key it sent

`native-game.spec.mjs` asserts that a scancode pressed over the game pane reaches the fixture, and
it now receives `key 0 1` — `SDL_SCANCODE_UNKNOWN` — where it expects `key 26 1` (`W`). The pane and
the surface transport are working: the fixture is reached, and only the scancode is wrong, so the
failure is in translating the automation event's keycode to a scancode.

**Not caused by the current work.** The same spec fails at `737b58a`, the commit before this
session's native changes, in a clean worktree built from that revision. It passed earlier the same
day (`npm run test:desktop` 70/70), so the likely cause is machine state that
`SDL_GetScancodeFromKey` depends on — the active keyboard layout or input source — rather than a
code change. Recorded rather than absorbed; diagnosing it is its own task, and until then the
desktop suite reads 69/70 with this one red for a reason that is written down.

**Update 2026-09-10: it passes again**, twice in a row and in a full `test:desktop` run (71/71), with
no change to the game, surface or automation paths in between. That confirms the diagnosis above —
the failure depends on machine state `SDL_GetScancodeFromKey` reads, most likely the active keyboard
layout, not on the code. Left open rather than closed: an intermittent test that nobody can explain
is worse than a red one, and the next person to see `key 0 1` should find this entry rather than
re-derive it. Closing it needs the spec to stop depending on a layout-sensitive keycode-to-scancode
translation — sending a scancode directly would do it.

## KI-076 — microui takes focus on a stale hover without rechecking the pointer

`mu_update_control` sets focus when `ctx->hover == id && ctx->mouse_pressed`, **without** rechecking
`mouseover`, and it only clears a stale hover when that same control is updated again
(`third_party/microui/microui.c:691`). A control that stops being drawn therefore keeps the hover it
had. Virtualising the task list (spec 120) made that reachable and harmful: hovering a row's button,
scrolling it out of the window, then pressing anywhere submitted the vanished button — reproduced as
*"a press away from any control must not submit a button that is no longer drawn (chooser became
F132 spawn)"*, for a task not on screen. **Decompose** spawns an agent, so it was not cosmetic.

Fixed in the owned layer rather than upstream, which stays pristine: `control()` in
`orchestrator/native/ui/ui.c` now also requires `mu_mouse_over(ctx, rect)` before it submits, which
protects every owned control and not only the tracker's. Kept open because the underlying microui
behaviour is unchanged, so any future control that reads `ctx->hover` or `ctx->focus` directly can
still be surprised by it. Found by a cross-vendor review of spec 120.

## KI-077 — microui's scrollbar arithmetic overflows on a very large content height

`third_party/microui/microui.c:1014` multiplies the scroll position by the track length in `int`.
With about 40 000 rows in a 2 000-pixel body the product is ~2.23e9 and exceeds signed 32-bit range,
so thumb placement is undefined; the drag path has the same unchecked multiplication. Pre-existing
and upstream: virtualising the list preserves the true content height and therefore preserves this,
and no inventory in the field is near that size (`~/nolf-improved` keeps 1317). Recorded so a future
list that *is* that large does not rediscover it as a mystery. Found by a cross-vendor review of
spec 120.

## KI-078 — a chooser that shrinks leaves one frame scrolled past the new content end

microui computes the scrollbar from the *previous* frame's `content_size`, because `pop_container`
measures after the scrollbar is drawn. Closing or shortening a tall chooser near the bottom of the
list therefore draws one frame with stale thumb geometry and possibly empty space below the content,
corrected on the next frame. Pre-existing and independent of virtualisation — exact spacers keep the
content height right but cannot change the ordering. Found by a cross-vendor review of spec 120.

## KI-079 — F120's Vulkan render-identity check (**closed 2026-09-10**; the loader was here all along)

Recorded as owed because `--renderer vulkan --smoke-test` answered "Failed to load Vulkan Portability
library" and `native-render.spec.mjs` printed *"vulkan unavailable on this machine"*. That reading was
wrong, and the correction is worth keeping because the failure was indistinguishable from a machine
with no Vulkan at all.

**Homebrew had already installed `molten-vk`, `vulkan-loader` and `vulkan-validationlayers`.** The
loader was found — the spec already sets `SDL_VULKAN_LIBRARY`. What was missing is the **ICD
manifest**: Homebrew installs MoltenVK's at `/opt/homebrew/etc/vulkan/icd.d/MoltenVK_icd.json`, and
the loader's default search path covers `share/vulkan/icd.d`, not `etc/`. So the loader loaded,
enumerated zero drivers, and the failure surfaced through SDL as though Vulkan were absent.

One variable, `VK_ICD_FILENAMES`, is the whole difference. `vulkanEnv()` now discovers that manifest
the same way it already discovers the loader and the layer path, and the result is:

| | measured |
| --- | --- |
| Vulkan vs the SDL reference | workspace 0.0096%, terminal 0.011%, primitives 0.79% differing — all within tolerance, **0 pixels outside the edge band** |
| Cross-backend | `opengl-vs-vulkan` and `metal-vs-vulkan` both clean |
| Validation layers | **0 messages** |
| Frame medians | 0.222 / 0.319 / 0.348 ms, against a 4 ms ceiling |
| Resident memory | 144,976 KiB against SDL's 163,424 — **below** the reference, budget met |

**F120's fourth criterion is therefore met on this machine**, and the device layer is exercised on a
real driver rather than compile-checked. The general lesson: *"unavailable on this machine"* is a
claim about the environment, and it deserves the same suspicion as any other unverified claim —
`brew list` answered it in one command after the conclusion had stood for a day.

## KI-080 — the render spec's OpenGL memory budget fails intermittently on a loaded machine

`native-render.spec.mjs` asserts a 32 MiB resident-memory ceiling per backend. On this machine it
now fails for OpenGL by 2–10% — 33152, 33232, 33376, 33456 and 36160 KiB across runs — and the same
spec passed repeatedly earlier the same day. It is **not** caused by the GPU device layer or the
pack: a clean worktree built from `197b539`, before either, fails identically at 33152 KiB under the
same conditions, and both builds pass when the machine is quieter. The worst number came from a full
`test:desktop` run, where 72 specs spawn desktops; system free memory was 39% at the time.

It recurs: two further full-suite runs on 2026-09-10 gave 33648 KiB and a clean pass, back to back,
with nothing changed between them but how busy the machine was.

Deliberately **not** fixed by raising the ceiling. A budget that moves whenever it is exceeded stops
being a budget, and `AGENTS.md` forbids rewriting a requirement so it passes. What would close this
is a measurement less sensitive to host load — a median of several samples, or measuring the delta
against a same-run baseline process rather than an absolute ceiling — or simply running the suite on
an idle machine. Until then a red row here should be checked against a quiet run before it is
believed.

## KI-081 — the Sessions resume spec times out waiting for conversations after a reconnect

`native-sessions.spec.mjs`'s *"offers a project's past conversations for resume"* failed once in
three consecutive full-suite runs on 2026-09-10, timing out after 9.5 s with `conversations: []` in
the client view while the host state held both rows. The captured view shows the reason: the status
line reads *"Session connection restored. Reattaching retained processes."* — the client had just
reconnected, and the conversation list had not been pushed again by the time the wait expired. The
two specs immediately after it, which use the same feature, passed in the same run.

Unrelated to the GPU work; recorded rather than re-run until green. What would close it is either a
push of the conversation list on reconnect, or a wait keyed to the reconnect rather than a fixed
timeout. Until then a red row here should be checked against a quiet run before it is believed, the
same as KI-080.

## KI-082 — F123 and F126 each wait on the other, and only the owner can cut it

F123's eighth criterion says the pack carries the seam **"with GL and Vulkan backends chosen at build
time"**. F126 — which is where a Vulkan backend can first be judged on something other than a compile
— **depends on F123**. So as written, F123 cannot pass until F126 does, and F126 cannot start until
F123 passes. I drafted both rows, so this is mine.

The knot is not merely bureaucratic. Spec 123 measured why a Vulkan backend cannot be written here:
the seam has no render-target concept, because GL let its call sites bind a framebuffer behind its
back. In vtmb-vr, **13 files bind a render target with raw `glBindFramebuffer` (63 calls) and not one
of them is among the 13 files behind the seam.** Every call site that would shape the missing API is
in the *unmigrated* 49.

Three ways out, for the owner:

1. **Amend F123's criterion 8** to what the pack can actually deliver before a consumer exists: the
   seam and the GL backend, with the Vulkan backend moved to F126 where its evidence lives. This is
   the recommendation — it matches D52's own wording, *"its first proof is a Vulkan backend behind
   the surface already migrated"*, which places the proof at VtMB, not here.
2. **Leave the criterion and accept F123 stays `passes: false`** until F126 lands, treating the row
   as a container rather than a gate. Honest, but it makes D50's "a game's Vulkan work waits behind
   the pack" unresolvable by inspection.
3. **Write a Vulkan backend here and gate it on compiling.** Rejected in the writing: it is exactly
   the standard spec 122 refused for the device layer, and refusing it there is what led to finding
   the loader and actually running the thing.

`AGENTS.md` requires an owner decision before a criterion moves, so nothing is edited pending that.

