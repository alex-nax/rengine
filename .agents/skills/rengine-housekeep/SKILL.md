---
name: rengine-housekeep
description: Reconcile a small batch of rEngine specs and docs against the code (code as shipped is the source of truth) — fix drifted specs, move rejected approaches into docs/lessons-learned.md, file follow-up rows for defects found while reading, keep docs/architecture.md and spec 056's checkpoint current, and stamp each spec in the housekeeping ledger so later batches skip it. Use in /Users/alex/rengine.
---

# Codex Adapter

Read `.claude/skills/rengine-housekeep/SKILL.md` in full and execute that workflow as the canonical
definition. Apply `AGENTS.md` if instructions conflict. The project has one shared session log,
`Codex-progress.md`; commit only your own paths and never stage another session's files.
