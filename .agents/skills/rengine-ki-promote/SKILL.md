---
name: rengine-ki-promote
description: Drain the known-issues.md backlog in small verified batches — check each open KI row against today's code, promote real defects or gaps to features.json rows, mark stale rows resolved with evidence, merge duplicates into the feature that already tracks them, and defer judgment calls to the owner. Use in /Users/alex/rengine.
---

# Codex Adapter

Read `.claude/skills/rengine-ki-promote/SKILL.md` in full and execute that workflow as the canonical
definition. Apply `AGENTS.md` if instructions conflict. The project has one shared session log,
`Codex-progress.md`; commit only your own paths and never stage another session's files.
