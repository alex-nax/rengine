{
  "schema_version": 1,
  "project": "rengine",
  "created": "2026-09-05",
  "review_status": "approved",
  "review_record": "docs/specs/055-nolf-workspace-goal.md — owner active implementation goal, 2026-09-05",
  "features": [
    {
      "id": 32,
      "milestone": "O0",
      "category": "design",
      "priority": "high",
      "description": "Specify the first desktop workspace, terminal, draft recovery and NOLF integration slice.",
      "acceptance_criteria": [
        "Carry forward macOS/Windows, empty-to-split layout, movable tabs, basic editing/optional Vim, persistent sidecar sessions, the session browser and multiple project/worktree roots with explicit per-session bindings as confirmed requirements.",
        "Pin the owner-selected flat NOLF host build/profile; specify actual rendered output/input in a new tab, native host ownership and acceptance commands on both platforms, with VtMB as the second adapter.",
        "Specify stable root identities and local recovery drafts with explicit file saves; define checkpoint/conflict handling, initial Vim subset and predeclared measurable terminal/editor/game budgets; keep agent installers, Quest and XR as later gates."
      ],
      "passes": false,
      "dependencies": [],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/032-desktop-v0.md",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 33,
      "milestone": "O0",
      "category": "discovery",
      "priority": "high",
      "description": "Qualify the desktop UI, terminal and game-surface stack on macOS and Windows.",
      "acceptance_criteria": [
        "Compare a small set of existing layout/editor/terminal components against native desktop and later Quest-client constraints with source/license/version references.",
        "On both platforms demonstrate a real PTY, editable text/Vim-mode feasibility and a real cooperative graphical surface with resize/input; static mockups are insufficient.",
        "Measure agreed budgets and choose an implementation stack with known limits and rollback; failure produces a revised proposal, not a supported-platform claim."
      ],
      "passes": false,
      "dependencies": [
        32
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/decisions/desktop-stack.md and bounded feasibility prototype",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 34,
      "milestone": "O1",
      "category": "workspace",
      "priority": "high",
      "description": "Implement the empty workspace with recursive splits and movable tab groups.",
      "acceptance_criteria": [
        "Start empty, split horizontally/vertically, resize and collapse splits, and open/reorder/move tabs between pane groups.",
        "Keyboard/pointer focus follows the intended tab; moving a view preserves its session and root binding and does not trigger a new session or change another tab's context.",
        "Exercise the actual UI on macOS and Windows, including minimum sizes and displays with different scaling."
      ],
      "passes": false,
      "dependencies": [
        33
      ],
      "owner_workspace": "rengine",
      "deliverable": "orchestrator layout implementation and UI evidence",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 35,
      "milestone": "O1",
      "category": "sessions",
      "priority": "high",
      "description": "Implement a desktop sidecar that owns interactive sessions independently of views.",
      "acceptance_criteria": [
        "Session records identify owning sidecar, stable project/worktree root, type, target, launch cwd, process/PTY identity and state; launch only through explicit commands and distinguish shell cwd from launch association.",
        "Closing a view or GUI leaves sessions alive; reconnect attaches to the same session; explicit Stop terminates the intended process tree and reports exit.",
        "Test sessions in two separate worktrees, GUI loss, sidecar failure, missing roots and stale identities on macOS/Windows; never retarget a session by focus/display name or claim a terminated process has resumed."
      ],
      "passes": false,
      "dependencies": [
        33
      ],
      "owner_workspace": "rengine",
      "deliverable": "sidecar session service and lifecycle tests",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 36,
      "milestone": "O1",
      "category": "terminal",
      "priority": "high",
      "description": "Connect real terminal panes to sidecar-owned PTY sessions.",
      "acceptance_criteria": [
        "Exercise an interactive shell/full-screen terminal program with input, resize, scrollback, Unicode, paste and control keys on both desktops.",
        "Move/detach/reattach terminals bound to different roots without losing their process and retained output; focus routes input only to the selected session and a shell cwd change does not rebind its launch root.",
        "Long-lived sessions have no inherited verification-job deadline; measure responsiveness and retain failure/exit state."
      ],
      "passes": false,
      "dependencies": [
        34,
        35
      ],
      "owner_workspace": "rengine",
      "deliverable": "terminal provider and desktop acceptance evidence",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 37,
      "milestone": "O1",
      "category": "editor",
      "priority": "high",
      "description": "Provide project tree, previews and basic text editing with optional Vim mode.",
      "acceptance_criteria": [
        "Browse multiple declared roots, preview files, and edit/save text with undo/redo/search, dirty-buffer status and external-change handling; identical relative filenames in separate worktrees retain distinct root-bound buffers and save destinations.",
        "Exercise the F32-defined Vim subset and ordinary editing on macOS/Windows; preserve supported encoding/line endings and avoid silent overwrite of external edits.",
        "Retain unsaved edits as local recovery drafts on view close/GUI restart and write working files only on explicit Save; exercise external edits, checkpoint failure and explicit Discard without losing the only unsaved copy; large/binary/unsupported files fail or preview explicitly."
      ],
      "passes": false,
      "dependencies": [
        34,
        35
      ],
      "owner_workspace": "rengine",
      "deliverable": "file/tree/editor providers and edit-path evidence",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 38,
      "milestone": "O1",
      "category": "workspace",
      "priority": "high",
      "description": "Persist layout and recover views onto retained sessions after GUI restart.",
      "acceptance_criteria": [
        "Restore splits, tab placement, stable root/session associations and durable local editor drafts after closing/reopening the GUI with multiple projects/worktrees; no restore path silently writes working files.",
        "Resolve live session IDs through the sidecar; no restore path relaunches commands automatically or confuses a dead process with a live one.",
        "Interrupted saves and unknown persistence versions recover with an actionable result; test macOS/Windows paths and changed/missing/removed roots without silently remapping sessions, relaunching commands or stopping retained processes."
      ],
      "passes": false,
      "dependencies": [
        34,
        35,
        36,
        37
      ],
      "owner_workspace": "rengine",
      "deliverable": "versioned workspace persistence and recovery evidence",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 39,
      "milestone": "O2",
      "category": "agents",
      "priority": "medium",
      "description": "Implement the terminal-based Bash agent selector and extensible launch registry.",
      "acceptance_criteria": [
        "Discover supported installed CLI agents and allow an explicit custom installed command; launch in the chosen terminal's explicit root and launch directory regardless of another tab's focused project.",
        "Keep recipes/version detection outside pane code and specify the Windows Bash/native-or-WSL path contract without guessing conversions.",
        "Prove selection/launch both inside the orchestrator and standalone on macOS/Windows, including missing agent and cancelled selection."
      ],
      "passes": false,
      "dependencies": [
        36
      ],
      "owner_workspace": "rengine",
      "deliverable": "standalone agent launcher and registered CLI recipes",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 40,
      "milestone": "O2",
      "category": "agents",
      "priority": "medium",
      "description": "Add visible selected-agent installation and recoverable setup recipes.",
      "acceptance_criteria": [
        "For a user-selected supported recipe, show official source, version, destination and required changes before installation; do not install on tab open alone.",
        "Verify the installed CLI/version and support cancellation, failed download/install and repeat invocation without corrupting an existing installation.",
        "Exercise at least the first agreed real CLI recipe per desktop platform plus isolated failure fixtures; preserve unrelated agent configuration."
      ],
      "passes": false,
      "dependencies": [
        39
      ],
      "owner_workspace": "rengine",
      "deliverable": "versioned agent install recipes and installation evidence",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 41,
      "milestone": "O2",
      "category": "agents",
      "priority": "medium",
      "description": "Bootstrap project MCP integrations through agent-specific configuration adapters.",
      "acceptance_criteria": [
        "Merge the explicitly bound root's project profile through the supported CLI/version adapter without replacing unrelated settings, using a global active project or assuming every agent shares a format.",
        "Distinguish configured, reachable, unsupported and failed integrations; re-running setup is idempotent and exposes repair steps.",
        "Prove the selected agent calls an actual local test MCP tool scoped to its project/session; unknown CLI configuration is not silently guessed."
      ],
      "passes": false,
      "dependencies": [
        39
      ],
      "owner_workspace": "rengine",
      "deliverable": "agent integration profiles and connectivity evidence",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 42,
      "milestone": "O3",
      "category": "surfaces",
      "priority": "high",
      "description": "Implement the cooperative game surface and input contract for desktop panes.",
      "acceptance_criteria": [
        "Transport actual frames from a separate graphical process to a tab with explicit session identity, frame lifetime, resize and input semantics.",
        "Prove focus/aspect/DPI handling, view movement, detach/reattach and explicit stop without affecting another session on macOS and Windows.",
        "Measure the F32/F33 budgets and keep the transport distinct from MCP control; expose capabilities suitable for a later remote client."
      ],
      "passes": false,
      "dependencies": [
        33,
        35
      ],
      "owner_workspace": "rengine",
      "deliverable": "versioned surface adapter and native graphical fixture",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 43,
      "milestone": "O3",
      "category": "host-adoption",
      "priority": "high",
      "description": "Render flat NOLF into an orchestrator tab on macOS and Windows.",
      "acceptance_criteria": [
        "From the owning nolf-improved workspace, implement the flat NOLF adapter using the F32-pinned native build/launch profile and user-owned assets.",
        "Show actual game output in a new tab and verify resize, input, tab move, close/reattach and explicit stop through the sidecar on macOS and Windows.",
        "Run relevant existing host gates, record the tested game/profile and rollback; an external window or log-only tab does not complete the pane requirement."
      ],
      "passes": false,
      "dependencies": [
        42
      ],
      "owner_workspace": "nolf-improved",
      "deliverable": "reLith-owned surface adapter and two-platform game evidence",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 48,
      "milestone": "O5",
      "category": "integration",
      "priority": "high",
      "description": "Complete the first desktop workflow with a terminal, editor, session browser and flat game tab.",
      "acceptance_criteria": [
        "On macOS and Windows execute the empty-to-split workflow with multiple roots, ordinary and optional Vim editing, local draft recovery with explicit saves, real terminals and flat NOLF in a new tab.",
        "Move and detach views, find retained sessions in the browser, reattach after GUI restart, and explicitly stop the intended game/process; identical filenames and similarly named sessions across roots never redirect a file write or session operation.",
        "Record real rendered/input evidence and agreed performance measurements; a layout mock, terminal-only shell or external game window cannot close this milestone."
      ],
      "passes": false,
      "dependencies": [
        36,
        37,
        38,
        43,
        54
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/evidence/desktop-v0.md and packaged desktop builds",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 54,
      "milestone": "O1",
      "category": "sessions",
      "priority": "high",
      "description": "Provide the session browser for inspecting, reattaching and explicitly stopping sessions.",
      "acceptance_criteria": [
        "List live/detached/exited sessions with explicit project/worktree root, type, target, state and attached-view information; distinguish similarly named sessions and retain root association even after its removal from the layout.",
        "Attach or focus an existing session and explicitly stop one with accurate exit reporting; moving/closing the browser does not stop other sessions.",
        "Exercise detached game and terminal sessions across GUI restart on macOS and Windows, including a stale/dead session and two similarly named worktrees; attach and Stop affect only the selected root/session."
      ],
      "passes": false,
      "dependencies": [
        34,
        35
      ],
      "owner_workspace": "rengine",
      "deliverable": "session browser UI and two-platform lifecycle evidence",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 55,
      "milestone": "O6",
      "category": "integration",
      "priority": "high",
      "description": "Launch the NOLF orchestrator with live game, project tree, editor and preferred agent CLI onboarding.",
      "acceptance_criteria": [
        "A documented launch command opens an explicit NOLF project with tree/editor, real terminals and an interactive live NOLF game tab on macOS and Windows.",
        "The standalone Bash launcher finds, installs/downloads, updates or launches the preferred supported CLI agent and supports a custom executable; the real CLI boots in the selected root.",
        "Verify file saves and recovery drafts, session detach/reattach/Stop, tab moves and GUI restart through the actual workflow; record live NOLF and agent evidence alongside automated checks."
      ],
      "passes": false,
      "dependencies": [
        39,
        40,
        41,
        48
      ],
      "owner_workspace": "rengine",
      "deliverable": "launchable workspace and docs/evidence/nolf-workspace.md",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 56,
      "milestone": "R0",
      "category": "surfaces",
      "priority": "high",
      "description": "Define the backend-neutral draw list and the SDL_Renderer reference adapter for the desktop.",
      "acceptance_criteria": [
        "A versioned draw-list contract covers clip, rounded alpha rectangles, frames with inner highlight, shadows, text runs in two faces, tinted icons, focus rings and game textures in logical pixels with a density factor; nothing above it includes a graphics-API header.",
        "microui commands and the owned control layer emit only draw-list commands; pinned upstream microui stays unmodified.",
        "The SDL_Renderer adapter consumes the list; native smoke snapshots stay byte-identical to the pre-change baseline while CTest and the native desktop suite pass."
      ],
      "passes": true,
      "dependencies": [],
      "owner_workspace": "rengine",
      "deliverable": "orchestrator/native/render/ contract and reference adapter with evidence",
      "external_references": [],
      "evidence": [
        "docs/evidence/draw-list-macos-2026-09-06.md"
      ]
    },
    {
      "id": 57,
      "milestone": "R0",
      "category": "surfaces",
      "priority": "high",
      "description": "Implement the OpenGL adapter on macOS behind the draw list.",
      "acceptance_criteria": [
        "An OpenGL core-profile adapter renders every draw-list primitive through owned, versioned shaders with glyph and icon atlases; backend selection is explicit with SDL_Renderer fallback and is recorded in run evidence.",
        "Per-primitive and whole-screen snapshot comparisons against the reference adapter pass within a tolerance recorded before the run on macOS.",
        "Frame time and memory are measured against budgets set beforehand, and the native desktop suite passes on the OpenGL backend."
      ],
      "passes": true,
      "dependencies": [
        56
      ],
      "owner_workspace": "rengine",
      "deliverable": "OpenGL adapter and docs/evidence per desktop",
      "external_references": [],
      "evidence": [
        "docs/evidence/opengl-adapter-macos-2026-09-06.md"
      ]
    },
    {
      "id": 58,
      "milestone": "R1",
      "category": "surfaces",
      "priority": "medium",
      "description": "Implement the Metal adapter on macOS behind the draw list.",
      "acceptance_criteria": [
        "The Metal adapter renders every draw-list primitive with the same snapshot comparisons and measurements as the OpenGL adapter on macOS.",
        "Backend selection, fallback and evidence recording match the OpenGL adapter."
      ],
      "passes": true,
      "dependencies": [
        57
      ],
      "owner_workspace": "rengine",
      "deliverable": "Metal adapter and docs/evidence",
      "external_references": [],
      "evidence": [
        "docs/evidence/metal-adapter-macos-2026-09-06.md"
      ]
    },
    {
      "id": 59,
      "milestone": "R1",
      "category": "surfaces",
      "priority": "medium",
      "description": "Implement the Vulkan adapter on Windows, extending to other platforms when they are targeted.",
      "acceptance_criteria": [
        "The Vulkan adapter renders every draw-list primitive with the same snapshot comparisons and measurements as the OpenGL adapter on Windows.",
        "Backend selection, fallback and evidence recording match the OpenGL adapter; additional platforms record their own evidence."
      ],
      "passes": true,
      "dependencies": [
        57
      ],
      "owner_workspace": "rengine",
      "deliverable": "Vulkan adapter and docs/evidence",
      "external_references": [],
      "evidence": [
        "docs/evidence/vulkan-adapter-macos-2026-09-06.md",
        "docs/evidence/vulkan-adapter-windows-2026-09-06.md"
      ]
    },
    {
      "id": 60,
      "milestone": "R2",
      "category": "design",
      "priority": "high",
      "description": "Implement the Claude Design foundations on the GPU renderer: theme generation, the owned control layer, toolbar, tab strip and status bar.",
      "acceptance_criteria": [
        "design/tokens.css layers and presets generate the runtime theme, and the default, teal and light presets switch live on the GPU backend.",
        "An owned control library over pristine microui provides the design's buttons, selects, fields, checkboxes, sliders, separators, focus ring and transition clock in microui's conventions, as its own build target with no workspace dependencies.",
        "The toolbar, tab strip and status bar match their Claude Design cards on macOS: a generated card reference asserts geometry and resolved colours per state in a native snapshot test, and the full snapshots are recorded as evidence.",
        "Every existing desktop behaviour gate still passes on the GPU backend."
      ],
      "passes": true,
      "dependencies": [
        57
      ],
      "owner_workspace": "rengine",
      "deliverable": "themed native desktop and docs/evidence",
      "external_references": [],
      "evidence": [
        "docs/evidence/design-foundations-macos-2026-09-06.md"
      ]
    },
    {
      "id": 61,
      "milestone": "R3",
      "category": "host-adoption",
      "priority": "medium",
      "description": "Make the renderer a curated capability with one game adopting it through an adapter.",
      "acceptance_criteria": [
        "A catalog entry carries the library-quality record: contract, pinned dependencies, measured resource behaviour and conformance checks.",
        "One game adopts the renderer through its own adapter with passing integration checks and can revert without workspace changes.",
        "The renderer stays optional for games and curated libraries; no umbrella runtime is required."
      ],
      "passes": false,
      "dependencies": [
        57,
        58,
        59
      ],
      "owner_workspace": "rengine",
      "deliverable": "catalog entry, conformance checks and adoption evidence",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 62,
      "milestone": "R1",
      "category": "surfaces",
      "priority": "medium",
      "description": "Verify the OpenGL adapter on Windows behind the draw list.",
      "acceptance_criteria": [
        "The OpenGL adapter builds and starts on the Windows desktop from the authorized source transfer (KI-014), with backend selection and evidence recording as on macOS.",
        "Per-primitive and whole-screen snapshot comparisons against the reference adapter pass on Windows within the tolerances recorded in spec 068.",
        "Frame time and memory are measured on Windows against the spec 068 budgets, and the native desktop suite passes there on the OpenGL backend."
      ],
      "passes": false,
      "dependencies": [
        57
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/evidence for the Windows OpenGL adapter",
      "external_references": [],
      "evidence": [
        "docs/evidence/opengl-adapter-windows-2026-09-06.md"
      ]
    },
    {
      "id": 63,
      "milestone": "O1",
      "category": "editor",
      "priority": "medium",
      "description": "Register project file formats through a versioned declaration and run their bounded preview commands.",
      "acceptance_criteria": [
        "contracts/project-v1.schema.json defines contract 1 (formats with id, title, case-insensitive match globs, modes, default, literal argv preview/entry commands with ${file}/${entry} placeholders and timeoutMs/maxBytes bounds); the service rejects unknown contracts, shell-style argv[0], unknown placeholders, missing keys and non-array commands, and reports a malformed declaration visibly without disabling the root.",
        "Formats, preview (tree/text), entry (size, SHA-256, text when UTF-8 without NUL, hex window) and raw byte-window routes run declared commands only on explicit requests, inside the authenticated root boundary, with cwd = project root, the shell environment, no shell, and fail with stderr's first line on non-zero exit, timeout or oversized output; the host and the replaceable workspace worker both serve them.",
        "The root-bound MCP tool preview_file returns the parsed subtree or text and entry facts through the identical command; service tests exercise host, worker and MCP paths with a temporary project producer, and the nolf-improved declaration with relith-rez is recorded as a real consumer check."
      ],
      "passes": false,
      "dependencies": [],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/074-project-format-registry.md, contracts/project-v1.schema.json, orchestrator/server/formats.mjs and its tests",
      "external_references": [
        "nolf-improved docs/specs/feature-1614-rez-format-registration.md"
      ],
      "evidence": []
    },
    {
      "id": 64,
      "milestone": "O1",
      "category": "editor",
      "priority": "medium",
      "description": "Open registered formats in the native editor with raw hex and preview modes.",
      "acceptance_criteria": [
        "A tab whose file name matches a registered format opens in the declared default mode with a visible switch among its modes; raw is a read-only hex view (offset, 16 bytes per row, ASCII column) over a bounded paged window and is the fallback for every file the text read rejects; the chosen mode survives GUI restart.",
        "Preview shows a collapsible tree with sizes for kind tree and a read-only text pane for kind text; selecting a tree file with an entry command shows its bytes read-only as text or hex; a failing command shows stderr's first line, names the command and offers Retry; Save and drafts stay text-only.",
        "A native fixture drives open, mode switching, tree expansion, entry selection, restart, failure and unregistered-binary fallback through real SDL events; the owner verifies nolf/NOLF.REZ in the real project window after a layered update."
      ],
      "passes": false,
      "dependencies": [
        63
      ],
      "owner_workspace": "rengine",
      "deliverable": "orchestrator/native/formatview.c, hexview.c, native-format-registry.spec.mjs and evidence",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 65,
      "milestone": "O1",
      "category": "editor",
      "priority": "medium",
      "description": "Declare a project dashboard (contract 2) and serve its actions, availability and captures.",
      "acceptance_criteria": [
        "contracts/project-v1.schema.json accepts contract 1 and 2; contract 2 adds an optional dashboard of groups and actions (kind script, log or capture with their fields, requires/tools/artifacts) with unknown keys, unknown kinds, mixed-kind fields, duplicate ids, non-root-relative paths and bad env keys rejected by name, structural errors before cross rules; a bad dashboard is reported without disabling the formats section; the copied nolf-improved merged document validates with zero errors.",
        "The dashboard route computes per-action availability (missing required files inside the root boundary, tools absent from the sidecar PATH) without running anything; dashboard-run creates the retained script or log session with literal args and env merged over the shell environment and refuses unavailable actions; dashboard-capture runs the command once under contract-1 bounds, requires the PNG signature, writes <into>/<timestamp>.png and appends to manifest.json inside the root, rejecting non-PNG output and escaping directories.",
        "Host and replaceable worker serve the routes; MCP dashboard_actions lists availability read-only, dashboard_capture returns the manifest entry, open_script accepts env; service tests fail before the reader knows contract 2 and cover every rule above."
      ],
      "passes": false,
      "dependencies": [
        63
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/075-project-dashboard.md, contracts/project-v1.schema.json (contract 2), orchestrator/server/dashboard.mjs and tests",
      "external_references": [
        "nolf-improved docs/specs/feature-1615-project-dashboard.md"
      ],
      "evidence": []
    },
    {
      "id": 66,
      "milestone": "O1",
      "category": "editor",
      "priority": "medium",
      "description": "Render the project dashboard as a native tab with runnable actions.",
      "acceptance_criteria": [
        "A Dashboard tab per declared root opens from the toolbar and automatically once when a bound root declares a dashboard and the layout holds none; groups are sections, each available action is a button with its title and description, an unavailable action is a label naming the first missing item.",
        "A script action opens its retained session as a script tab with its args and env, a log action opens a terminal tab, a capture action runs the capture and shows the written path in the status line, artifacts reveal their path in the project tree, and a declaration error renders in the tab without disabling the workspace.",
        "A native fixture drives auto-open, groups, the disabled action, the script tab output, the capture file and manifest, artifact reveal, close, restart and toolbar reopen through real SDL events; the owner verifies the real nolf-improved dashboard after its staged section moves into project.json and a layered update."
      ],
      "passes": false,
      "dependencies": [
        65,
        64
      ],
      "owner_workspace": "rengine",
      "deliverable": "orchestrator/native/dashboard.c, native-dashboard.spec.mjs and evidence",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 67,
      "milestone": "R2",
      "category": "design",
      "priority": "high",
      "description": "Implement the Claude Design update for the tree, editor, terminal and session browser.",
      "acceptance_criteria": [
        "The tree, editor, terminal and session browser match their Claude Design cards on macOS under the generated card reference, including the editor breadcrumb bar, gutter, current-line tint and syntax colours, the terminal cell metrics, and the session table's state pills.",
        "Overlay scrollbars with rounded thumbs replace the current bars in every scrollable view, with rest, hover and dragging states.",
        "Every existing desktop behaviour gate still passes on the GPU backend."
      ],
      "passes": false,
      "dependencies": [
        60,
        37,
        54
      ],
      "owner_workspace": "rengine",
      "deliverable": "themed views and docs/evidence",
      "external_references": [],
      "evidence": [
        "docs/evidence/design-views-macos-2026-09-06.md"
      ]
    },
    {
      "id": 68,
      "milestone": "R2",
      "category": "design",
      "priority": "high",
      "description": "Implement the Claude Design menus, the settings popover and theme files.",
      "acceptance_criteria": [
        "Menus and popovers match their cards on macOS as one overlay layer with a shadow, keyboard hints and separators; opening one closes the other, and Escape or an outside click closes the top surface.",
        "A settings popover opens from the toolbar and carries the theme preset, the accent hue slider drawn with the draw-list gradient primitive, the syntax scheme, Vim mode and the explorer's mode; every setting persists as a workspace preference and applies immediately.",
        "Theme files in the card's format override all three token layers and import and export through the popover's path field; a project root's theme is offered and applied only on explicit activation, remembered per root.",
        "Every existing desktop behaviour gate still passes on the GPU backend."
      ],
      "passes": true,
      "dependencies": [
        60
      ],
      "owner_workspace": "rengine",
      "deliverable": "themed overlays, theme files and docs/evidence",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/native-settings.spec.mjs: the popover carries the five settings, the accent track ramps through more than eight hues, a hue change persists and re-tints the brand, Escape and an outside press close the top surface, opening a menu closes the popover and the reverse, the pane menu runs its rows and the shortcut it prints does the same thing.",
        "orchestrator/tests/native-settings.spec.mjs: a theme file setting the palette ramp, a semantic role and a view token reaches the view that reads it; the project theme is not applied until it is chosen, is remembered per root and returns on the next desktop; export writes the card's format and imports back.",
        "orchestrator/tests/native-design.spec.mjs and design/cards.json: the popover ground, hover, separator and hint come from the generated card reference.",
        "orchestrator/tests/native-scrollbars.spec.mjs: scrolling a view repaints no pixel above it (the clip correction found while building this).",
        ".cache/evidence/settings-popover.json, .cache/evidence/settings-popover.bmp, .cache/evidence/settings-menu.bmp",
        "Gates on macOS 2026-09-06: npm test 56/56, npm run test:desktop 22/22, CTest 5/5, python3 tools/design.py check, the cross-backend render comparison including the new gradient in the primitives scene."
      ]
    },
    {
      "id": 69,
      "milestone": "R2",
      "category": "design",
      "priority": "high",
      "description": "Record Windows card evidence for the Claude Design update.",
      "acceptance_criteria": [
        "The card reference assertions for every surface covered by F60, F67 and F68 pass on the Windows desktop.",
        "Native snapshots of those surfaces on Windows are recorded as evidence alongside the macOS set."
      ],
      "passes": false,
      "dependencies": [
        60,
        67,
        68,
        62
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/evidence for the Windows design update",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 70,
      "milestone": "O1",
      "category": "integration",
      "priority": "medium",
      "description": "Store the project integration recipe as a runbook, a scaffolding wizard, copied templates and a test.",
      "acceptance_criteria": [
        "docs/runbooks/project-integration.md carries the ordered recipe with runnable commands (prerequisites, submodule pin policy, editor.sh launching point, contract-3 .rengine/project.json, the tests a consumer keeps and how to register them, skills/AGENTS wiring, the cross-vendor review gate, opening the project window, layered updates, owner-verified consumer gates, pin bumps), the nolf-improved and vtmb-vr instances as tables, and what the orchestrator UI will automate.",
        "orchestrator/actions/integrate-project.sh follows the lib/wizard.sh conventions and scaffolds a consumer in five stages (verify the repository and the advertised pin, pin third_party/rengine, install editor.sh, write .rengine/project.json, copy the declaration test); explicit arguments run unattended, missing values prompt only on a TTY, existing files are reported and skipped, --dry-run prints the plan and writes nothing, and the follow-ups it leaves to the project are printed.",
        "orchestrator/templates/project/ holds editor.sh, the reference project.json, the generic test_rengine_project_decl.py and a README naming each destination, none of them naming a specific game or format; orchestrator/tests/integrate-project.test.mjs scaffolds a temporary git repository and asserts the declared structure through the shipped schema and cross-rule modules, the contract-1 core through readDeclaration, the launcher dry runs, the copied Python test, skip-never-overwrite, an empty --dry-run and the pinned submodule."
      ],
      "passes": true,
      "dependencies": [],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/077-project-integration-recipe.md, docs/runbooks/project-integration.md, orchestrator/actions/integrate-project.sh, orchestrator/templates/project/ and orchestrator/tests/integrate-project.test.mjs",
      "external_references": [
        "nolf-improved docs/specs/feature-1700-rengine-submodule.md",
        "nolf-improved docs/specs/feature-1703-editor-launcher.md",
        "nolf-improved docs/findings/rengine-format-registry-codex-review-2026-09-06.md"
      ],
      "evidence": [
        "docs/evidence/project-integration-recipe-macos-2026-09-06.md"
      ]
    },
    {
      "id": 71,
      "milestone": "O1",
      "category": "surfaces",
      "priority": "high",
      "description": "Declare a project's games in .rengine/project.json (contract 3, games array) and launch any of them from the dashboard, the launcher and generic agent tools.",
      "acceptance_criteria": [
        "contracts/project-v1.schema.json accepts contracts 1 and 2 unchanged and contract 3 with an optional games array (1-16 records of id, title, executable candidates, literal args, env, cwd, requires, surface embedded or external) beside the optional dashboard of spec 075; declaring games requires contract 3, so an older reader answers unknown contract rather than unknown key; a bad games array is reported as gamesError by name without disabling the formats or the dashboard; duplicate ids, a 17th record, bad or reserved env keys, placeholders in args, more than 8 executables, unknown surfaces and keys, shell argv[0] and escaping requires/cwd paths are rejected.",
        "game-config and game_preflight take an optional gameId (default: the first declared game; an unknown id names the declared ids) and report declared, title, surface, the first resolvable executable, args, env, cwd, requires, named issues (undeclared root, malformed declaration, missing candidates, each missing required file, a missing cwd, surface prerequisites) and ready without running anything; game, launch_game, the toolbar control and --launch-game reuse the running session of that root AND game id, or spawn it with the declared cwd and env, reserving a surface only for embedded; the session carries title <title> · <root name>, surface and game, and one root can run several declared games at once.",
        "No specific game is named in server, native, agent, theme or package-script code and no nolf_* tool alias remains; an external game tab shows the retained output with a running/exited status row; service tests and a native fixture drive all of the above; the real-NOLF qualification runs through a written contract-3 declaration; the owner verifies the real consumers after a layered workspace/desktop/connector update. (Corrected 2026-09-06 by owner decision, recorded in spec 078 and Codex-progress session 27: the toolbar game control this criterion originally required — absent without games, a title button for one, a Games menu for several — was built in 0cf70b6 and then removed outright, because dashboard launches reached only a terminal tab. Launching moved to the dashboard game action of F72; a configurable toolbar is a later, separately scoped feature.)"
      ],
      "passes": false,
      "dependencies": [
        63
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/078-project-game-declaration.md, contracts/project-v1.schema.json (games), orchestrator/server/games.mjs and game-rules.mjs, mcp-worker game tools, external game view, games.test.mjs, contracts.test.mjs, native-game-declaration.spec.mjs and evidence",
      "external_references": [
        "nolf-improved and vtmb-vr .rengine/project.json declarations"
      ],
      "evidence": [
        "docs/evidence/project-game-macos-2026-09-06.md"
      ]
    },
    {
      "id": 72,
      "milestone": "O1",
      "category": "surfaces",
      "priority": "high",
      "description": "Launch a declared game from the project dashboard through an action kind game, and remove the toolbar game control.",
      "acceptance_criteria": [
        "contracts/project-v1.schema.json adds the dashboard action kind game with a required game key (the id of a record in the same declaration's contract-3 games array) and an optional literal args list appended to that record's own args; the reader rejects a game action without game, one carrying another kind's field, a ${...} placeholder in its args and a reference to an undeclared id (naming the id and the declared ids) as dashboardError, leaving the formats and the games array intact, and skips the reference check when gamesError is already set.",
        "GET /api/dashboard marks a game action available from its referenced record's preflight (the spec-078 path, not a second copy of the checks) and unavailable with that record's first issue when the executable is missing, in addition to the action's own requires and tools; POST /api/dashboard-run on a game action returns a type game session carrying the record's args followed by the action's, reuses it on a second run, and refuses a second action referencing the same record with different args with status 409 naming both argv; the replaceable worker serves the same through the retained host's game route.",
        "The toolbar game button, its Games menu, its unavailable-entry rendering, its per-game preflight cache and its theme metrics and strings are gone; toolbar row one is a fixed set of columns whose arithmetic no longer depends on the declaration; design.py check passes; a native fixture shows no toolbar game control for a root that declares games, launches an external game from a dashboard game action into its tab with the running status, reuses it on a second click and shows an unbuilt record's action as a disabled label naming its first issue; RENGINE_NOLF_ROOT=... npm run test:game-nolf launches the real NOLF build through a dashboard game action and sees its frames.",
        "Because a section fails whole and the dashboard is now the only human path to a launch, every cross-rule error in the declaration names the offending record beside its JSON path in all three sections ($.dashboard.groups[1].actions[1] (quest-screen).into must be root-relative, $.games[1] (vtmb-vr).cwd ..., $.formats[0] (troika-vpk).default ...); an action with no usable id is placed by its group, a record with no named ancestor keeps the bare path and undefined is never printed; duplicate-id errors keep the bare path so the repeating occurrence stays unambiguous; the report stays capped at three problems and names how many it hides; the native status row carries the id early enough not to clip it."
      ],
      "passes": false,
      "dependencies": [
        71
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/078-project-game-declaration.md and 075-project-dashboard.md, contracts/project-v1.schema.json (game action kind), orchestrator/server/dashboard-rules.mjs, dashboard.mjs, games.mjs and sessions.mjs, orchestrator/runtime/worker.mjs, native dashboard tab and toolbar removal, theme.json/theme.h, dashboard.test.mjs, contracts.test.mjs, native-game-declaration.spec.mjs, native-nolf.spec.mjs and evidence",
      "external_references": [
        "vtmb-vr .rengine/project.json dashboard quick-start actions"
      ],
      "evidence": [
        "docs/evidence/project-game-macos-2026-09-06.md"
      ]
    },
    {
      "id": 73,
      "milestone": "R2",
      "category": "design",
      "priority": "medium",
      "description": "Let the project explorer expand directories in place, chosen by setting, with a bounded loaded-row cap.",
      "acceptance_criteria": [
        "With the explorer set to nested, a directory expands in place with the card's indentation and caret and collapses again without reloading its siblings; a click on the caret drills in and makes that directory the root.",
        "With the explorer set to flat, the view drills down as it does today; the setting decides the mode and nothing changes it behind the person using it.",
        "A cap on loaded rows collapses the least-recently-expanded directory, never one that is an ancestor of the directory being opened or on the selected path, and refuses that expansion with a status line when every candidate is protected.",
        "Expansion state survives a reload of the same directory, and every existing tree and layout gate still passes."
      ],
      "passes": false,
      "dependencies": [
        67,
        68
      ],
      "owner_workspace": "rengine",
      "deliverable": "nested explorer and docs/evidence",
      "external_references": [],
      "evidence": [
        "docs/evidence/nested-explorer-macos-2026-09-06.md",
        "orchestrator/tests/native-explorer.spec.mjs: with the setting on, a directory expands in place at the card's indentation and keeps its siblings, a second level nests under the first, a click collapses the branch whole, and the caret drills in; with the setting off the view drills down as before.",
        "orchestrator/tests/native-explorer.spec.mjs: the row cap collapses the least-recently-expanded branch and names it in the status line, and refuses the expansion instead when the opened directory and the selected file leave no unprotected branch.",
        "orchestrator/tests/native-explorer.spec.mjs: expansion survives a reload of the same directory.",
        "Gates on macOS 2026-09-06: npm test 56/56, npm run test:desktop 26/26, CTest 5/5, python3 tools/design.py check, the cross-backend render comparison."
      ]
    },
    {
      "id": 74,
      "milestone": "O1",
      "category": "surfaces",
      "priority": "high",
      "description": "Serve the declaration-backed game preflight from the replaceable workspace worker, so a routine layered update delivers the per-project game capability.",
      "acceptance_criteria": [
        "orchestrator/server/games.mjs exports inspectGame(root, gameId) — the single body Games.inspect delegates to — and orchestrator/runtime/worker.mjs serves GET /api/game-config from it, so the dashboard's game-action availability and game_preflight both answer from the project's own declaration above a retained session host that predates contract 3; the worker advertises projectGame: 1 beside dashboard: 1 and a layered workspace update alone lights the MCP guard up.",
        "Launching stays with the session host, which owns Sessions (PTY), the embedded Surfaces reservation and the session-id map behind /surface, and whose /api/terminal refuses type game; the worker advertises projectGameLaunch only by mirroring the host's own projectGame, refuses POST /api/game and the game branch of /api/dashboard-run by name above an older host instead of forwarding into the removed built-in game, and launch_game gates on projectGameLaunch with that message. supervisor.mjs claims neither capability in its workspaceWorkerUnavailable fallback, where the host answers.",
        "A regression drives a worker above a proxy that advertises only handoff: 1 and answers game-config with the removed built-in NOLF config: it asserts capabilities.projectGame is 1 and projectGameLaunch absent, that game-config returns each declared record (including an unknown gameId 404 naming the declared ids), that the dashboard marks a declared game available, that both launch paths and launch_game refuse by name, and that game_preflight through the real MCP connector answers from the declaration; the merged-host path keeps projectGameLaunch: 1 and launches through the worker's game route unchanged. npm test, npm run test:desktop, CTest, ./init.sh, design.py check, features.py validate and RENGINE_NOLF_ROOT=... npm run test:game-nolf pass, and both live consumer declarations re-validate through the fixed code. Passing waits on the owner's live verification after update_workspace, as F71/F72 do."
      ],
      "passes": false,
      "dependencies": [
        71,
        72
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/078-project-game-declaration.md (layered-update asymmetry), orchestrator/server/games.mjs and main.mjs, orchestrator/runtime/worker.mjs, orchestrator/agents/mcp-worker.mjs, orchestrator/tests/games.test.mjs and dashboard.test.mjs",
      "external_references": [
        "vtmb-vr and nolf-improved .rengine/project.json declarations"
      ],
      "evidence": [
        "docs/evidence/worker-game-routes-macos-2026-09-06.md"
      ]
    },
    {
      "id": 75,
      "milestone": "O1",
      "category": "surfaces",
      "priority": "high",
      "description": "Record a live game pane: a rolling buffer by default, a toggle on the game tab that commits a segment from the ring or from an explicit start/stop, and committed segments queryable over MCP as timestamped keyframes, a log slice and a manifest on one clock.",
      "acceptance_criteria": [
        "The ring holds JPEG keyframes encoded as they arrive, never raw frames, through third_party/stb/stb_image_write.h pinned at the stb revision third_party/sources.json already records; orchestrator/native/recording.c bounds it by seconds AND bytes with defaults 120 s / 64 MiB / 10 fps / 640 px / quality 70, read from the validated workspace preference recording and clamped again in the desktop, and the game tab's row shows the bound it actually applied. rengine_recording_test (CTest native_recording) covers both bounds, the declared sample rate, the bottom-up-to-top-down flip, commit-last-N, explicit start/stop, an explicit segment that outruns the ring reporting truncated with droppedLeadMs, a commit draining over ticks with its manifest written last, a session exit committing an open segment, and a close finishing a drain.",
        "A segment is <root>/.cache/recordings/<id>/ with a self-ignoring .gitignore, manifest.json (version 1, written last so an incomplete commit is not listed), keyframes.jsonl, keyframes/NNNNNN.jpg and log.jsonl; every keyframe and every log line carries atMs, an absolute wall time and, for keyframes, the game frame sequence, so an agent can put a frame beside the line printed while it was on screen. Audio is a declared slot naming capture-mcp and KI-044 rather than a silent omission; no second audio stack and no video container are built here.",
        "orchestrator/server/recordings.mjs serves listRecordings/readRecording as a pure filesystem read; the session host and the replaceable workspace worker both answer GET /api/recordings and GET /api/recording from it and both advertise recordings: 1, so a layered workspace update alone delivers the capability (spec 078's asymmetry, KI-043) and supervisor.mjs claims nothing; recordings_list and recording_read are read-only MCP tools gated by that flag with the workspace-layer remedy. A native fixture (native-recording.spec.mjs, in test:desktop) drives the toggle against a real frame stream, commits both gestures and reads the committed segment back. npm test, npm run test:desktop, CTest, ./init.sh, design.py check, features.py validate and sidecar validation pass with zero native warnings. Passing waits on the owner's live verification after a layered update, as F71/F72/F74 do."
      ],
      "passes": false,
      "dependencies": [
        71,
        74
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/081-game-recording.md, orchestrator/native/recording.c and jpeg.c, orchestrator/server/recordings.mjs, orchestrator/runtime/worker.mjs, orchestrator/agents/mcp-worker.mjs, orchestrator/native/tests/recording_test.c, orchestrator/tests/recordings.test.mjs and native-recording.spec.mjs",
      "external_references": [
        "capture-mcp (per-app audio and transcription) — KI-044"
      ],
      "evidence": []
    },
    {
      "id": 76,
      "milestone": "O1",
      "category": "surfaces",
      "priority": "high",
      "description": "Contract 4 devices: a project declares where each target runs, rEngine probes reachability under the declared-command boundary and reports availability in those terms, replacing the tools-on-PATH proxy and the local executable stat for non-local targets.",
      "acceptance_criteria": [
        "contract accepts 1, 2, 3 and 4; contract 4 adds the optional top-level devices array (1-8 records of id, kind local|ssh|adb, title, optional host/selector each exactly one of value|env, requires, tools, probe, probeTimeoutMs) and an optional device on a game record and a dashboard action defaulting to local. Declaring devices, or any device key, under contract 3 is rejected by version naming requires contract 4 and the declared contract, never by unknown key; contracts 1-3 read unchanged and both live consumer declarations (vtmb-vr, nolf-improved) still read clean.",
        "Device rules are named cross-field errors: unique kebab ids, the reserved local id/kind pairing with at most one local device and no probe/host/selector on it, exactly one of value|env on host and selector, a probe placeholder whose source is not declared, and an unknown device reference on a game or action listing the declared ids and local.",
        "A target bound to a non-local device is never resolved or stat-ed against the local filesystem: inspectGame reports the device, its reachability and the target's own (still local) requires, names where the executable lives, and the message Game executable not found no longer appears for it. Availability composes device reachability with the target's own requires/tools and names the failing half.",
        "Probes run under the same execution boundary as every other declared command (no shell, cwd the project root, shell environment, stdin closed, bounded output, default 5000 ms capped at 60000, killed as a process group on timeout) and only on an explicit availability check or refresh, never on a timer or during tree listing. Exit 0 is reachable; non-zero or timeout is unreachable with the first stderr line as the reason; a missing or empty host/selector env is unreachable by name without a spawn; device requires/tools short-circuit before the probe.",
        "Probe results are cached per root, device and resolved argv for 15 s with in-flight coalescing so one dashboard listing probes each device once, and an explicit refresh bypasses the cache.",
        "GET /api/devices?rootId[&refresh] is served by the replaceable workspace worker, which advertises projectDevices: 1 (spec 078 / KI-043: a capability served only by the retained host cannot be delivered by a layered update); a read-only MCP devices tool is gated on it and game_preflight/dashboard_actions report device-derived availability. launch_game on a non-local device refuses by name, naming the project's own script actions bound to that device, and attempts nothing.",
        "A native Devices section lists declared devices with status, a manual refresh and the targets bound to each, showing one reason for an unreachable device rather than repeating it per action, through the owned control layer and generated RE_METRIC_*/RE_COLOR_* constants. npm test, npm run test:desktop, CTest, ./init.sh, python3 tools/design.py check, features.py validate and the sidecar check pass, and a native fixture covers the Devices section including the unreachable case."
      ],
      "passes": false,
      "dependencies": [
        74
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/082-project-devices.md, contracts/project-v1.schema.json, orchestrator/server/{device-rules,devices,formats,games,dashboard}.mjs, orchestrator/runtime/worker.mjs, orchestrator/agents/mcp-worker.mjs, orchestrator/native/{devices.c,devices.h,app.c,workspace.c,theme.json}, orchestrator/tests/{devices.test.mjs,device-fixtures.mjs,native-devices.spec.mjs}",
      "external_references": [
        "vtmb-vr/docs/specs/feature-1146-device-targets.md (consumer proposal)",
        "vtmb-vr and nolf-improved .rengine/project.json declarations"
      ],
      "evidence": []
    },
    {
      "id": 77,
      "milestone": "O1",
      "category": "surfaces",
      "priority": "high",
      "description": "A third game surface, cooperative: rEngine reserves the surface and passes RENGINE_SURFACE_PORT/RENGINE_SURFACE_TOKEN exactly as embedded does but injects nothing, so a game whose own engine speaks the surface protocol -- a statically linked or non-SDL2 runtime the adapter can never interpose -- gets a live workspace pane instead of falling back to its own window.",
      "acceptance_criteria": [
        "The surface enum is embedded | external | cooperative and needs no contract bump: it is a new value of a closed enum, so a reader that predates it refuses the document and drops the whole games array rather than degrading, and nothing on the injection path tests anything but equality with embedded. Both directions are tested: the current schema refuses an unknown value naming all three, and a reconstruction of the predecessor enum refuses cooperative and yields no games.",
        "A cooperative launch composes the record's own env plus RENGINE_SURFACE_PORT and RENGINE_SURFACE_TOKEN and NO DYLD_/LD_ key at all, asserted on the environment rEngine composes rather than on the child's, because macOS purges DYLD_* before a protected interpreter can report it. This is the regression that keeps the two-producer race unreachable by configuration: an injected adapter and a self-connecting game would greet the same token on one channel, surfaces.mjs destroys whichever socket arrives second, and both sides reconnect.",
        "Preflight reports no adapter and no platform issue for a cooperative record on any platform: the macOS adapter precondition and the needs host integration and qualification on this platform issue stay with embedded, because a cooperative game is portable by construction. embedded on macOS still composes DYLD_INSERT_LIBRARIES with the adapter and external still reserves nothing.",
        "The contract-4 rule binding a streaming game to the local device covers cooperative through the same code and the same message as embedded, named once in game-rules.mjs rather than tested twice.",
        "The session stays type game carrying its surface, so the native pane, the /surface upgrade, Sessions and Stop behave as they do for embedded, verified rather than assumed: a native fixture launches a cooperative game through the real game route and shows a rising frame sequence, surface cooperative in the snapshot and none of the external tab's PTY text or status row. Launch identity, coalescing and the different-arguments refusal are unchanged.",
        "The suite never depends on a consumer binary: a committed fixture producer reads the two variables, greets both channels through the server's own encoder, streams frames and reports the injection variables it was handed. npm test, npm run test:desktop, CTest, ./init.sh, python3 tools/design.py check, features.py validate and the sidecar check pass, the native build has zero warnings, and the real-NOLF qualification still passes unchanged since NOLF stays embedded."
      ],
      "passes": false,
      "dependencies": [
        71
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/078-project-game-declaration.md, contracts/project-v1.schema.json, orchestrator/server/{game-rules,games}.mjs, orchestrator/native/app.c, orchestrator/actions/integrate-project.sh, orchestrator/templates/project/test_rengine_project_decl.py, docs/runbooks/project-integration.md, orchestrator/tests/{cooperative.test.mjs,surface-producer.mjs,game-fixtures.mjs,native-cooperative.spec.mjs}",
      "external_references": [
        "vtmb-vr F1147 (the consumer's own surface-protocol client, src/platform/workspace_surface.cpp)"
      ],
      "evidence": []
    },
    {
      "id": 78,
      "milestone": "O1",
      "category": "workspace",
      "priority": "medium",
      "description": "Add a task-tracking view with a declared backend: the local git inventory by default, GitHub Issues or Linear where a project declares one.",
      "acceptance_criteria": [
        "A project declares its tracker in contract 5; a tracker block under an earlier contract is refused by name and required version rather than as an unknown key, and the declaration carries no credential field at all.",
        "With no declaration and a features.json present, the view lists every row with the state the local tool reports, and a row whose dependency is unmet reads blocked.",
        "A declared GitHub or Linear tracker lists its issues read-only with an open-in-browser action, using a credential read from the workspace state directory and never from the project file.",
        "A remote tracker renders from cache with an explicit staleness indicator when the network is unavailable, refreshes on demand, and never blocks the frame.",
        "Every existing desktop behaviour gate still passes on the GPU backend."
      ],
      "passes": false,
      "dependencies": [
        63,
        60
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/083-task-tracking.md, contracts/project-v1.schema.json, orchestrator/server/tracker*.mjs, orchestrator/native/tracker.c, and their tests",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/tracker.test.mjs: the local backend derives the same readiness tools/features.py reports, an undeclared project still shows its own inventory, and a project with neither says so rather than showing an empty list.",
        "orchestrator/tests/tracker.test.mjs: a tracker block under contract 4 is refused naming the required version, a provider missing its locator is refused naming it, a locator belonging to another provider is refused, and a token key in the declaration is refused outright.",
        "orchestrator/tests/tracker.test.mjs: a Linear team reaches the neutral row keeping its own state names with the shared category; the personal key is sent bare; a second read inside the window costs no request and an explicit refresh does spend one.",
        "orchestrator/tests/tracker.test.mjs: a missing token, a refused token and a Linear rate limit each answer in the backend's vocabulary rather than as HTTP status codes; GitHub issues reach the same row and pull requests are excluded.",
        "orchestrator/tests/native-tracker.spec.mjs: the Tasks tab opens only when asked, lists the inventory with each row a reachable control, and a declared Linear tracker without a token names the file to create instead of showing an empty list.",
        "Sabotages run separately, each failing only its own claim: readiness ignoring unmet dependencies; a Bearer prefix on a Linear personal key; the declared provider ignored in favour of local.",
        "Gates on macOS 2026-09-07: npm test 89/89, CTest 6/6, python3 tools/design.py check."
      ]
    },
    {
      "id": 79,
      "milestone": "O1",
      "category": "design",
      "priority": "medium",
      "description": "Let a project name the workspace: a declared display title and glyph logo in the chrome and the window title, with rEdit as the default.",
      "acceptance_criteria": [
        "The default title is rEdit in both the chrome and the operating system window title, and a project declaring neither key is unchanged apart from that word.",
        "A project declaring a title and a glyph with a design token name shows both in the chrome and in the window title; the token resolves against the live theme preset and accent hue.",
        "Identity comes from the window's primary root: binding a second root and focusing a tab in it does not change the chrome.",
        "A token name that does not resolve, or a glyph longer than the chip allows, is refused at declaration time naming the offending value.",
        "The trailing-cell fixture still measures the same right edge."
      ],
      "passes": true,
      "dependencies": [
        60
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/084-project-identity.md, contracts/project-v1.schema.json, orchestrator/native/workspace.c and main.c, and their tests",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/native-identity.spec.mjs: an undeclared project shows rEdit in the chrome and in the operating system window title; a project declaring a title and glyph under contract 5 wears both, and the brand chip takes the declared token colour rather than the accent.",
        "orchestrator/tests/native-identity.spec.mjs: identity comes from the window's primary root — selecting a second, declared root through the project menu does not rename the chrome. Verified failing when identity reads the selected root instead.",
        "orchestrator/tests/native-identity.spec.mjs: title and icon under contract 4 are refused naming the required version, and an icon token outside the design set is refused naming the token.",
        "Sabotages run separately, each failing only its own claim: identity read from the selected root; the declared token ignored in favour of the accent.",
        "Gates on macOS 2026-09-07: npm test 78/78, npm run test:desktop 38/38, CTest 6/6, python3 tools/design.py check."
      ]
    },
    {
      "id": 80,
      "milestone": "O1",
      "category": "surfaces",
      "priority": "high",
      "description": "Actionable Devices tab: each device's bound dashboard actions render as controls that run from there through the dashboard's own route, carrying the availability dashboardActions already computed, and each bound game reports the preflight the launch uses -- replacing the inert comma-separated list of target ids.",
      "acceptance_criteria": [
        "GET /api/devices resolves the project's dashboard actions once, with the same dashboardActions the dashboard route uses, and files each under the device it names: per device a controls array of {id, title, kind, available, missing} in declaration order and a targets array of {id, title, ready, remote, issue, location} for bound game records, beside the existing games/actions id lists, which keep their shape. Both the retained host and the replaceable worker serve it. A layer that omits the arrays still draws the old Targets line.",
        "Availability is carried, never recomputed: a listing with controls on it costs one probe per device and no more, because resolution runs after the device statuses in the same request and reads the cache they filled. Drawing a control issues no request; probes stay on open and Refresh.",
        "The Devices tab runs a control through POST /api/dashboard-run (or /api/dashboard-capture), the route the Dashboard tab uses, so a script action opened from Devices lands in a script tab with its declared arguments and no second execution path exists. An unavailable action is drawn disabled.",
        "An action blocked only by its device restates nothing -- the device's single reason stays on the device row -- while one blocked by its own requires/tools/preflight names that first, as the dashboard tab does. A device with nothing bound says so.",
        "A bound game draws its preflight state and carries no launch control; a target on a non-local device never reads ready and names where it runs instead, since launch_game refuses it by design.",
        "A bootstrap-shaped action -- one whose purpose is to make a device usable -- is gated exactly like every other action bound to that device and is not reordered; the decision and its reasoning are recorded in spec 082.",
        "Every new row's leading column reserves its trailing pill's width, asserted rather than eyeballed: every trailing pill ends at the same x as the device row's own status pill, each column keeps one width, and that edge lies inside the pane.",
        "A key pressed over the Devices section reaches the workspace and an open menu, never a bound control: the platform chord opens a shell rather than running the control under the pointer, Return/Space/typing start nothing because a control submits on a mouse press and holds no keyboard focus, and a popover over the section stays open, changes nothing beneath it and leaves the section working.",
        "npm test, npm run test:desktop, CTest, ./init.sh, python3 tools/design.py check, features.py validate and the sidecar check pass, and both live consumer declarations still validate."
      ],
      "passes": false,
      "dependencies": [
        76
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/082-project-devices.md, orchestrator/server/{devices,main}.mjs, orchestrator/runtime/worker.mjs, orchestrator/agents/mcp-worker.mjs, orchestrator/native/{devices.c,theme.json,theme.h}, orchestrator/tests/{devices.test.mjs,native-devices.spec.mjs} (input seam merged with 1a9c591)",
      "external_references": [
        "vtmb-vr scripts/wizards/remote-rengine.sh, the pcvr-bound install action this makes reachable"
      ],
      "evidence": [
        "docs/evidence/device-controls-macos-2026-09-07.md"
      ]
    },
    {
      "id": 81,
      "milestone": "O1",
      "category": "integration",
      "priority": "high",
      "description": "Open projects with externally stored rEdit capabilities and a home-directory launcher, without adding integration files to their checkout.",
      "acceptance_criteria": [
        "Persist an explicit external declaration on the canonical project root; preserve it on reopen, refuse conflicting bindings and unsupported retained hosts, and report the selected filename on errors.",
        "Serve external formats, identity, dashboard, devices and game preflight through both the host and replaceable worker while file access, command cwd and sessions retain the real project binding.",
        "Install external helpers and a quoted launcher at explicit paths outside the project, refuse differing files and inside-project destinations, and expose existing web-project commands only on explicit selection.",
        "Verify the native macOS consumer through the installed hirebase-v2 home launcher, including its source tree, identity, dashboard status command and JSON preview, with unchanged consumer source files."
      ],
      "passes": true,
      "dependencies": [],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/085-external-project-capabilities.md, orchestrator/external-project.mjs, root declaration binding and native acceptance",
      "external_references": [
        "Owner request, 2026-09-07: hirebase-v2 keeps all rEdit extensions outside its checkout; install a launcher in home and launch it."
      ],
      "evidence": [
        "docs/evidence/external-project-macos-2026-09-07.md"
      ]
    },
    {
      "id": 90,
      "milestone": "O1",
      "category": "workspace",
      "priority": "high",
      "description": "The project token: the instance issues one token per project root; any bound agent can contest it and, absent a rejection by the holder or the person at the desktop within the window, the token transfers to the contester at the deadline; the holder alone may launch, stop, run scripts, capture, reload or replace layers over MCP, refused by name otherwise; every bound agent can watch a filtered lifecycle feed -- token transitions, game sessions, device-bound actions, captures, layer updates -- as a resumable WebSocket its monitor reads; each agent launch gets its own identity and an agent started outside the workspace binds by discovery; all of it ships through the replaceable layers with the session host untouched.",
      "acceptance_criteria": [
        "Two agents launched on one root receive distinct identities in per-launch context files and the shared root file is no longer what the tool worker reads; an agent bound from a plain terminal through bind.mjs receives the same shape and appears in token_status once it calls anything.",
        "With the token held by A, each gated tool (launch_game, stop_session, open_script, dashboard_capture, reload_desktop, update_workspace) called by B is refused naming A, how long A has held it and token_contest, and nothing is launched, stopped, run or replaced; the same actions from the desktop succeed regardless of the holder.",
        "A contest opens a window of the configured length; the holder's token_reject within it keeps the token and the contester reads the reason; silence transfers the token at the deadline as a frame with by: deadline; a rejected contester is refused a second contest until its cooldown ends, naming the time.",
        "A contest against a holder whose pid is gone resolves at once, by: holder-gone.",
        "Reject, Grant, Revoke and Free from the desktop take effect immediately and are frames with by: desktop.",
        "Launching a game, a device-bound dashboard action starting and ending, a capture starting and being committed, a workspace layer replaced and every token transition each appear as one feed frame with a monotonic sequence; a monitor opened with after=N receives every retained frame after N; no PTY output ever appears on the feed, proven with a session that prints while the feed is watched.",
        "Replacing the workspace worker while a contest is open keeps the holder and the deadline, and the contest resolves at the original time.",
        "agentToken: 1 is advertised only by a worker serving the ledger; against an older worker the tools refuse naming the layer to update.",
        "The status-bar segment shows free, held and the countdown, and its popover carries Reject, Grant, Revoke and Free."
      ],
      "passes": false,
      "dependencies": [
        74,
        76,
        80,
        81
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/095-project-token.md; identity in orchestrator/agents/{config,launch}.mjs and a new bind.mjs; ledger, feed and gates in orchestrator/runtime/worker.mjs; token_*/feed_* tools and the identity header in orchestrator/agents/mcp-worker.mjs; the status-bar segment in orchestrator/native/app.c",
      "external_references": [],
      "evidence": [
        "docs/evidence/agent-identity-2026-09-07.md",
        "docs/evidence/project-token-2026-09-07.md",
        "docs/evidence/token-desktop-2026-09-07.md",
        "docs/evidence/project-token-e2e-2026-09-07.md",
        "docs/evidence/conversation-is-identity-2026-09-07.md"
      ]
    },
    {
      "id": 85,
      "milestone": "O1",
      "category": "workspace",
      "priority": "high",
      "description": "A headless start: run the sidecar alone, with no desktop build, no desktop spawn and no agent, so an instance can be installed on a machine that has no C toolchain and be reached over the caller's own tunnel.",
      "acceptance_criteria": [
        "A headless start neither imports build.mjs nor resolves the native binary: it comes up on a machine where no cmake, nmake or compiler is on PATH, which is the condition an SSH logon on the Windows box has and always will.",
        "It ensures the sidecar through ensureSidecar rather than starting a second kind of service, writes url/token/instance into the chosen --state directory, and answers /api/state with the same capability set the workspace service declares.",
        "--project registers the root, so the instance is useful the moment it is up.",
        "It starts no agent session and no terminal session, and it refuses --agent, --handoff, --launch-game, --inspect-ui and --declaration naming the flag and its reason, before any sidecar is started.",
        "The bind stays 127.0.0.1 with its capability token; a connection to the same port on a routable address is refused.",
        "It prints one machine-parseable ready line carrying the endpoint, then stays in the foreground; the sidecar and its retained sessions survive that process being stopped, and the process exits non-zero if the sidecar dies.",
        "It is exposed the way start and resume are: a launcher flag and an npm script. The existing full start path still builds and spawns the desktop unchanged."
      ],
      "passes": true,
      "dependencies": [],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/090-headless-workspace-start.md, orchestrator/launcher/headless.mjs, the --headless branch in orchestrator/launch.mjs, the start:headless script, and orchestrator/tests/headless.test.mjs",
      "external_references": [],
      "evidence": [
        "docs/evidence/headless-start-macos-2026-09-07.md"
      ]
    },
    {
      "id": 91,
      "milestone": "O1",
      "category": "workspace",
      "priority": "high",
      "description": "The surface owns its colour declaration: shellEnvironment sets TERM and COLORTERM, so a NO_COLOR inherited from whatever launched the workspace is dropped instead of being forwarded into every pane the session host will ever spawn, while an explicit override still suppresses colour.",
      "acceptance_criteria": [
        "A workspace launched from a shell carrying NO_COLOR=1 hands its panes an environment without it, and the pane still reports TERM=xterm-256color and COLORTERM=truecolor.",
        "A caller that passes NO_COLOR in overrides still gets it, so a project action can ask for plain output on purpose.",
        "The drop is case-insensitive on Windows, like every other name this function composes.",
        "The regression was observed failing for its own reason before the fix: the inherited value reached the composed environment."
      ],
      "passes": true,
      "dependencies": [],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/096-agent-session-resume.md; shellEnvironment in orchestrator/server/sessions.mjs; orchestrator/tests/environment.test.mjs.",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/environment.test.mjs: the surface owns its colour declaration, so an inherited NO_COLOR never reaches a pane"
      ]
    },
    {
      "id": 92,
      "milestone": "O1",
      "category": "workspace",
      "priority": "high",
      "description": "Agent panes carry the conversation they hold: rEngine names one at launch for a CLI that accepts being told, records it on the session and reports it in workspace_info, and restart_agent replaces the pane's process on that same conversation with a freshly composed environment, leaving the retained session host and every other pane untouched.",
      "acceptance_criteria": [
        "A fresh claude pane is launched with --session-id naming a UUID rEngine minted, and that identifier appears on the session record and in workspace_info.",
        "restart_agent stops the pane's child and starts a new one with --resume on the same identifier; the pane's process id changes, the session host pid does not, and no other session is affected.",
        "An agent whose CLI names its own conversations, such as codex, is recorded with no identifier, receives none of the conversation arguments, and is refused by name on restart rather than started as a second conversation.",
        "A pane never inherits another pane's conversation: the transport variables are cleared from the inherited environment before each spawn.",
        "A retained session host that predates the capability refuses restart_agent by name and says replacing the host requires quiescence.",
        "Each regression was observed failing for its own reason before the implementation existed.",
        "LIVE, still outstanding: a real agent pane restarted from a host carrying this change continues the same conversation, recorded as evidence under docs/evidence/."
      ],
      "passes": false,
      "dependencies": [
        90,
        91
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/096-agent-session-resume.md; agentConversation and agentLaunch in orchestrator/agents/config.mjs; orchestrator/agents/launch.mjs; spawnTerminal, snapshot and restartAgent in orchestrator/server/sessions.mjs; /api/agent-restart and the agentConversations capability in orchestrator/server/main.mjs; the restart_agent tool in orchestrator/agents/mcp-worker.mjs; orchestrator/tests/{agent-config,sessions}.test.mjs.",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/agent-config.test.mjs: rEngine names the conversation it launches and resumes that same one",
        "orchestrator/tests/sessions.test.mjs: restarting refuses anything it cannot put back into its own conversation",
        "orchestrator/tests/conversation-identity.test.mjs: a pane launch carries exactly one conversation, and that conversation is the identity",
        "orchestrator/tests/conversation-identity.test.mjs: a person’s own --resume wins over the host’s conversation, and the record follows the launch",
        "orchestrator/tests/conversation-identity.test.mjs: a launch that continues or forks reports no conversation rather than claiming a minted one",
        "orchestrator/tests/conversation-identity.test.mjs: a restart puts the pane back on the same conversation, so the identity survives it",
        "docs/evidence/conversation-is-identity-2026-09-07.md"
      ]
    },
    {
      "id": 93,
      "milestone": "O1",
      "category": "workspace",
      "priority": "high",
      "description": "Conversations outlive the session host that recorded them: the workspace persists them per project root, bounded and most recent first, and an agent pane offers the project's own conversations so resuming one is a choice in the pane rather than a command the person has to remember.",
      "acceptance_criteria": [
        "A conversation recorded by one session host is listed by a new host opened on the same state directory, most recently seen first, scoped to its own root.",
        "Re-recording an existing conversation touches it and moves it to the front rather than adding a second row, and the list is bounded at 20 per root with the oldest dropped.",
        "An agent pane whose project has history lists it with when each was last seen, and choosing one resumes that conversation instead of starting a second.",
        "Enter starts a new conversation and keeps the identifier the workspace minted; an out-of-range choice starts a new conversation rather than guessing.",
        "The offer is silent with no listing, an empty listing, or only another agent's rows, and a restart that already named its conversation is not asked again.",
        "A workspace-minted identifier does NOT suppress the offer: a fresh pane still sees what it could resume instead.",
        "The pane reports what it actually launched, so the session record and the persisted list follow the person's choice rather than what was minted.",
        "LIVE, still outstanding: from a host carrying this change, a pane offers a prior conversation, resuming it continues that conversation, and the offer survives a host restart. Recorded under docs/evidence/."
      ],
      "passes": false,
      "dependencies": [
        92
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/097-agent-conversation-persistence.md; listConversations and recordConversation in orchestrator/server/store.mjs; the listing, describeAge and Sessions.recordConversation in orchestrator/server/sessions.mjs; the report in orchestrator/agents/launch.mjs; /api/agent-conversation and conversations on /api/state in orchestrator/server/main.mjs; scoped conversations in orchestrator/agents/mcp-worker.mjs; choose_conversation in scripts/agent.sh; orchestrator/tests/{conversations,conversation-picker}.test.mjs.",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/conversations.test.mjs: conversations outlive the session host that recorded them",
        "orchestrator/tests/conversations.test.mjs: the remembered list is bounded, so a long-lived project cannot grow it without limit",
        "orchestrator/tests/conversation-picker.test.mjs: a pane offers the conversations this project already has",
        "orchestrator/tests/conversation-picker.test.mjs: a workspace-minted conversation is still offered the history, and Enter keeps the new one"
      ]
    },
    {
      "id": 94,
      "milestone": "O1",
      "category": "workspace",
      "priority": "high",
      "description": "A workspace's retained session host can be replaced on purpose: the launcher's --replace-host finds the host through its own descriptor and the process table (never pgrep), stops its update supervisor and then the host gracefully then forcefully, confirms the port is released, starts a fresh host from the current checkout, and reports what it stopped and started; it refuses by name a PID that serves another state directory or a launcher running inside the workspace it would replace, and a normal start says when the host is older than the code.",
      "acceptance_criteria": [
        "The host of a state directory is the PID its sidecar.json names, and only when ps shows that PID as server/main.mjs --state for that same directory; a PID that is another directory's host (vtmb-vr, nolf-improved, a worktree's preflight host), a supervisor, or any other process is refused by name with nothing signalled, and a dead PID is reported stale rather than refused.",
        "A launcher whose ancestor chain contains the host PID is refused with a message to run from a terminal outside rEngine, and the host it runs inside is untouched.",
        "Only a supervisor whose runtime.json names this host's instance, whose PID is alive and is supervisor.mjs, is stopped; it is stopped before the host; each process gets SIGTERM and SIGKILL only if SIGTERM is ignored, in that order.",
        "Against a real throwaway host: the old PID exits, its port refuses connections, sidecar.json names a new live PID with a new instance, roots persist and sessions do not, and the report names the old PID, each ended running session, each signalled process and its outcome, and the new PID.",
        "launch.mjs --headless --replace-host --state DIR comes up supervising a different host than the one that was there, and the generated .command forwards --replace-host unchanged.",
        "A normal start prints which file changed after the host started and names --replace-host; it never replaces on its own.",
        "Each regression was observed failing for its own reason: the directory comparison, the SIGKILL escalation, the ancestor refusal and the launcher's honouring of the flag were each removed in turn and only their own tests went red.",
        "LIVE, still outstanding: the owner replaces the hirebase-v2 host (PID 68944, running since 2026-09-07 09:16:13) with `~/hirebase-v2.command --replace-host` from a terminal outside rEngine, and the tracker tab, terminal colour and conversation offer then match the checkout; recorded under docs/evidence/."
      ],
      "passes": false,
      "dependencies": [
        85
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/098-replace-session-host.md; orchestrator/launcher/replace.mjs (findHost, insideHost, findSupervisors, stopProcess, portReleased, hostAge, replaceHost); --replace-host and the host-age notice in orchestrator/launch.mjs; orchestrator/tests/replace-host.test.mjs.",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/replace-host.test.mjs: the host of a state directory is the PID its descriptor names, and only if that PID serves that directory",
        "orchestrator/tests/replace-host.test.mjs: a refusal leaves the real host untouched: another directory claimed, or a launcher inside the workspace",
        "orchestrator/tests/replace-host.test.mjs: replacing a throwaway host: the old one exits, its port closes, a new one answers, and the report says so",
        "orchestrator/tests/replace-host.test.mjs: --headless --replace-host through the launcher comes up as a different host"
      ]
    },
    {
      "id": 95,
      "milestone": "O1",
      "category": "workspace",
      "priority": "high",
      "description": "The native Sessions tab is the place a person resumes or attaches agent work: it lists the bound project's agent conversations, offering Attach for one a live pane holds and Resume for one no pane holds, so getting back into a conversation is a choice in the desktop rather than a /resume typed by hand; a live agent that names its own conversations is attach-only and marked not resumable, and the two are deduplicated so a conversation a pane holds is never also offered for resume.",
      "acceptance_criteria": [
        "With conversations recorded for the project and no pane holding them, the Sessions tab lists them most recently seen first, each with a resume control keyed by its conversation id and no attach, and starts no session to make the offer.",
        "Pressing Resume starts an agent pane bound to that exact conversation id with the resume flag, not a freshly minted one, so the person never types /resume or pastes an id.",
        "A live agent pane offers Attach onto the running pane and is never offered for resume; a conversation a running pane holds appears once, as that pane's Attach.",
        "A live agent that holds no conversation id (an agent that names its own) is shown attach-only and marked not resumable rather than offering a resume that would start a second conversation.",
        "Each row shows the agent, whether it is live or past, and when it was last seen, using the describeAge wording; the desktop formats the persisted lastSeenAt and asks the service for no string.",
        "The raw process list with its Stop control is unchanged; no colour or row-size literal enters a native source.",
        "Each regression was observed failing for its own reason before the Conversations section existed.",
        "LIVE, still outstanding: from a host carrying this change, the Sessions tab lists a prior conversation, Resume brings it back rather than starting a second, and a live pane offers Attach; recorded under docs/evidence/."
      ],
      "passes": false,
      "dependencies": [
        93
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/099-sessions-tab-resume.md; the Conversations section, describe_age, conversation_session and resume_conversation in orchestrator/native/workspace.c; orchestrator/tests/native-sessions.spec.mjs; its entry in package.json test:desktop.",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/native-sessions.spec.mjs: the Sessions tab offers a project's past conversations for resume, most recent first",
        "orchestrator/tests/native-sessions.spec.mjs: Resume starts a pane bound to that same conversation, not a fresh one",
        "orchestrator/tests/native-sessions.spec.mjs: a live agent that names its own conversations is attach-only and marked not resumable"
      ]
    },
    {
      "id": 96,
      "milestone": "O1",
      "category": "workspace",
      "priority": "medium",
      "description": "Recover conversations the workspace never recorded: read the agent CLI's own session storage for the bound project and offer those alongside the ones rEngine minted, so a conversation started outside a pane, or one from before the workspace tracked it, is never lost.",
      "acceptance_criteria": [
        "For claude, conversations are discovered from its per-project session directory (the project path with separators replaced, one file per session id) and only that project's directory is read, so another project's conversations can never be offered.",
        "For codex, the same is done through its own rollout storage, validating the session's recorded working directory against the project root, generalising what handoff.mjs already does for a single explicit id.",
        "A discovered conversation is shown as discovered rather than recorded, with when it was last active; resuming one records it, after which it is an ordinary remembered conversation.",
        "Discovery reads only what it needs to identify and date a session, never its content, and is bounded to the newest handful so opening the tab stays cheap.",
        "An agent with no discoverable storage degrades to the recorded list rather than erroring.",
        "The case that motivated this is covered: a conversation resumed from a terminal, which the workspace never spawned and so never recorded, still appears for its project."
      ],
      "passes": false,
      "dependencies": [
        93,
        95
      ],
      "owner_workspace": "rengine",
      "deliverable": "A spec under docs/specs/; discovery beside orchestrator/agents/handoff.mjs, whose codex rollout lookup is the existing precedent; surfaced through the same /api/state conversations path the Sessions tab already reads.",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 97,
      "milestone": "O1",
      "category": "workspace",
      "priority": "medium",
      "description": "A Linear tracker narrows to a person and to the states that mean active: the tracker block gains optional assignee and states keys, so a workspace opens on its owner's own in-progress work instead of on every row the team holds; both keys absent behaves exactly as before, and either key under the local or GitHub backend is refused by name rather than quietly ignored.",
      "acceptance_criteria": [
        "The filter reaches Linear as a single $filter: IssueFilter variable, so the query document is one fixed string for every shape and no clause is interpolated into it.",
        "assignee \"me\" builds assignee: { isMe: { eq: true } }, whoever the token belongs to; any other value builds assignee: { displayName: { eq: value } }. Asserted on the request body, never on the rows that come back.",
        "Declared states build state: { type: { in: [...] } } from the category vocabulary the neutral row already normalises to - backlog, unstarted, started, completed, canceled - in declaration order, and both keys narrow together.",
        "With both keys absent the filter is deep-equal to the two clauses the previous document produced, including the null-safe project: { name: { eq: null } }; an undeclared key contributes no clause at all rather than an empty one Linear would read as a constraint.",
        "A category outside the vocabulary, a repeated one, an empty array, a non-array, and an assignee outside 1..64 characters are each refused by readDeclaration with a message naming the key, before any request is made; a refused declaration never reaches the network.",
        "Either key under provider local or github is refused as belonging to provider linear, the way repository, team, inventory and project already are.",
        "Two declarations differing only in their narrowing do not share a cache entry, while the same declaration twice inside the window still costs one request.",
        "Row normalisation, the thirty-second cache with in-flight coalescing, the failure vocabulary and the read-only contract are unchanged, and the desktop needs no change; no test makes a live network call.",
        "Each regression was observed failing for its own reason: six sabotages, each producing exactly the red it claims to catch, tabled in docs/evidence/tracker-filters-2026-09-07.md.",
        "LIVE, still outstanding: after the owner replaces the hirebase-v2 host with `~/hirebase-v2.command --replace-host`, that project's declaration gains the two keys and its tracker tab shows only the owner's active rows rather than a hundred; recorded under docs/evidence/."
      ],
      "passes": false,
      "dependencies": [
        78
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/100-tracker-view-filters.md; assignee and states on the tracker block in contracts/project-v1.schema.json; the linear-only rule in trackerRules (orchestrator/server/formats.mjs); linearFilter, the single-variable LINEAR_QUERY and the filter-aware cache key in orchestrator/server/tracker.mjs; orchestrator/tests/tracker-filter.test.mjs.",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/tracker-filter.test.mjs: the filter names the signed-in user, or a person by display name",
        "orchestrator/tests/tracker-filter.test.mjs: declared states reach the query as the category vocabulary, in declaration order",
        "orchestrator/tests/tracker-filter.test.mjs: with neither key declared the filter is the one the tracker has always sent",
        "orchestrator/tests/tracker-filter.test.mjs: a state category outside the vocabulary is refused when it is declared, not when it is queried",
        "orchestrator/tests/tracker-filter.test.mjs: both keys belong to Linear, and a backend that cannot honour one refuses it by name",
        "orchestrator/tests/tracker-filter.test.mjs: two declarations that ask different questions do not share one cached answer"
      ]
    },
    {
      "id": 98,
      "milestone": "O1",
      "category": "workspace",
      "priority": "high",
      "description": "New server capabilities reach a running workspace and its attached MCP agent sessions without restarting the session host: a route that needs no PTY, surface or store state is served by the replaceable workspace worker (the tracker routes are the proof, with the host’s state directory taken from its own /api/state or found in the process table by the descriptor’s instance), the MCP facade refreshes its tool worker when the connector generation changes without waiting for a request and announces tools/list_changed, a call naming a tool the workspace no longer has is answered with the current names and the way back, and list_tasks exposes the tracker to agents.",
      "acceptance_criteria": [
        "Above a retained host that answers /api/tracker with 404 and says nothing about its state directory, the worker serves GET /api/tracker, POST /api/tracker/signin and POST /api/tracker/signout from its own checkout, advertises tracker: 1, and the sign-in setup names that host’s own trackers directory, found through the process table by the descriptor’s instance.",
        "A host from this checkout reports stateDir on /api/state, and a worker above it needs no process table to name the directory.",
        "The state directory is keyed by the descriptor’s instance: a worker handed a proxy’s URL still finds the right directory, and a host that is not the first row in the table is not mistaken for another.",
        "A connector-layer update issued to the supervisor reaches an idle facade: tools/list_changed is sent with no request through the facade, the next tools/list shows the probed worker’s tools, and update_status reports a new tool-worker pid.",
        "A call naming a tool the current worker does not have is answered with the connector generation, the current tool names and the client guidance, not the SDK’s bare not-found.",
        "list_tasks returns the tracker rows through the real facade.",
        "Each regression was observed failing for its own reason: the route forwarded to the retained host; stateDir dropped from /api/state, with the host-level assertion lifted so the worker-level one discriminates alone; the URL as the key, and the first host row taken on trust; the descriptor watcher removed; the stale-name answer dropped; the facade running its sibling instead of the probed file.",
        "LIVE, read-only, done 2026-09-07: a worker from this checkout started in-process against host PID 33465 answered /api/tracker for all three roots and named the live state directory, while the same route through supervisor 44390 still returned the host’s 404; a scratch supervisor, worker and facade from this checkout above the same host listed list_tasks and delivered a connector update to an idle facade in under a second; recorded in docs/evidence/live-capability-updates-2026-09-07.md.",
        "LIVE, done 2026-09-07 13:45 after the merge: the workspace,connector update on supervisor 44390 succeeded in 337 ms; the supervisor then advertised tracker: 1 and answered GET /api/tracker with the 51 local rows where it had returned the retained host’s 404, a facade from this checkout listed and called list_tasks against the same workspace, and every retained session survived with host PID 33465 never signalled. The owner’s own connector, loaded before the facade existed, still needs /mcp → Reconnect to see list_tasks; recorded in docs/evidence/live-capability-updates-2026-09-07.md."
      ],
      "passes": false,
      "dependencies": [
        74,
        78
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/101-live-capability-updates.md; orchestrator/runtime/tracker.mjs; the tracker routes and tracker capability in orchestrator/runtime/worker.mjs; stateDir on /api/state in orchestrator/server/main.mjs with the shared signIn in orchestrator/server/tracker-auth.mjs; toolWorker in the supervisor descriptor (orchestrator/runtime/supervisor.mjs); the descriptor watcher, probed-worker choice and stale-name answer in orchestrator/agents/mcp.mjs; list_tasks in orchestrator/agents/mcp-worker.mjs; orchestrator/tests/hot-update.test.mjs with its stale-tool-worker.mjs fixture.",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/hot-update.test.mjs: the tracker routes are served by the worker above a retained host that never had them",
        "orchestrator/tests/hot-update.test.mjs: a host that says where its state lives needs no process table",
        "orchestrator/tests/hot-update.test.mjs: the state directory is keyed by the descriptor instance, never the URL or the first host row",
        "orchestrator/tests/hot-update.test.mjs: an idle facade learns of a connector update without a request, and a stale name is refused with the way back",
        "docs/evidence/live-capability-updates-2026-09-07.md: the read-only live probe of host PID 33465 and the sabotage table",
        "docs/evidence/live-capability-updates-2026-09-07.md: the live workspace,connector update on supervisor 44390 and the tracker it delivered"
      ]
    },
    {
      "id": 105,
      "milestone": "O1",
      "category": "workspace",
      "priority": "high",
      "description": "Task-driven agents (spec 103): the project token serialises workspace-mediated writes to project state -- task_add/task_update/task_decompose run the project's declared write command for the holder only, one at a time; the Sessions tab marks the holder and revokes or frees the token beside it; the Tasks pane spawns a chosen agent and model on a task (a conversation that records the task) or decomposes it into subtasks through an agent brief; prompts are project files with shipped defaults; contract 6 adds tracker.write and agents.",
      "acceptance_criteria": [
        "On the Sessions tab the live agent holding the token is marked and offers Revoke; pressing it frees the token as a token.revoked frame by desktop and another agent's token_contest then claims it; a holder whose process is gone offers Free with the same effect.",
        "task_add, task_update and task_decompose from a non-holder are refused naming the holder and token_contest with nothing written; from the holder they run the declared tracker.write command with the row as JSON; two holders in sequence never interleave a write, proved with a slow fake write command and two callers.",
        "A project without tracker.write is told so by name; GitHub and Linear providers refuse writes by name.",
        "Spawn from the Tasks pane starts an agent pane with the chosen CLI and model on a fresh conversation seeded with the task prompt, records task on the conversation, the Sessions tab and workspace_info show the task beside the agent, and agent.spawned is on the feed.",
        "Decompose starts an agent on the decomposition prompt; with a scripted agent the resulting task_add calls create child rows with parent set and no other file changes.",
        ".rengine/prompts/*.md override the shipped prompts, a missing file uses the default, and a misspelled placeholder is reported rather than emptied.",
        "Contract 6 (tracker.write, agents) validates, a contract-5 declaration is accepted unchanged, and an older host is refused by name on the new keys.",
        "Everything ships in replaceable layers; the host changes only to accept task on the conversation record, refused by name before the next replacement."
      ],
      "passes": false,
      "dependencies": [
        78,
        90,
        93,
        95
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/103-task-driven-agents.md; Sessions-tab token controls in orchestrator/native/workspace.c; task write routes and agent-spawn in orchestrator/runtime/worker.mjs; task_*/spawn_agent tools in orchestrator/agents/mcp-worker.mjs; Tasks-pane controls in orchestrator/native/tracker.c; contract 6 in contracts/project-v1.schema.json; shipped prompts under orchestrator/templates/prompts",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 99,
      "milestone": "O1",
      "category": "workspace",
      "priority": "medium",
      "description": "rEdit is a Claude Code IDE: the workspace publishes the lock file the CLI reads, serves MCP over the WebSocket it names, and pushes the editor's selection, so an agent pane running in the workspace can connect to the editor it is running inside rather than to nothing.",
      "acceptance_criteria": [
        "The published lock is the one the CLI parses: named <port>.lock for the port actually listening, useWebSocket true, ideName rEdit, workspaceFolders the bound roots, and pid the SESSION HOST's — the only process in a pane's ancestor chain, which is what the CLI checks.",
        "With no host pid to name, nothing is published and the reason says so, rather than leaving a lock the CLI silently skips.",
        "A real MCP client over the socket completes initialize and tools/list and reads getDiagnostics, which answers an empty diagnostic list because rEdit runs no language server.",
        "The socket requires the lock's token in the header the CLI actually uses, and refuses a connection without it with a policy close; the mcp subprotocol the CLI asks for is echoed back.",
        "A selection posted by the desktop reaches every connected CLI as a selection_changed notification.",
        "Stale rEdit locks are collected at startup and the bridge unlinks its own on retirement, because the lock names the long-lived host and the CLI's own sweep — which deletes locks whose pid is dead — can therefore never collect ours; another IDE's lock is left alone.",
        "Each regression was observed failing for its own reason: the lock naming the worker, publishing without a host pid, diagnostics refusing, an untrusted connection admitted, the notification renamed, the sweep taking another IDE's lock, the subprotocol dropped, and close leaving its lock behind.",
        "LIVE, done 2026-09-07: the real claude 2.1.263 in a PTY, cwd inside a bound root, showed 'Select IDE ... 1. rEdit' and reported 'Connected to rEdit.'; the accepted handshake presented the lock's token in x-claude-code-ide-authorization and negotiated the mcp subprotocol, and print mode was found never to connect at all. Recorded in docs/evidence/editor-as-claude-ide-2026-09-07.md.",
        "FOLLOW-UP, not in this row: the desktop's own reporting of its editor selection over the POST route this serves, at_mentioned from an explicit send-to-Claude gesture, and openDiff with accept/reject rendered in a pane."
      ],
      "passes": false,
      "dependencies": [
        74
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/102-editor-as-claude-ide.md; orchestrator/runtime/ide.mjs; the bridge lifecycle and POST /api/ide-selection in orchestrator/runtime/worker.mjs; pid on /api/state in orchestrator/server/main.mjs; orchestrator/tests/ide.test.mjs.",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/ide.test.mjs: the lock file is the one Claude Code knows how to read",
        "orchestrator/tests/ide.test.mjs: an MCP client over the socket lists the tools and reads diagnostics",
        "orchestrator/tests/ide.test.mjs: a connection without the token is refused",
        "orchestrator/tests/ide.test.mjs: a selection posted by the desktop reaches a connected CLI",
        "orchestrator/tests/ide.test.mjs: stale locks are collected and the bridge removes its own",
        "docs/evidence/editor-as-claude-ide-2026-09-07.md: the real CLI connecting, the observed handshake and the eight sabotages"
      ]
    },
    {
      "id": 100,
      "milestone": "O1",
      "category": "workspace",
      "priority": "high",
      "description": "The editor tells the agent what the person is looking at: the focused editor pane reports its file and selected range to the workspace, which pushes selection_changed to every connected CLI, and an explicit send-to-Claude gesture in the pane sends at_mentioned, so an agent in a pane can act on the selection instead of being told about it.",
      "acceptance_criteria": [
        "A selection made in a focused editor pane reaches every connected CLI as selection_changed carrying the file path, the selected text and a start/end line and character range, and moving the caret without selecting reports the empty range rather than nothing.",
        "The range is line and character as the CLI counts them, verified against a file with tabs and multi-byte characters, not byte offsets into the buffer.",
        "Only the focused pane reports: switching panes or tabs changes what the agent sees, and a pane that loses focus stops reporting rather than leaving a stale selection standing.",
        "An explicit gesture in the editor pane sends at_mentioned for the current file and selection, and is distinguishable in the CLI from the passive selection stream. MET: a labelled \"To agent\" control in the editor header, drawn only where the workspace serves the IDE routes, sending { filePath, lineStart, lineEnd } — the CLI's own schema, read from its binary rather than guessed. A key chord was rejected: an editor that grows a gesture nobody can find is worse than one that waits. LIVE 2026-09-07: the route answered `delivered: 1` against a really-connected CLI, which establishes that the notification reached it; how that CLI renders a mention was not observed.",
        "Reporting is bounded: a selection larger than a declared cap is reported as a range with truncated text rather than sending an unbounded buffer over the socket, and rapid selection changes are coalesced rather than sent per keystroke.",
        "With no CLI connected the desktop still runs unchanged and posts nothing, and a worker replaced under a live desktop resumes receiving without the desktop restarting.",
        "Each regression was observed failing for its own reason."
      ],
      "passes": false,
      "dependencies": [
        99
      ],
      "owner_workspace": "rengine",
      "deliverable": "A selection accessor over ReEditor's existing select_start/select_end in orchestrator/native/editor.{c,h}, the report from the focused pane in the desktop, and the existing POST /api/ide-selection it feeds; docs/specs/102-editor-as-claude-ide.md slice 2.",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/native-ide-selection.spec.mjs: the focused editor reports its selection in the units the protocol counts",
        "orchestrator/tests/ide.test.mjs: the worker resolves the desktop's root and path into the file path the CLI is given",
        "docs/evidence/editor-as-claude-ide-2026-09-07.md: the three native sabotages, and the live selection that reached a connected CLI",
        "orchestrator/tests/ide.test.mjs: the deliberate mention is a different notification from the passive selection",
        "orchestrator/tests/native-diagnostics.spec.mjs: the pane offers the control and says it sent"
      ]
    },
    {
      "id": 101,
      "milestone": "O1",
      "category": "workspace",
      "priority": "medium",
      "description": "Claude's edits are reviewed in the editor rather than in the terminal: the workspace serves openDiff, close_tab and closeAllDiffTabs, and a diff opens as a tab in rEdit whose accept or reject is the answer the CLI is waiting on.",
      "acceptance_criteria": [
        "openDiff({old_file_path, new_file_path, new_file_contents, tab_name}) opens a diff tab in the desktop showing the proposed contents against what is on disk.",
        "Accepting resolves the call as diff_accepted and rejecting as diff_rejected, in the shape the CLI expects, and the CLI's own terminal diff is not also shown.",
        "close_tab({tab_name}) and closeAllDiffTabs({}) close what they name and report what they closed, including when the person already closed the tab by hand.",
        "A diff whose file changes on disk while the tab is open says so rather than silently applying against a stale base.",
        "A CLI that disconnects while a diff tab is open leaves the tab in a state the person can dismiss, and no promise is left pending forever.",
        "Each regression was observed failing for its own reason."
      ],
      "passes": false,
      "dependencies": [
        99,
        100
      ],
      "owner_workspace": "rengine",
      "deliverable": "The diff tools in orchestrator/runtime/ide.mjs, a diff tab in the native desktop, and the request path that carries the verdict back; docs/specs/102-editor-as-claude-ide.md slice 2.",
      "external_references": [],
      "evidence": []
    },
    {
      "id": 102,
      "milestone": "O1",
      "category": "workspace",
      "priority": "high",
      "description": "rEdit speaks the Language Server Protocol: a project declares its language servers, the workspace worker runs them and keeps their documents in sync with the editor's buffers, and their diagnostics reach both the editor pane and getDiagnostics — so an agent asks the editor what is broken and gets the same answer the person is looking at.",
      "acceptance_criteria": [
        "A project declares its language servers by command and by the file patterns each serves; nothing is downloaded or installed by rEngine, and an undeclared project keeps answering an empty diagnostic list rather than guessing at a server.",
        "A declared server that is missing from the machine is reported as unavailable, by name and with what was looked for, and the workspace keeps working — an editor pane still opens and edits files.",
        "The worker owns the server processes and the LSP session: initialize with the workspace roots, textDocument/didOpen, didChange and didClose driven by what the editor actually has open, and shutdown when the last document closes or the worker retires, leaving no orphaned server process.",
        "Document sync carries the editor's unsaved buffer, not the file on disk, so diagnostics describe what the person is looking at; positions cross the boundary as UTF-16 line and character, which is what the protocol counts, verified against tabs and multi-byte characters.",
        "publishDiagnostics reaches two consumers from one store: the editor pane renders them, and mcp__ide__getDiagnostics answers with the same set for a file, so the agent and the person never disagree about what is broken. MET: the pane asks the worker twice a second, is answered `unchanged` almost every time, and draws an underline under each reported range coloured by severity; the same store answers mcp__ide__getDiagnostics. LIMIT, recorded rather than glossed: the spec asserts the count the pane holds and that it stays held, not the cells the underline lands on — the draw loop's own column tracking is exercised on every frame but not asserted positionally.",
        "A server that crashes is restarted with a bounded backoff, its diagnostics are cleared rather than left stale, and repeated failure is reported instead of retried forever.",
        "Parsed output from a declared check action remains a second source merged into the same store, distinguishable by origin, because a build reports failures no language server sees. NOT MET: the second source is not built; only the protocol's own diagnostics are served.",
        "Each regression was observed failing for its own reason."
      ],
      "passes": false,
      "dependencies": [
        99,
        63
      ],
      "owner_workspace": "rengine",
      "deliverable": "An LSP client owned by the workspace worker, the language-server block in the project declaration (contract 6), document sync from the native editor, and the diagnostics store behind getDiagnostics and the editor's own rendering. Charter D37; docs/specs/102-editor-as-claude-ide.md.",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/lsp.test.mjs: a declared server is started, told the buffer, and its diagnostics are what getDiagnostics reads",
        "orchestrator/tests/lsp.test.mjs: a declared server that is not on the machine is named, and the workspace keeps working",
        "orchestrator/tests/lsp.test.mjs: a server that crashes is restarted, and its diagnostics do not outlive it",
        "orchestrator/tests/ide.test.mjs: getDiagnostics answers with what the project's declared language server published",
        "docs/evidence/editor-as-claude-ide-2026-09-07.md: the six LSP sabotages",
        "orchestrator/tests/native-diagnostics.spec.mjs: the editor pane draws what the project's language server said"
      ]
    },
    {
      "id": 103,
      "milestone": "O1",
      "category": "workspace",
      "priority": "medium",
      "description": "An agent pane is connected to the editor it runs inside without anyone typing /ide: the workspace launches a supported CLI with its own auto-connect option when exactly one rEdit is published for that root.",
      "acceptance_criteria": [
        "A newly launched Claude pane is connected to rEdit without a slash command, and the pane prints the reason once at startup rather than on every turn.",
        "A CLI with no such option, or a directory with no published editor, launches exactly as it does today with nothing added to its command line, and the reason says which case it was.",
        "The option is carried by a table in the agent launch configuration, so adding a CLI that supports it is a declaration rather than a code change.",
        "The pane connects to THIS workspace's editor, named by port rather than chosen by counting: the session host is an ancestor of every pane it forks, so the lock naming a pid in this process's ancestor chain is ours, and its port is passed as CLAUDE_CODE_SSE_PORT, which the CLI honours to select one editor outright. Counting was the first rule and would have declined forever on this machine, where two workspaces bind /Users/alex/rengine; it remains the fallback when the ancestor chain identifies none of the offered editors.",
        "An editor is offered for a directory only when a declared folder actually contains it: a sibling whose name merely starts with the folder's name (/work/rengine-old against /work/rengine) is not inside it, and a lock whose process is gone is not offered at all.",
        "CORRECTED: an earlier criterion said this could only reach a running workspace by replacing its session host. That is wrong. The host spawns scripts/agent.sh, which execs orchestrator/agents/launch.mjs from the checkout at every pane launch, so the decision is made in the pane by code read from disk — it reaches a running workspace with no host replacement and no layered update.",
        "Each regression was observed failing for its own reason: the flag passed whenever any editor is offered, a bare prefix used for containment, a dead editor still offered, and every CLI given the flag.",
        "LIVE, done 2026-09-07: a newly opened Claude pane in this workspace came up connected without a slash command. Its /ide menu listed two editors — hirebase-v2's workspace (which also binds /Users/alex/rengine) and this one — with the check mark already on this one, and POST /api/ide-selection reported delivered: 2, the existing pane and the new one. The launch resolved this pane's ancestors to session host 33465 and passed CLAUDE_CODE_SSE_PORT=65353. Recorded in docs/evidence/editor-as-claude-ide-2026-09-07.md."
      ],
      "passes": false,
      "dependencies": [
        99
      ],
      "owner_workspace": "rengine",
      "deliverable": "orchestrator/agents/ide-connect.mjs; the FLAGS table and the plan's ide decision in orchestrator/agents/config.mjs; the cwd and the printed reason in orchestrator/agents/launch.mjs; orchestrator/tests/ide-connect.test.mjs.",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/ide-connect.test.mjs: exactly one is the rule, because that is the rule the CLI applies",
        "orchestrator/tests/ide-connect.test.mjs: an editor is offered when its folders cover the directory, and not when they merely look like it",
        "orchestrator/tests/ide-connect.test.mjs: a CLI with no auto-connect option gets nothing added to its command line",
        "orchestrator/tests/ide-connect.test.mjs: the launch plan carries the flag, and says why when it does not",
        "docs/evidence/editor-as-claude-ide-2026-09-07.md: the four-editor measurement that set the rule",
        "orchestrator/tests/ide-connect.test.mjs: this workspace's own editor is named by port, so a machine-mate's does not block it",
        "docs/evidence/editor-as-claude-ide-2026-09-07.md: the pane that came up connected with two editors offered"
      ]
    },
    {
      "id": 106,
      "milestone": "O1",
      "category": "workspace",
      "priority": "medium",
      "description": "A project's brand mark and wordmark are its own artwork: the chrome rasterises declared SVG in place of the letter chip and the title text, and falls back to the glyph when a declaration names a file it cannot read.",
      "acceptance_criteria": [
        "A declared icon.image is drawn in the mark chip and its own ink reaches the screen, rather than a letter on a token fill.",
        "A declared wordmark is drawn in place of the title text, and a light/dark pair follows the theme the bar is drawn on.",
        "The OS window title stays the declared text, because no image can reach it.",
        "Artwork that is missing, malformed or oversized leaves the glyph and the title in place, and the workspace reports the problem rather than swallowing it.",
        "icon carries exactly one of glyph and image; artwork paths are declaration-relative, refused when they escape, and refused below contract 8.",
        "Rasterising happens once per file and pixel box, not once per frame."
      ],
      "passes": true,
      "dependencies": [
        79
      ],
      "owner_workspace": "rengine",
      "deliverable": "third_party/nanosvg, orchestrator/native/svg.{c,h}, contract 8, toolbar_brand artwork",
      "external_references": [],
      "evidence": [
        "docs/specs/104-project-brand-artwork.md",
        "orchestrator/native/tests/svg_test.c (native_svg): the real Kohai mark and wordmark rasterise, uniform fit, refusals",
        "orchestrator/tests/contracts.test.mjs: exactly one mark, .svg only, confined paths, contract-8 floor, missing file reported",
        "orchestrator/tests/native-identity.spec.mjs: the chrome wears the artwork (#CC4F4C found in the bar) and falls back to the glyph without it"
      ]
    },
    {
      "id": 107,
      "milestone": "O1",
      "category": "workspace",
      "priority": "medium",
      "description": "A workspace can restart its own update supervisor without ending a session: an action in the rEdit dashboard stops the supervisor for a state directory and starts a detached one from the current checkout, so the layer that performs layered updates — and therefore cannot receive one — can be replaced from inside the workspace it serves.",
      "acceptance_criteria": [
        "The session host is never signalled: the host for the state directory is found through its own descriptor confirmed against the process table, a PID that is not that host is refused by name with nothing signalled, and another workspace's host and supervisor are left alone.",
        "The replacement is started detached, with no inherited pipes, so it survives the pane that asked for it — which is normally a pane inside the workspace being restarted, whose window the restart closes.",
        "The launcher is invoked without --replace-host, adopting the running host rather than ending its sessions; the absence of that flag is asserted.",
        "The report names the host it kept and each supervisor it stopped, including how many desktop windows close with it, so a vanishing editor window is predicted rather than surprising.",
        "It is reachable as a declared dashboard action, opening as a retained interactive script tab like every other action, and it asks for confirmation before signalling anything.",
        "Each regression was observed failing for its own reason: the host added to the stop list, the replacement left attached, --replace-host added to the launcher, and the plan computed after stopping had begun.",
        "LIVE, done 2026-09-07 17:27 on the owner's own workspace: the action's tool stopped supervisor 44390 (2 managed desktop windows) and started a detached replacement; the workspace came back as supervisor 57193, worker 57195 and desktop 57227 with all 13 sessions, and session host 33465 was never signalled. Recorded in docs/evidence/editor-as-claude-ide-2026-09-07.md.",
        "Every criterion above is met including the live run, but this row stays passes: false because its prerequisite F94 (--replace-host, whose process-table helpers this is built on) is not itself verified. The dependency is real rather than bookkeeping: the refusals that keep the session host safe are F94's code."
      ],
      "passes": false,
      "dependencies": [
        94
      ],
      "owner_workspace": "rengine",
      "deliverable": "orchestrator/launcher/restart-supervisor.mjs, orchestrator/actions/restart-supervisor.sh, its dashboard declaration in .rengine/project.json, and orchestrator/tests/restart-supervisor.test.mjs.",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/restart-supervisor.test.mjs: the session host is never among the processes stopped",
        "orchestrator/tests/restart-supervisor.test.mjs: the replacement is detached, or it would die with the pane that asked for it",
        "orchestrator/tests/restart-supervisor.test.mjs: a state directory with no live host is refused, and nothing is signalled",
        "docs/evidence/editor-as-claude-ide-2026-09-07.md: the live supervisor restart on the owner's workspace"
      ]
    },
    {
      "id": 109,
      "milestone": "O1",
      "category": "integration",
      "priority": "medium",
      "description": "A pack is one pinned, versioned artifact a project declares, and its facets say how it is consumed: contract 9 adds a packs block whose library facet is source and a CMake target a game consumes at build time and whose plugin facet is a module the editor loads at run time, with \"Powered by rEngine\" checkable on the library facet alone.",
      "acceptance_criteria": [
        "A contract-9 declaration whose pack declares both facets reads back through readDeclaration with its name, pin, library and plugin intact and packsError unset; the renderer's both-facets shape and iklib's library-only shape both read.",
        "A contract-8 declaration that names packs is refused with \"packs requires contract 9\", never as an unknown key, and its formats still read; a contract past the ceiling is refused whole and the refusal names the contracts that are supported.",
        "A pin is a spoken version and a checked revision: a 40- or 64-character lowercase hex digest is accepted and a branch, a tag, a short prefix, an uppercase digest and an over-long string are each refused by name, because a version alone is a claim rather than a pin.",
        "Each refusal is by name and specific: a facet key under the wrong facet says which facet owns it, a pack with no facet, a repeated pack name, an escaping library.path or plugin.module, and poweredBy on a plugin-only pack each name the pack and the key. A pack with no name is placed by its index and never printed as undefined.",
        "D24 stays as written: poweredBy is accepted on a library facet and refused on a plugin-only pack, so an editor plugin does not earn the claim. It names the pack the claim rests on and asserts nothing about the game integration checks, whose evidence is F21/F29.",
        "A declaration that does not name packs is unchanged: a document carrying all ten blocks of contracts 1-8 reads with the same keys, the same values and no new ones, and packs cannot leak into a result that never asked for it.",
        "Nothing in this feature acquires, downloads, unpacks, loads or interprets a pack. The block carries no URL, registry or fetch step, the reader opens neither library.path nor plugin.module, and plugin.abi is shape-checked and never parsed, ordered or compared. Acquisition stays with KI-008, loading with D38's own spec, editions with D40.",
        "Each regression was observed failing for its own reason: fourteen sabotages, including D24 inverted onto the plugin facet, a tag accepted as a revision, the block never reaching its section and the block leaking into a declaration that never named it. Recorded in docs/evidence/pack-manifest-2026-09-07.md."
      ],
      "passes": true,
      "dependencies": [
        106
      ],
      "owner_workspace": "rengine",
      "deliverable": "docs/specs/107-pack-manifest.md, the packs block and $defs.pack in contracts/project-v1.schema.json, packsRules plus the contract-9 ceiling and SECTIONS entry in orchestrator/server/formats.mjs, and orchestrator/tests/packs.test.mjs.",
      "external_references": [],
      "evidence": [
        "docs/specs/107-pack-manifest.md: the manifest shape, the pin's two halves and the forms that were rejected",
        "orchestrator/tests/packs.test.mjs: contract 9 carries a pack whose facets are a library and a plugin",
        "orchestrator/tests/packs.test.mjs: packs on contract 8 is refused for the contract it needs, not as an unknown key",
        "orchestrator/tests/packs.test.mjs: each pack refusal names the pack and the key",
        "orchestrator/tests/packs.test.mjs: a declaration that does not name packs reads exactly as it did (the ten blocks of contracts 1-8)",
        "docs/evidence/pack-manifest-2026-09-07.md: the fourteen-row sabotage table, including the inverted D24 that only the acceptance test caught",
        "orchestrator/tests/task-writes.test.mjs: the contract-ceiling tripwire, raised to 9 with contract 6's own keys reconfirmed"
      ]
    },
    {
      "id": 110,
      "milestone": "O1",
      "category": "workspace",
      "priority": "medium",
      "description": "The product is Red, and its name stops being hard-coded: one declaration in orchestrator/native/theme.json is generated into theme.h for the desktop and orchestrator/runtime/product.mjs for the workspace layer, every consumer reads the generated value, and a guard fails when shipping code spells the name instead (charter D41, spec 108).",
      "acceptance_criteria": [
        "The name is declared once and generated: theme.json carries the product block, tools/design.py generate writes RE_PRODUCT_NAME and RE_PRODUCT_SUITE into theme.h and PRODUCT_NAME and PRODUCT_SUITE into orchestrator/runtime/product.mjs, and design.py check fails when either generated file is stale or hand-edited.",
        "All four consumers read the generated value: RE_DEFAULT_TITLE is an alias of RE_PRODUCT_NAME, IDE_NAME is PRODUCT_NAME, the LSP clientInfo name is PRODUCT_NAME, and the tracker sign-in callback page takes its title from PRODUCT_NAME.",
        "The rename to Red is complete in shipping code: grepping the shipping tree finds the declaration, the generated output and one deliberate canary, and nothing hand-written; dated records (Codex-progress.md, docs/evidence, earlier features rows, charter D36's own wording) keep the old name on purpose.",
        "The published change is safe: the lock file publishes the declared name and orchestrator/agents/ide-connect.mjs still recognises this workspace's own editor by comparing against the same export, so auto-connect (F103) cannot silently stop matching after a rename.",
        "A guard fails when a literal product name — current or retired — is reintroduced in shipping code, in a string or outside one, while a comment naming the product is left alone.",
        "Breaking the generated path fails loudly rather than falling back to a stale string: without the generated module the JS consumers throw at import, and without the macro the desktop does not compile.",
        "Each regression was observed failing for its own reason, with the runs recorded: a hard-coded current name, a hard-coded retired name, a hand-edited theme.h, a hand-edited product.mjs, a declaration changed without regenerating, a deleted module and a deleted macro."
      ],
      "passes": true,
      "dependencies": [
        79
      ],
      "owner_workspace": "rengine",
      "deliverable": "The product block in orchestrator/native/theme.json, the generator and guard in tools/design.py, the generated orchestrator/runtime/product.mjs, the rewired consumers in orchestrator/native/app.h, orchestrator/runtime/ide.mjs, orchestrator/runtime/lsp.mjs and orchestrator/server/tracker-auth.mjs, and orchestrator/tests/product-name.test.mjs.",
      "external_references": [],
      "evidence": [
        "orchestrator/tests/product-name.test.mjs: the generated artifacts carry the declaration rather than a copy of it",
        "orchestrator/tests/product-name.test.mjs: the lock file other people read publishes the declared name, and IDE_NAME is that name",
        "orchestrator/tests/product-name.test.mjs: a language server is told the declared name, read off the bytes sent to it",
        "orchestrator/tests/product-name.test.mjs: the sign-in callback page wears the declared name, read off the page it serves",
        "orchestrator/tests/product-name.test.mjs: no shipping source hard-codes the product name, and the guard goes red on a decoy that holds one in a string and in a regular expression while its comment is left alone",
        "docs/evidence/product-name-2026-09-07.md: the sabotage table, including the two runs that break the generated path"
      ]
    },
    {
      "id": 108,
      "milestone": "O1",
      "category": "workspace",
      "priority": "medium",
      "description": "An extension is an in-process native plugin (charter D38): a module built against one C header loads by absolute path, negotiates its ABI and draw-list versions, registers tabs through the owned layer and draws by appending to the frame's draw list — and a module that will not load, exports no entry point or declares an incompatible ABI is refused by name while the window keeps running.",
      "acceptance_criteria": [
        "A plugin built against orchestrator/native/plugin_abi.h loads from its absolute module path in a real window; the tab it registered appears in the layout with its title, and its draw-list commands reach the frame — the identifiable colour it draws is read back from a snapshot inside the tab's rectangle.",
        "The ABI is versioned separately from the draw list and a plugin declares both: an ABI version other than the desktop's — older or newer — is refused naming the module path and both versions, and its start is never called; a newer draw-list version is refused the same way and an older one is accepted, because the list is additive (spec 067).",
        "A declaration whose abi string is not this desktop's is refused before the module file is touched; a path that will not open, a file that is not a module, a relative path, a module without re_plugin_entry, a descriptor whose name disagrees with the declaration, and a start that declines are each refused by name and skipped, and the window keeps answering and rendering afterwards.",
        "Every plugin the window was asked to load has a row in the inspected state as loaded or refused with its reason; loading the same name twice is one plugin and one tab, so a declaration that arrives repeatedly does not touch the module again.",
        "A plugin's frame opens with the clip set to its tab's area, a clip it sets can only narrow that area, the frame closes with a clip reset whatever the plugin did, registration outside start is refused, and a theme colour is resolved by token name from the live theme so a preset switch reaches the plugin on the next frame.",
        "A plugin tab is persisted by identity like any other view: a restarted window restores it as a placeholder naming the plugin, and the same tab — not a second one — comes alive when the plugin loads again.",
        "The plugin reaches no store, session or host state: the host table it receives holds registration, drawing, clipping, measurement and colour lookup and nothing else (D38). Controls, textures, input, a reopen menu and unloading are deferred with their reasons in spec 106.",
        "NOT MET — Windows: the loader compiles to LoadLibrary/GetProcAddress and the fixture modules build as DLLs from the same TOML, but no plugin has been loaded on Windows while the desktop suite's repair (KI-038) is outstanding. This row stays passes: false until that evidence exists.",
        "Each regression was observed failing for its own reason: the sabotage table in docs/evidence/plugin-abi-2026-09-07.md."
      ],
      "passes": false,
      "dependencies": [
        56,
        60
      ],
      "owner_workspace": "rengine",
      "deliverable": "orchestrator/native/plugin_abi.h (the header a plugin compiles against), orchestrator/native/plugin.{c,h} (registry, version negotiation, the one dlopen/LoadLibrary wrapper), orchestrator/native/pluginview.{c,h} (RE_PLUGIN views and re_app_plugin_load for the declaring lane), the automation `plugin` op, the fixture modules under orchestrator/native/tests/plugins/, rengine_plugin_test (CTest native_plugin), orchestrator/tests/native-plugin.spec.mjs; docs/specs/106-plugin-abi.md.",
      "external_references": [],
      "evidence": [
        "docs/specs/106-plugin-abi.md",
        "orchestrator/native/tests/plugin_test.c (native_plugin): real modules loaded and refused by name for each reason, the ten-command frame, idempotence, colour by token",
        "orchestrator/tests/native-plugin.spec.mjs: the fixture's tab and its pixels in a real window, the clip, the restart; refusals with the window still answering",
        "docs/evidence/plugin-abi-2026-09-07.md: the sabotage table"
      ]
    }
  ]
}
