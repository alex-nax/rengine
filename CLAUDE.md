# Claude Code entry point

@AGENTS.md

`AGENTS.md` is the canonical workflow for every agent. Use `Codex-progress.md` as the shared
session log. Product decisions and integration boundaries live in the linked documents.
